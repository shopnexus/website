NEXT_PUBLIC_WS_URL=ws://localhost:5000/api/v1/ws
# The Google Cloud "Web application" OAuth client id. Public by design — it is the
# audience the API checks the id token against (config oauth.google_audiences).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
