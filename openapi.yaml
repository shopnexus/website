components:
    parameters:
        Cursor:
            description: Opaque cursor from the previous page's `next_cursor`. Omit for the first page.
            in: query
            name: cursor
            required: false
            schema:
                type: string
        Limit:
            in: query
            name: limit
            required: false
            schema:
                default: 20
                maximum: 100
                minimum: 1
                type: integer
    responses:
        BadRequest:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Validation or malformed request
        Conflict:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Conflicts with the current state (already exists, already done, wrong state)
        Forbidden:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Authenticated but not allowed to act on this resource
        NotFound:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Resource not found
        Unauthorized:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Missing or invalid credentials
        UnprocessableEntity:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Well-formed but rejected by a business rule
    schemas:
        AcceptOfferRequest:
            description: Acceptance still needs a delivery address and a carrier — the negotiation only settled price and quantity.
            properties:
                contact_id:
                    $ref: '#/components/schemas/ContactID'
                note:
                    maxLength: 500
                    type: string
                transport_option:
                    maxLength: 100
                    pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
                    type: string
            required:
                - contact_id
                - transport_option
            type: object
        AccountID:
            example: acc_62mxefynht57b
            pattern: ^acc_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        AccountRole:
            description: |
                `user` self-registers and both buys and sells. `moderator` is granted by an admin. `admin` is configured, not created.
            enum:
                - user
                - moderator
                - admin
            type: string
        AccountStatus:
            enum:
                - active
                - suspended
            type: string
        AccountSummary:
            description: The compact form used in follower and following lists.
            properties:
                avatar:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                id:
                    $ref: '#/components/schemas/AccountID'
                name:
                    type: string
            required:
                - id
                - name
            type: object
        AccountSummaryPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/AccountSummary'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        AddAttachmentsRequest:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    minItems: 1
                    type: array
            required:
                - attachments
            type: object
        AddCartItemRequest:
            properties:
                quantity:
                    format: int64
                    minimum: 1
                    type: integer
                sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
            required:
                - sku_id
                - quantity
            type: object
        AdminAccount:
            description: |
                The staff view of an account. Carries the identifiers and the suspension state that `PublicAccount` withholds.
            properties:
                created_at:
                    format: date-time
                    type: string
                email:
                    nullable: true
                    type: string
                email_verified:
                    type: boolean
                id:
                    $ref: '#/components/schemas/AccountID'
                identity_verified:
                    type: boolean
                name:
                    type: string
                phone:
                    nullable: true
                    type: string
                role:
                    $ref: '#/components/schemas/AccountRole'
                status:
                    $ref: '#/components/schemas/AccountStatus'
                suspended_until:
                    description: Null while suspended means the suspension is permanent.
                    format: date-time
                    nullable: true
                    type: string
                suspension_reason:
                    nullable: true
                    type: string
                username:
                    nullable: true
                    type: string
            required:
                - id
                - email_verified
                - name
                - role
                - status
                - identity_verified
                - created_at
            type: object
        AdminAccountPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/AdminAccount'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        AdminIdentityDocument:
            description: A queue entry, which needs the subject alongside the document.
            properties:
                account:
                    $ref: '#/components/schemas/AccountSummary'
                document:
                    $ref: '#/components/schemas/IdentityDocument'
            required:
                - document
                - account
            type: object
        AdminReport:
            description: A queue entry. A moderator needs the reporter and the target beside the report itself.
            properties:
                open_reports_against_target:
                    description: |
                        How many other unresolved reports name the same target. A pattern is what a decision rests on rather than a single complaint.
                    format: int64
                    type: integer
                report:
                    $ref: '#/components/schemas/Report'
                reporter:
                    $ref: '#/components/schemas/AccountSummary'
                resolved_by:
                    allOf:
                        - $ref: '#/components/schemas/AccountSummary'
                    nullable: true
                target:
                    additionalProperties: true
                    description: |
                        The reported content, shaped by `ref_type`. Fetched from the module that owns it.
                    type: object
            required:
                - report
                - reporter
                - open_reports_against_target
            type: object
        AdminReportPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/AdminReport'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        AuthResult:
            properties:
                access_token:
                    type: string
                account:
                    $ref: '#/components/schemas/Me'
                expires_in:
                    description: Access token lifetime in seconds.
                    type: integer
                refresh_token:
                    type: string
            required:
                - access_token
                - refresh_token
                - expires_in
                - account
            type: object
        BankAccount:
            properties:
                account_holder:
                    type: string
                account_number_masked:
                    description: Only the last digits. The full number leaves the system solely to move money.
                    example: '****1234'
                    type: string
                bank_code:
                    description: Bank identifier, e.g. `vcb`
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/BankAccountID'
                is_default:
                    type: boolean
            required:
                - id
                - bank_code
                - account_number_masked
                - account_holder
                - is_default
                - created_at
            type: object
        BankAccountID:
            example: bnk_2qr5wtc4mx8jd
            pattern: ^bnk_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        BankAccountList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/BankAccount'
                    type: array
            required:
                - items
            type: object
        CancellationRequest:
            properties:
                reason:
                    maxLength: 2000
                    type: string
            required:
                - reason
            type: object
        CartItem:
            properties:
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/CartItemID'
                quantity:
                    format: int64
                    type: integer
                sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
            required:
                - id
                - sku_id
                - quantity
                - created_at
            type: object
        CartItemID:
            example: crt_3n8kvq1wd6ptz
            pattern: ^crt_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        CartItemPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/CartItem'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        Category:
            properties:
                description:
                    type: string
                id:
                    $ref: '#/components/schemas/CategoryID'
                name:
                    type: string
                parent_id:
                    allOf:
                        - $ref: '#/components/schemas/CategoryID'
                    description: Null for a root category.
                    nullable: true
            required:
                - id
                - name
                - description
            type: object
        CategoryID:
            example: cat_7v2p41ryaj811
            pattern: ^cat_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        CategoryList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Category'
                    type: array
            required:
                - items
            type: object
        ChangePasswordRequest:
            properties:
                current_password:
                    type: string
                new_password:
                    maxLength: 72
                    minLength: 8
                    type: string
            required:
                - current_password
                - new_password
            type: object
        ChatUnreadCount:
            properties:
                conversations:
                    description: How many threads have anything unread.
                    format: int64
                    type: integer
                unread:
                    format: int64
                    type: integer
            required:
                - unread
                - conversations
            type: object
        CheckoutLine:
            properties:
                note:
                    maxLength: 500
                    type: string
                quantity:
                    format: int64
                    minimum: 1
                    type: integer
                sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
                transport_option:
                    maxLength: 100
                    pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
                    type: string
            required:
                - sku_id
                - quantity
                - transport_option
            type: object
        CheckoutRequest:
            properties:
                contact_id:
                    $ref: '#/components/schemas/ContactID'
                currency:
                    allOf:
                        - $ref: '#/components/schemas/CurrencyCode'
                    description: Must match the listing's currency.
                lines:
                    items:
                        $ref: '#/components/schemas/CheckoutLine'
                    minItems: 1
                    type: array
            required:
                - lines
                - contact_id
                - currency
            type: object
        CheckoutResult:
            description: |
                The items and the single session that pays for them. Nothing is settled until the session succeeds, and no order exists until the seller confirms.
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/OrderItem'
                    type: array
                payment_session:
                    $ref: '#/components/schemas/PaymentSession'
            required:
                - items
                - payment_session
            type: object
        ConfirmOrderRequest:
            properties:
                draft_id:
                    $ref: '#/components/schemas/DraftOrderID'
                note:
                    maxLength: 500
                    type: string
                pickup_contact_id:
                    $ref: '#/components/schemas/ContactID'
                transport_option:
                    maxLength: 100
                    pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
                    type: string
            required:
                - draft_id
                - transport_option
                - pickup_contact_id
            type: object
        ConfirmOrderResult:
            properties:
                order:
                    $ref: '#/components/schemas/Order'
                shipping_fee_session:
                    allOf:
                        - $ref: '#/components/schemas/PaymentSession'
                    description: Present only when the listing says the seller pays shipping.
                    nullable: true
            required:
                - order
            type: object
        ConfirmReceiptRequest:
            properties:
                attachments:
                    description: |
                        Unboxing photos or video. At least one is mandatory — a later refund or dispute is judged on this evidence.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    minItems: 1
                    type: array
                note:
                    maxLength: 2000
                    type: string
            required:
                - attachments
            type: object
        Contact:
            properties:
                address:
                    description: Street or house-number line below ward level.
                    type: string
                address_detail:
                    description: Unit or floor or notes.
                    nullable: true
                    type: string
                address_type:
                    $ref: '#/components/schemas/ContactAddressType'
                country:
                    maxLength: 2
                    minLength: 2
                    type: string
                created_at:
                    format: date-time
                    type: string
                district_code:
                    description: |
                        Null where the country has no district tier. Vietnam dropped it in 2025 and now goes province to ward.
                    nullable: true
                    type: string
                district_name:
                    nullable: true
                    type: string
                full_name:
                    type: string
                id:
                    $ref: '#/components/schemas/ContactID'
                is_default_delivery:
                    type: boolean
                is_default_pickup:
                    type: boolean
                latitude:
                    format: double
                    nullable: true
                    type: number
                longitude:
                    format: double
                    nullable: true
                    type: number
                phone:
                    description: E.164.
                    type: string
                phone_verified:
                    type: boolean
                postal_code:
                    nullable: true
                    type: string
                provider_codes:
                    additionalProperties: true
                    description: |
                        Per-carrier territory ids. Carriers number territories their own way and some still require a district.
                    type: object
                province_code:
                    type: string
                province_name:
                    type: string
                ward_code:
                    type: string
                ward_name:
                    type: string
            required:
                - id
                - full_name
                - phone
                - phone_verified
                - address_type
                - is_default_delivery
                - is_default_pickup
                - country
                - province_code
                - province_name
                - ward_code
                - ward_name
                - address
                - created_at
            type: object
        ContactAddressType:
            enum:
                - home
                - work
            type: string
        ContactID:
            example: ctc_kvq1wd6ptz3n8
            pattern: ^ctc_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ContactList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Contact'
                    type: array
            required:
                - items
            type: object
        Conversation:
            properties:
                counterparty:
                    $ref: '#/components/schemas/AccountSummary'
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ConversationID'
                last_message:
                    allOf:
                        - $ref: '#/components/schemas/Message'
                    nullable: true
                last_message_at:
                    description: |
                        Starts at the creation time so an empty thread still sorts predictably in the inbox.
                    format: date-time
                    type: string
                unread:
                    description: Unread messages in this thread.
                    format: int64
                    type: integer
            required:
                - id
                - counterparty
                - last_message_at
                - unread
                - created_at
            type: object
        ConversationID:
            example: cnv_6ptz3n8kvq1wd
            pattern: ^cnv_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ConversationPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Conversation'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        CreateBankAccountRequest:
            properties:
                account_holder:
                    maxLength: 100
                    minLength: 1
                    type: string
                account_number:
                    maxLength: 50
                    minLength: 1
                    type: string
                bank_code:
                    maxLength: 20
                    minLength: 1
                    type: string
                is_default:
                    default: false
                    type: boolean
            required:
                - bank_code
                - account_number
                - account_holder
            type: object
        CreateCategoryRequest:
            properties:
                description:
                    maxLength: 2000
                    type: string
                name:
                    maxLength: 100
                    minLength: 1
                    type: string
                parent_id:
                    $ref: '#/components/schemas/CategoryID'
            required:
                - name
                - description
            type: object
        CreateContactRequest:
            properties:
                address:
                    maxLength: 255
                    minLength: 1
                    type: string
                address_detail:
                    maxLength: 255
                    type: string
                address_type:
                    $ref: '#/components/schemas/ContactAddressType'
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                district_code:
                    description: Send both district fields or neither.
                    maxLength: 20
                    type: string
                district_name:
                    maxLength: 100
                    type: string
                full_name:
                    maxLength: 100
                    minLength: 1
                    type: string
                is_default_delivery:
                    default: false
                    type: boolean
                is_default_pickup:
                    default: false
                    type: boolean
                latitude:
                    format: double
                    type: number
                longitude:
                    format: double
                    type: number
                phone:
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                postal_code:
                    maxLength: 20
                    type: string
                province_code:
                    maxLength: 20
                    minLength: 1
                    type: string
                province_name:
                    maxLength: 100
                    minLength: 1
                    type: string
                ward_code:
                    maxLength: 20
                    minLength: 1
                    type: string
                ward_name:
                    maxLength: 100
                    minLength: 1
                    type: string
            required:
                - full_name
                - phone
                - address_type
                - country
                - province_code
                - province_name
                - ward_code
                - ward_name
                - address
            type: object
        CreateDraftRequest:
            properties:
                spu_id:
                    $ref: '#/components/schemas/ProductSPUID'
            required:
                - spu_id
            type: object
        CreateListingRequest:
            properties:
                attachments:
                    description: Ordered. The first becomes the cover.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                category_id:
                    $ref: '#/components/schemas/CategoryID'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                description:
                    maxLength: 20000
                    type: string
                name:
                    maxLength: 200
                    minLength: 1
                    type: string
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                shipping_paid_by:
                    $ref: '#/components/schemas/ShippingPaidBy'
                skus:
                    description: At least one. Price and shipping weight live on the variant.
                    items:
                        $ref: '#/components/schemas/CreateSkuRequest'
                    minItems: 1
                    type: array
                specifications:
                    additionalProperties: true
                    type: object
                tags:
                    items:
                        $ref: '#/components/schemas/TagSlug'
                    type: array
            required:
                - name
                - description
                - category_id
                - condition
                - price_mode
                - shipping_paid_by
                - currency
                - skus
            type: object
        CreateModeratorRequest:
            properties:
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                email:
                    format: email
                    maxLength: 255
                    type: string
                locale:
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                name:
                    maxLength: 100
                    minLength: 1
                    type: string
                password:
                    maxLength: 72
                    minLength: 8
                    type: string
                timezone:
                    maxLength: 64
                    type: string
            required:
                - email
                - password
                - name
                - country
                - locale
                - timezone
            type: object
        CreateOfferRequest:
            properties:
                quantity:
                    format: int64
                    minimum: 1
                    type: integer
                reason:
                    maxLength: 500
                    type: string
                sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
                total:
                    format: int64
                    minimum: 1
                    type: integer
            required:
                - sku_id
                - quantity
                - total
            type: object
        CreateOptionRequest:
            properties:
                data:
                    additionalProperties: true
                    type: object
                description:
                    maxLength: 2000
                    type: string
                id:
                    $ref: '#/components/schemas/OptionSlug'
                is_enabled:
                    default: true
                    type: boolean
                logo_resource_id:
                    $ref: '#/components/schemas/ResourceID'
                name:
                    maxLength: 200
                    minLength: 1
                    type: string
                priority:
                    default: 0
                    type: integer
                provider:
                    maxLength: 100
                    pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
                    type: string
                type:
                    $ref: '#/components/schemas/OptionType'
            required:
                - id
                - type
                - provider
                - name
            type: object
        CreateRefundRequest:
            properties:
                attachments:
                    description: Evidence is mandatory.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    minItems: 1
                    type: array
                reason:
                    maxLength: 2000
                    type: string
            required:
                - reason
                - attachments
            type: object
        CreateSkuRequest:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                attributes:
                    additionalProperties: true
                    type: object
                package_details:
                    additionalProperties: true
                    type: object
                price:
                    format: int64
                    minimum: 0
                    type: integer
                stock:
                    default: 0
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - price
                - attributes
                - package_details
            type: object
        CreateTagRequest:
            properties:
                description:
                    maxLength: 255
                    type: string
                slug:
                    $ref: '#/components/schemas/TagSlug'
            required:
                - slug
            type: object
        CreateUploadRequest:
            properties:
                checksum:
                    description: |
                        Content hash if known. A match returns the stored resource and skips the upload entirely.
                    maxLength: 128
                    type: string
                mime:
                    maxLength: 100
                    minLength: 1
                    type: string
                size:
                    description: |
                        Expected byte count, used to bound the presigned PUT. The recorded size is what storage reports at completion, not this.
                    format: int64
                    minimum: 1
                    type: integer
            required:
                - mime
                - size
            type: object
        CreateWithdrawalRequest:
            properties:
                amount:
                    format: int64
                    minimum: 1
                    type: integer
                bank_account_id:
                    $ref: '#/components/schemas/BankAccountID'
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
            required:
                - currency
                - amount
                - bank_account_id
            type: object
        CurrencyCode:
            description: |
                ISO 4217. Never inferred from the account's country — one currency spans many countries, and a seller may want a balance in a foreign one.
            example: VND
            maxLength: 3
            minLength: 3
            pattern: ^[A-Z]{3}$
            type: string
        Device:
            properties:
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/DeviceID'
                last_seen_at:
                    format: date-time
                    type: string
                platform:
                    $ref: '#/components/schemas/DevicePlatform'
                push_token_suffix:
                    description: |
                        The tail of the token, enough for a client to recognise its own install. The whole token is a delivery credential and is never returned.
                    type: string
            required:
                - id
                - platform
                - push_token_suffix
                - last_seen_at
                - created_at
            type: object
        DeviceID:
            example: dvc_ynht57b62mxef
            pattern: ^dvc_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        DeviceList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Device'
                    type: array
            required:
                - items
            type: object
        DevicePlatform:
            enum:
                - ios
                - android
                - web
            type: string
        DisputeOutcome:
            description: A ruling, so `open` is not one of the choices.
            enum:
                - seller-wins
                - buyer-wins
            type: string
        DisputeRulingRequest:
            properties:
                note:
                    description: Recorded for audit; the ruling is final
                    maxLength: 2000
                    type: string
                outcome:
                    $ref: '#/components/schemas/DisputeOutcome'
            required:
                - outcome
                - note
            type: object
        DisputeStatus:
            enum:
                - open
                - seller-wins
                - buyer-wins
            type: string
        DraftOrder:
            properties:
                cancelled_at:
                    format: date-time
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/DraftOrderID'
                snapshot:
                    $ref: '#/components/schemas/DraftSPUSnapshot'
                spu_id:
                    $ref: '#/components/schemas/ProductSPUID'
                valid_until:
                    format: date-time
                    type: string
            required:
                - id
                - spu_id
                - snapshot
                - created_at
                - valid_until
            type: object
        DraftOrderID:
            example: drf_8kvq1wd6ptz3n
            pattern: ^drf_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        DraftOrderPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/DraftOrder'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        DraftSKUSnapshot:
            description: One purchasable variant as it stood when the session opened.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                attributes:
                    additionalProperties: true
                    type: object
                id:
                    $ref: '#/components/schemas/ProductSKUID'
                package_details:
                    additionalProperties: true
                    description: Weight and dimensions; the shipping quote is computed from these.
                    type: object
                price:
                    description: Smallest currency unit
                    format: int64
                    type: integer
            required:
                - id
                - price
            type: object
        DraftSPUSnapshot:
            description: |
                The frozen listing terms. Carries the SKUs because price and shipping weight live there, and those are what must not move under the buyer.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                id:
                    $ref: '#/components/schemas/ProductSPUID'
                name:
                    type: string
                price_mode:
                    enum:
                        - fixed
                        - negotiable
                    type: string
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                shipping_paid_by:
                    $ref: '#/components/schemas/OrderShippingPaidBy'
                skus:
                    items:
                        $ref: '#/components/schemas/DraftSKUSnapshot'
                    type: array
            required:
                - id
                - name
                - seller_id
                - currency
                - price_mode
                - skus
            type: object
        EmailVerificationRequest:
            properties:
                token:
                    type: string
            required:
                - token
            type: object
        Error:
            properties:
                error:
                    properties:
                        code:
                            type: string
                        message:
                            type: string
                    required:
                        - code
                        - message
                    type: object
            required:
                - error
            type: object
        Favorite:
            properties:
                created_at:
                    format: date-time
                    type: string
                spu_id:
                    $ref: '#/components/schemas/ProductSPUID'
            required:
                - spu_id
                - created_at
            type: object
        FavoritePage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Favorite'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        Feedback:
            properties:
                comment:
                    type: string
                created_at:
                    format: date-time
                    type: string
                direction:
                    $ref: '#/components/schemas/FeedbackDirection'
                id:
                    $ref: '#/components/schemas/FeedbackID'
                order_id:
                    $ref: '#/components/schemas/OrderID'
                published_at:
                    description: |
                        Null while the rating is still blind. Only published feedback is visible to anyone but its author and counted towards reputation.
                    format: date-time
                    nullable: true
                    type: string
                ratee_id:
                    $ref: '#/components/schemas/AccountID'
                rater:
                    $ref: '#/components/schemas/AccountSummary'
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            required:
                - id
                - order_id
                - rater
                - ratee_id
                - direction
                - rating
                - comment
                - created_at
            type: object
        FeedbackDirection:
            enum:
                - buyer-to-seller
                - seller-to-buyer
            type: string
        FeedbackID:
            example: fbk_2h9qk4mfx7bd3
            pattern: ^fbk_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        FeedbackPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Feedback'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        IdentityDocument:
            description: |
                A verification outcome. No document number and no scan: the vendor performs the check and only its verdict is kept.
            properties:
                created_at:
                    format: date-time
                    type: string
                doc_type:
                    $ref: '#/components/schemas/IdentityDocumentType'
                expires_at:
                    description: |
                        When the document itself runs out. A payout gate reads this as well as the status.
                    format: date-time
                    nullable: true
                    type: string
                id:
                    $ref: '#/components/schemas/IdentityDocumentID'
                provider:
                    description: KYC vendor, kebab-case.
                    type: string
                rejection_reason:
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/IdentityStatus'
                verified_at:
                    format: date-time
                    nullable: true
                    type: string
            required:
                - id
                - doc_type
                - provider
                - status
                - created_at
            type: object
        IdentityDocumentID:
            example: idd_57b62mxefynht
            pattern: ^idd_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        IdentityDocumentList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/IdentityDocument'
                    type: array
            required:
                - items
            type: object
        IdentityDocumentPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/AdminIdentityDocument'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        IdentityDocumentType:
            enum:
                - national-id
                - passport
                - driver-license
            type: string
        IdentityStatus:
            enum:
                - pending
                - verified
                - rejected
            type: string
        IdentityVerdictRequest:
            properties:
                expires_at:
                    description: When the document runs out.
                    format: date-time
                    type: string
                rejection_reason:
                    description: Required when rejecting.
                    maxLength: 2000
                    type: string
                status:
                    description: A verdict so pending is not a choice.
                    enum:
                        - verified
                        - rejected
                    type: string
            required:
                - status
            type: object
        IdentityVerificationTicket:
            properties:
                document:
                    $ref: '#/components/schemas/IdentityDocument'
                vendor_session_expires_at:
                    format: date-time
                    nullable: true
                    type: string
                vendor_session_url:
                    description: Where the caller finishes the check with the vendor.
                    nullable: true
                    type: string
            required:
                - document
            type: object
        Listing:
            description: The card shown in a feed. `GET /listings/{id}` returns the full form.
            properties:
                category_id:
                    $ref: '#/components/schemas/CategoryID'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                cover:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                id:
                    $ref: '#/components/schemas/ProductSPUID'
                name:
                    type: string
                price:
                    description: |
                        The featured variant's price, or the cheapest one when a price sort is in force. Not stored on the listing.
                    format: int64
                    type: integer
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                rating:
                    description: |
                        Average review rating from 1 to 5, or 0 when there are none yet. Maintained from review events because reviews live in another schema and cannot be joined.
                    format: double
                    type: number
                seller:
                    $ref: '#/components/schemas/AccountSummary'
                slug:
                    description: URL-friendly and globally unique.
                    type: string
                status:
                    $ref: '#/components/schemas/ListingStatus'
            required:
                - id
                - slug
                - name
                - status
                - condition
                - price_mode
                - currency
                - price
                - rating
                - category_id
                - seller
                - created_at
            type: object
        ListingCondition:
            description: Item condition. This is a marketplace for used goods so it is listing-level and not optional.
            enum:
                - new
                - used
                - damaged
            type: string
        ListingDetail:
            properties:
                category:
                    $ref: '#/components/schemas/Category'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                description:
                    type: string
                featured_sku_id:
                    allOf:
                        - $ref: '#/components/schemas/ProductSKUID'
                    nullable: true
                id:
                    $ref: '#/components/schemas/ProductSPUID'
                images:
                    description: Ordered. The first is the cover.
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                name:
                    type: string
                pending_edit:
                    additionalProperties: true
                    description: |
                        An edit waiting on moderation. Visible to the owner and to staff only; buyers see the published version until it is approved.
                    nullable: true
                    type: object
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                rating:
                    format: double
                    type: number
                seller:
                    $ref: '#/components/schemas/AccountSummary'
                shipping_paid_by:
                    $ref: '#/components/schemas/ShippingPaidBy'
                skus:
                    items:
                        $ref: '#/components/schemas/Sku'
                    type: array
                slug:
                    type: string
                specifications:
                    additionalProperties: true
                    description: Structured attributes specific to the product type.
                    type: object
                status:
                    $ref: '#/components/schemas/ListingStatus'
                tags:
                    items:
                        $ref: '#/components/schemas/TagSlug'
                    type: array
            required:
                - id
                - slug
                - name
                - description
                - status
                - condition
                - price_mode
                - shipping_paid_by
                - currency
                - specifications
                - images
                - category
                - tags
                - skus
                - rating
                - seller
                - created_at
            type: object
        ListingPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Listing'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        ListingSearchHit:
            properties:
                listing:
                    $ref: '#/components/schemas/Listing'
                score:
                    description: Higher is more relevant in every mode.
                    format: double
                    type: number
            required:
                - listing
                - score
            type: object
        ListingSearchResult:
            description: |
                Ranked, so it is not cursor-paginated: page two of a relevance ranking is a different query, not the tail of this one. Ask for a bigger limit instead.
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/ListingSearchHit'
                    type: array
                mode:
                    description: The mode that actually ran.
                    enum:
                        - lexical
                        - semantic
                        - hybrid
                    type: string
            required:
                - items
                - mode
            type: object
        ListingStatus:
            description: |
                Lifecycle and moderation in one column. `hidden` is a live listing the seller took down for now; deletion is a separate flag entirely.
            enum:
                - draft
                - pending
                - active
                - hidden
            type: string
        LoginRequest:
            properties:
                identifier:
                    description: An email or a phone or a username.
                    type: string
                password:
                    type: string
            required:
                - identifier
                - password
            type: object
        LogoutRequest:
            properties:
                device_id:
                    $ref: '#/components/schemas/DeviceID'
            type: object
        MarkConversationReadRequest:
            properties:
                before:
                    description: |
                        Mark everything sent at or before this instant read. Omit to mark the whole thread read.
                    format: date-time
                    type: string
            type: object
        MarkNotificationsReadRequest:
            properties:
                before:
                    description: |
                        Mark everything created at or before this instant read. Omit to mark the whole feed read.
                    format: date-time
                    type: string
            type: object
        Me:
            description: The caller's own view. Everything here is private to them.
            properties:
                created_at:
                    format: date-time
                    type: string
                email:
                    format: email
                    nullable: true
                    type: string
                email_verified:
                    type: boolean
                has_password:
                    description: False on a provider-only account. Unlinking the last provider is refused when this is false.
                    type: boolean
                id:
                    $ref: '#/components/schemas/AccountID'
                identity_verified:
                    description: Whether a live verified identity document exists. The payout gate reads this.
                    type: boolean
                phone:
                    nullable: true
                    type: string
                profile:
                    $ref: '#/components/schemas/Profile'
                role:
                    $ref: '#/components/schemas/AccountRole'
                status:
                    $ref: '#/components/schemas/AccountStatus'
                username:
                    nullable: true
                    type: string
            required:
                - id
                - email_verified
                - role
                - status
                - has_password
                - identity_verified
                - profile
                - created_at
            type: object
        Message:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                body:
                    description: Empty on a redacted message.
                    type: string
                conversation_id:
                    $ref: '#/components/schemas/ConversationID'
                created_at:
                    format: date-time
                    type: string
                deleted_at:
                    description: Set on a redacted message. The row stays so a thread has no unexplained gaps.
                    format: date-time
                    nullable: true
                    type: string
                edited_at:
                    format: date-time
                    nullable: true
                    type: string
                id:
                    $ref: '#/components/schemas/MessageID'
                metadata:
                    additionalProperties: true
                    description: |
                        References to a listing or variant or order, and the payload of an offer card.
                    type: object
                sender_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: Null on a system message.
                    nullable: true
                status:
                    $ref: '#/components/schemas/MessageStatus'
                type:
                    $ref: '#/components/schemas/MessageType'
            required:
                - id
                - conversation_id
                - type
                - body
                - status
                - attachments
                - metadata
                - created_at
            type: object
        MessageID:
            example: msg_agnc1pe4pjb4k
            pattern: ^msg_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        MessagePage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Message'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        MessageStatus:
            enum:
                - sent
                - delivered
                - read
            type: string
        MessageType:
            description: |
                A `system` message is produced by the backend — an offer was accepted, an order shipped — and belongs to no participant, which is why it has no sender.
            enum:
                - user
                - system
            type: string
        ModerationNoteRequest:
            properties:
                note:
                    maxLength: 2000
                    type: string
            type: object
        Notification:
            properties:
                category:
                    $ref: '#/components/schemas/NotificationCategory'
                created_at:
                    description: |
                        Identifies the row together with the feed order, and it is what `POST /notifications/read` is given a bound against.
                    format: date-time
                    type: string
                payload:
                    additionalProperties: true
                    description: Structured content such as deep links and images.
                    type: object
                read_at:
                    format: date-time
                    nullable: true
                    type: string
                title:
                    type: string
            required:
                - category
                - title
                - payload
                - created_at
            type: object
        NotificationCategory:
            description: Also the preference key, which is why it is an enum — a typo would read as no preference and quietly send anyway.
            enum:
                - order
                - promotion
                - system
                - chat
                - social
            type: string
        NotificationChannel:
            enum:
                - in-app
                - push
                - email
                - sms
            type: string
        NotificationPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Notification'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        NotificationPreference:
            properties:
                category:
                    $ref: '#/components/schemas/NotificationCategory'
                channel:
                    $ref: '#/components/schemas/NotificationChannel'
                is_default:
                    description: |
                        True when no stored row exists and this is the domain default. Shown so a client can tell an explicit choice from an inherited one.
                    type: boolean
                is_enabled:
                    type: boolean
            required:
                - category
                - channel
                - is_enabled
                - is_default
            type: object
        NotificationPreferenceList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/NotificationPreference'
                    type: array
            required:
                - items
            type: object
        OAuthIdentity:
            properties:
                created_at:
                    format: date-time
                    type: string
                email:
                    description: |
                        As the provider reported it. May differ from the account's email and is not authoritative.
                    nullable: true
                    type: string
                provider:
                    $ref: '#/components/schemas/OAuthProvider'
            required:
                - provider
                - created_at
            type: object
        OAuthIdentityList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/OAuthIdentity'
                    type: array
            required:
                - items
            type: object
        OAuthLoginRequest:
            properties:
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                credential:
                    description: The provider's authorization code or id token.
                    type: string
                locale:
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                provider:
                    $ref: '#/components/schemas/OAuthProvider'
                timezone:
                    maxLength: 64
                    type: string
            required:
                - provider
                - credential
            type: object
        OAuthProvider:
            example: google
            pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
            type: string
        Offer:
            properties:
                author_id:
                    $ref: '#/components/schemas/AccountID'
                buyer_id:
                    $ref: '#/components/schemas/AccountID'
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                expires_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/OfferID'
                quantity:
                    format: int64
                    type: integer
                reason:
                    description: Shown on the offer card beside the proposed price
                    type: string
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
                status:
                    $ref: '#/components/schemas/OfferStatus'
                total:
                    description: Currently proposed total, smallest currency unit
                    format: int64
                    type: integer
            required:
                - id
                - sku_id
                - buyer_id
                - seller_id
                - author_id
                - status
                - quantity
                - total
                - currency
                - created_at
                - expires_at
            type: object
        OfferID:
            example: ofr_ptz3n8kvq1wd6
            pattern: ^ofr_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        OfferPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Offer'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        OfferStatus:
            enum:
                - active
                - accepted
                - cancelled
            type: string
        Option:
            description: |
                One pluggable integration. `vault_secret_path` is deliberately absent: the credentials live in Vault, and the path to them is not something the API hands out.
            properties:
                created_at:
                    format: date-time
                    type: string
                data:
                    additionalProperties: true
                    description: |
                        Non-secret configuration only — endpoints, supported currencies, feature flags.
                    type: object
                description:
                    type: string
                id:
                    $ref: '#/components/schemas/OptionSlug'
                is_enabled:
                    type: boolean
                logo:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                name:
                    type: string
                owner_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: The seller who configured it. Null means system-provided.
                    nullable: true
                priority:
                    description: Display order; ties are broken by `id`.
                    type: integer
                provider:
                    description: Sub-grouping key, kebab-case — `vnpay`, `ghn`.
                    type: string
                type:
                    $ref: '#/components/schemas/OptionType'
            required:
                - id
                - type
                - provider
                - name
                - description
                - priority
                - is_enabled
                - data
                - created_at
            type: object
        OptionList:
            description: |
                Unpaginated. A marketplace has a handful of rails and carriers, and the checkout screen wants all of them at once.
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Option'
                    type: array
            required:
                - items
            type: object
        OptionSlug:
            description: |
                Stable kebab-case identifier, marketplace-wide. A natural key, not an encoded one: other modules store it as a plain string, so it is readable on the wire by design.
            example: ghn-express
            maxLength: 100
            pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
            type: string
        OptionType:
            description: |
                High-level grouping key. Open-ended rather than an enum — adding an integration category must not need a migration and a spec change.
            example: transport
            maxLength: 100
            pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
            type: string
        Order:
            properties:
                address:
                    $ref: '#/components/schemas/OrderAddressSnapshot'
                buyer_id:
                    $ref: '#/components/schemas/AccountID'
                cancelled_at:
                    format: date-time
                    nullable: true
                    type: string
                completed_at:
                    description: Set by the payout job
                    format: date-time
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                draft_id:
                    $ref: '#/components/schemas/DraftOrderID'
                id:
                    $ref: '#/components/schemas/OrderID'
                items:
                    items:
                        $ref: '#/components/schemas/OrderItem'
                    type: array
                note:
                    description: Seller note
                    nullable: true
                    type: string
                payout_deadline_at:
                    description: '`received_at` + 72h. Absent a refund, a job pays the seller out at this point.'
                    format: date-time
                    nullable: true
                    type: string
                pickup_address:
                    $ref: '#/components/schemas/OrderAddressSnapshot'
                receipt_attachments:
                    description: Unboxing evidence supplied with the receipt confirmation.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                received_at:
                    description: When the buyer confirmed receipt. Null means the payout clock has not started.
                    format: date-time
                    nullable: true
                    type: string
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                state:
                    $ref: '#/components/schemas/OrderState'
                transport_id:
                    $ref: '#/components/schemas/TransportID'
            required:
                - id
                - draft_id
                - buyer_id
                - seller_id
                - transport_id
                - address
                - pickup_address
                - state
                - receipt_attachments
                - created_at
            type: object
        OrderAddressSnapshot:
            description: |
                A contact copied into the row, not a pointer to one. The administrative codes are what a carrier is called with, and the saved contact may have changed since the order was placed.
            properties:
                address:
                    description: Street or house-number line, below ward level
                    type: string
                address_detail:
                    description: Unit, floor, notes
                    nullable: true
                    type: string
                country:
                    description: ISO 3166-1 alpha-2
                    maxLength: 2
                    minLength: 2
                    type: string
                district_code:
                    description: Null where the country has no district tier
                    nullable: true
                    type: string
                district_name:
                    nullable: true
                    type: string
                full_name:
                    type: string
                phone:
                    description: E.164
                    type: string
                postal_code:
                    nullable: true
                    type: string
                provider_codes:
                    additionalProperties: true
                    description: 'Per-carrier territory ids, e.g. {"ghn": {"province_id": 201}}'
                    type: object
                province_code:
                    type: string
                province_name:
                    type: string
                ward_code:
                    type: string
                ward_name:
                    type: string
            required:
                - full_name
                - phone
                - country
                - province_code
                - ward_code
                - address
            type: object
        OrderFeedback:
            description: |
                Both directions on one order, as far as the caller is allowed to see them.
            properties:
                mine:
                    allOf:
                        - $ref: '#/components/schemas/Feedback'
                    nullable: true
                theirs:
                    allOf:
                        - $ref: '#/components/schemas/Feedback'
                    description: Present only once published.
                    nullable: true
                theirs_submitted:
                    description: |
                        Whether the counterparty has rated at all. Shown while their rating is still blind so the caller knows a reveal is coming.
                    type: boolean
            required:
                - theirs_submitted
            type: object
        OrderID:
            example: ord_fv2cpg50vkrfp
            pattern: ^ord_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        OrderItem:
            properties:
                address:
                    $ref: '#/components/schemas/OrderAddressSnapshot'
                buyer_id:
                    $ref: '#/components/schemas/AccountID'
                cancelled_at:
                    format: date-time
                    nullable: true
                    type: string
                cancelled_by_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: Null means the system cancelled it.
                    nullable: true
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                draft_id:
                    $ref: '#/components/schemas/DraftOrderID'
                id:
                    $ref: '#/components/schemas/OrderItemID'
                note:
                    description: Buyer note
                    nullable: true
                    type: string
                order_id:
                    allOf:
                        - $ref: '#/components/schemas/OrderID'
                    description: Null until the seller confirms.
                    nullable: true
                payment_status:
                    $ref: '#/components/schemas/OrderPaymentStatus'
                quantity:
                    format: int64
                    type: integer
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
                state:
                    $ref: '#/components/schemas/OrderItemState'
                total_amount:
                    description: Paid amount in the smallest unit of `currency`
                    format: int64
                    type: integer
                transport_option:
                    description: A transport option slug owned by the common module
                    type: string
            required:
                - id
                - draft_id
                - buyer_id
                - seller_id
                - sku_id
                - quantity
                - currency
                - total_amount
                - transport_option
                - address
                - state
                - payment_status
                - created_at
            type: object
        OrderItemID:
            example: itm_q1wd6ptz3n8kv
            pattern: ^itm_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        OrderItemPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/OrderItem'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        OrderItemState:
            description: |
                Derived, not stored. `pending` = paid but no order covers it yet, `confirmed` = linked to an order, `cancelled` = withdrawn by either party.
            enum:
                - pending
                - confirmed
                - cancelled
            type: string
        OrderPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Order'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        OrderPaymentStatus:
            description: Status of the payment session covering the item; owned by finance.
            enum:
                - pending
                - processing
                - success
                - cancelled
                - failed
            type: string
        OrderShippingPaidBy:
            description: |
                Chosen by the seller when the listing is created. `buyer` adds the fee to the checkout invoice; `seller` deducts it from the payout.
            enum:
                - buyer
                - seller
            type: string
        OrderState:
            description: |
                Derived from the two outcome timestamps: `open` means neither is set, which is also the predicate of the partial indexes behind this filter.
            enum:
                - open
                - completed
                - cancelled
            type: string
        PasswordResetConfirmRequest:
            properties:
                new_password:
                    maxLength: 72
                    minLength: 8
                    type: string
                token:
                    type: string
            required:
                - token
                - new_password
            type: object
        PasswordResetRequest:
            properties:
                identifier:
                    description: An email or a phone.
                    type: string
            required:
                - identifier
            type: object
        PaymentSession:
            properties:
                amount_paid:
                    description: Sum of the settled legs, so a partly-tendered session is distinguishable from an unpaid one.
                    format: int64
                    type: integer
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                expired_at:
                    description: A session still unsettled past this point is voided by a job.
                    format: date-time
                    type: string
                from_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: The payer. Null means the platform itself.
                    nullable: true
                id:
                    $ref: '#/components/schemas/PaymentSessionID'
                kind:
                    $ref: '#/components/schemas/PaymentSessionKind'
                note:
                    type: string
                paid_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                to_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: The payee. Null means the platform itself.
                    nullable: true
                total_amount:
                    description: Amount owed, smallest unit of `currency`
                    format: int64
                    type: integer
            required:
                - id
                - kind
                - status
                - currency
                - total_amount
                - amount_paid
                - note
                - created_at
                - expired_at
            type: object
        PaymentSessionID:
            example: pay_7bq2xn4tw9yef
            pattern: ^pay_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        PaymentSessionKind:
            description: |
                What the money is for; it is what routes the flow. A client cannot pick this — the module that owes the money does.
            enum:
                - buyer-checkout
                - seller-confirmation-fee
                - seller-payout
                - withdrawal
            type: string
        PaymentSessionPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/PaymentSession'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        PaymentSessionStatus:
            enum:
                - pending
                - processing
                - success
                - cancelled
                - failed
            type: string
        PriceMode:
            description: |
                `fixed` can be bought straight from the listing page. `negotiable` has to go through an offer first and cannot be checked out directly.
            enum:
                - fixed
                - negotiable
            type: string
        ProductSKUID:
            example: sku_vq1wd6ptz3n8k
            pattern: ^sku_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ProductSPUID:
            example: spu_1ryaj8117v2p4
            pattern: ^spu_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        Profile:
            properties:
                avatar:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                country:
                    maxLength: 2
                    minLength: 2
                    type: string
                created_at:
                    format: date-time
                    type: string
                date_of_birth:
                    format: date
                    nullable: true
                    type: string
                description:
                    nullable: true
                    type: string
                gender:
                    allOf:
                        - $ref: '#/components/schemas/ProfileGender'
                    nullable: true
                locale:
                    example: vi-VN
                    type: string
                name:
                    type: string
                timezone:
                    example: Asia/Ho_Chi_Minh
                    type: string
            required:
                - name
                - country
                - locale
                - timezone
                - created_at
            type: object
        ProfileGender:
            enum:
                - male
                - female
                - other
            type: string
        PublicAccount:
            description: |
                What anyone may see about an account. The seller page: no email, no phone, no birth date, no addresses.
            properties:
                avatar:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                created_at:
                    description: Member since.
                    format: date-time
                    type: string
                description:
                    nullable: true
                    type: string
                follower_count:
                    format: int64
                    type: integer
                id:
                    $ref: '#/components/schemas/AccountID'
                identity_verified:
                    description: Shown as a trust signal.
                    type: boolean
                name:
                    type: string
            required:
                - id
                - name
                - identity_verified
                - follower_count
                - created_at
            type: object
        RefreshRequest:
            properties:
                refresh_token:
                    type: string
            required:
                - refresh_token
            type: object
        Refund:
            properties:
                attachments:
                    description: The buyer's evidence, kept apart from the seller's on the dispute.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                buyer_id:
                    $ref: '#/components/schemas/AccountID'
                created_at:
                    format: date-time
                    type: string
                dispute_id:
                    allOf:
                        - $ref: '#/components/schemas/RefundDisputeID'
                    description: Set when a rejection opened a dispute; how either party reaches it.
                    nullable: true
                id:
                    $ref: '#/components/schemas/RefundID'
                order_id:
                    $ref: '#/components/schemas/OrderID'
                reason:
                    type: string
                rejection_reason:
                    nullable: true
                    type: string
                return_transport_id:
                    allOf:
                        - $ref: '#/components/schemas/TransportID'
                    description: The leg sending the goods back, recorded if and when it happens.
                    nullable: true
                review_deadline_at:
                    description: '`created_at` + 48h. Silence past it is a full refund, applied by a job.'
                    format: date-time
                    type: string
                seller_decided_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/RefundStatus'
            required:
                - id
                - order_id
                - buyer_id
                - status
                - reason
                - attachments
                - review_deadline_at
                - created_at
            type: object
        RefundDispute:
            properties:
                attachments:
                    description: The seller's evidence. The buyer's stays on the refund.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                buyer_id:
                    $ref: '#/components/schemas/AccountID'
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/RefundDisputeID'
                order_id:
                    $ref: '#/components/schemas/OrderID'
                reason:
                    description: The seller's grounds for refusing
                    type: string
                refund_id:
                    $ref: '#/components/schemas/RefundID'
                resolution_note:
                    nullable: true
                    type: string
                resolved_at:
                    format: date-time
                    nullable: true
                    type: string
                resolved_by_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: The moderator who ruled.
                    nullable: true
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                status:
                    $ref: '#/components/schemas/DisputeStatus'
            required:
                - id
                - refund_id
                - order_id
                - seller_id
                - buyer_id
                - status
                - reason
                - attachments
                - created_at
            type: object
        RefundDisputeID:
            example: dsp_n8kvq1wd6ptz3
            pattern: ^dsp_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        RefundDisputePage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/RefundDispute'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        RefundID:
            example: rfd_z3n8kvq1wd6pt
            pattern: ^rfd_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        RefundPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Refund'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        RefundStatus:
            description: |
                A refund goes straight to seller review; the buyer does not ship the goods back first. `accepted` and `rejected` are the seller's or the moderator's outcome, `cancelled` is the buyer withdrawing.
            enum:
                - awaiting-seller-review
                - disputed
                - accepted
                - rejected
                - cancelled
            type: string
        RegisterDeviceRequest:
            properties:
                platform:
                    $ref: '#/components/schemas/DevicePlatform'
                push_token:
                    maxLength: 4096
                    minLength: 1
                    type: string
            required:
                - platform
                - push_token
            type: object
        RegisterRequest:
            description: |
                At least one of email, phone or username is required. The profile fields are part of the same request because the account and its profile are created together.
            properties:
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                email:
                    format: email
                    maxLength: 255
                    type: string
                locale:
                    example: vi-VN
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                name:
                    description: Display name.
                    maxLength: 100
                    minLength: 1
                    type: string
                password:
                    maxLength: 72
                    minLength: 8
                    type: string
                phone:
                    description: E.164.
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                timezone:
                    example: Asia/Ho_Chi_Minh
                    maxLength: 64
                    type: string
                username:
                    maxLength: 100
                    minLength: 3
                    pattern: ^[a-z0-9._-]+$
                    type: string
            required:
                - password
                - name
                - country
                - locale
                - timezone
            type: object
        RejectRefundRequest:
            properties:
                attachments:
                    description: |
                        Counter-evidence is mandatory — the packed state of the goods before dispatch is what a moderator weighs against the buyer's evidence.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    minItems: 1
                    type: array
                reason:
                    maxLength: 2000
                    type: string
            required:
                - reason
                - attachments
            type: object
        Report:
            properties:
                action_taken:
                    allOf:
                        - $ref: '#/components/schemas/ReportAction'
                    description: Null until the report is resolved.
                    nullable: true
                created_at:
                    format: date-time
                    type: string
                detail:
                    type: string
                id:
                    $ref: '#/components/schemas/ReportID'
                reason:
                    $ref: '#/components/schemas/ReportReason'
                ref_id:
                    description: |
                        Opaque id of the reported thing. Which prefix is valid follows from `ref_type`, which is why the two are validated together.
                    example: spu_1ryaj8117v2p4
                    pattern: ^[a-z]{3}_[0-9a-hjkmnp-tv-z]{13}$
                    type: string
                ref_type:
                    $ref: '#/components/schemas/ReportRefType'
                resolution_note:
                    nullable: true
                    type: string
                resolved_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/ReportStatus'
            required:
                - id
                - ref_type
                - ref_id
                - reason
                - detail
                - status
                - created_at
            type: object
        ReportAction:
            description: What a moderator did about an upheld report. `none` goes with a dismissal.
            enum:
                - none
                - listing-removed
                - message-removed
                - account-suspended
                - warning
            type: string
        ReportID:
            example: rpt_9c4vt6bkn1pzs
            pattern: ^rpt_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ReportPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Report'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        ReportReason:
            enum:
                - scam
                - counterfeit
                - prohibited
                - harassment
                - spam
                - inappropriate
                - other
            type: string
        ReportRefType:
            enum:
                - listing
                - account
                - message
                - review
                - review-reply
            type: string
        ReportStatus:
            enum:
                - open
                - reviewing
                - actioned
                - dismissed
            type: string
        Reputation:
            description: |
                Recomputed, never written directly. Transaction ratings and product-review ratings stay apart: one order can produce both and summing them would count it twice.
            properties:
                account_id:
                    $ref: '#/components/schemas/AccountID'
                cancelled_orders:
                    format: int64
                    type: integer
                completed_orders:
                    format: int64
                    type: integer
                rating_average:
                    description: Average transaction feedback from 1 to 5. Zero when there is none.
                    format: double
                    type: number
                rating_count:
                    format: int64
                    type: integer
                review_rating_average:
                    description: Average product-review rating. Always zero for the buyer role.
                    format: double
                    type: number
                review_rating_count:
                    format: int64
                    type: integer
                role:
                    $ref: '#/components/schemas/ReputationRole'
                updated_at:
                    description: When it was last recomputed.
                    format: date-time
                    type: string
            required:
                - account_id
                - role
                - rating_average
                - rating_count
                - review_rating_average
                - review_rating_count
                - completed_orders
                - cancelled_orders
                - updated_at
            type: object
        ReputationRole:
            enum:
                - seller
                - buyer
            type: string
        ResolveReportRequest:
            properties:
                action_taken:
                    allOf:
                        - $ref: '#/components/schemas/ReportAction'
                    description: |
                        Required. `none` goes with a dismissal; upholding the report names what was done.
                note:
                    maxLength: 2000
                    type: string
                status:
                    description: A verdict so open and reviewing are not choices.
                    enum:
                        - actioned
                        - dismissed
                    type: string
            required:
                - status
                - action_taken
            type: object
        Resource:
            properties:
                checksum:
                    description: Content hash, used to share one stored object between identical uploads.
                    nullable: true
                    type: string
                completed_at:
                    description: |
                        Null until the upload was confirmed. An unconfirmed resource cannot be attached to anything.
                    format: date-time
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ResourceID'
                metadata:
                    additionalProperties: true
                    description: Backend-specific extras — dimensions, duration, CDN path.
                    type: object
                mime:
                    example: image/jpeg
                    type: string
                provider:
                    description: Storage backend, kebab-case — `s3`, `minio`, `local`.
                    type: string
                size:
                    description: Bytes, as read back from storage rather than as declared.
                    format: int64
                    type: integer
                url:
                    description: |
                        Short-lived URL to fetch the bytes. Null while the upload is unconfirmed. Not a stable address: store the id, not this.
                    nullable: true
                    type: string
                url_expires_at:
                    format: date-time
                    nullable: true
                    type: string
            required:
                - id
                - provider
                - mime
                - size
                - metadata
                - created_at
            type: object
        ResourceID:
            example: res_4mx8jd2qr5wtc
            pattern: ^res_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        Review:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                author:
                    $ref: '#/components/schemas/AccountSummary'
                body:
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ReviewID'
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
                replies:
                    items:
                        $ref: '#/components/schemas/ReviewReply'
                    type: array
                spu_id:
                    $ref: '#/components/schemas/ProductSPUID'
                votes:
                    $ref: '#/components/schemas/ReviewVoteTally'
            required:
                - id
                - spu_id
                - author
                - rating
                - body
                - attachments
                - replies
                - votes
                - created_at
            type: object
        ReviewID:
            example: rvw_qk4mfx7bd32h9
            pattern: ^rvw_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ReviewPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Review'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        ReviewReply:
            properties:
                author:
                    $ref: '#/components/schemas/AccountSummary'
                body:
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ReviewReplyID'
                is_seller:
                    description: Whether the author owns the listing.
                    type: boolean
            required:
                - id
                - author
                - is_seller
                - body
                - created_at
            type: object
        ReviewReplyID:
            example: rpl_mfx7bd32h9qk4
            pattern: ^rpl_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ReviewVoteTally:
            properties:
                helpful:
                    format: int64
                    type: integer
                my_vote:
                    description: The caller's own vote. Null when they have not voted.
                    enum:
                        - -1
                        - 1
                    nullable: true
                    type: integer
                not_helpful:
                    format: int64
                    type: integer
            required:
                - helpful
                - not_helpful
            type: object
        SendMessageRequest:
            description: A body or at least one attachment is required.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                body:
                    maxLength: 4000
                    type: string
                metadata:
                    additionalProperties: true
                    type: object
            type: object
        SetStockRequest:
            properties:
                stock:
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - stock
            type: object
        ShippingPaidBy:
            description: |
                Chosen by the seller when the listing is created. `buyer` adds the fee to the checkout invoice. `seller` deducts it from the payout.
            enum:
                - buyer
                - seller
            type: string
        Sku:
            description: A purchasable variant. Price and package details live here rather than on the listing.
            properties:
                attributes:
                    additionalProperties: true
                    description: Variant attributes such as size and colour.
                    type: object
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ProductSKUID'
                images:
                    description: Empty means fall back to the listing gallery.
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                package_details:
                    additionalProperties: true
                    description: Weight and dimensions. A shipping quote is computed from these.
                    type: object
                price:
                    description: Smallest unit of the listing's currency.
                    format: int64
                    type: integer
                stock:
                    $ref: '#/components/schemas/Stock'
            required:
                - id
                - price
                - attributes
                - package_details
                - images
                - created_at
            type: object
        StartConversationRequest:
            properties:
                account_id:
                    $ref: '#/components/schemas/AccountID'
            required:
                - account_id
            type: object
        StartIdentityVerificationRequest:
            properties:
                doc_type:
                    $ref: '#/components/schemas/IdentityDocumentType'
            required:
                - doc_type
            type: object
        StartPaymentRequest:
            properties:
                amount:
                    description: Omit to tender the whole outstanding balance. Pass it to split the session across rails.
                    format: int64
                    minimum: 1
                    type: integer
                payment_option:
                    description: A payment option slug from the common module.
                    maxLength: 100
                    pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
                    type: string
                return_url:
                    description: Where the gateway sends the payer back
                    type: string
            required:
                - payment_option
            type: object
        Stock:
            properties:
                available:
                    description: Total minus reserved.
                    format: int64
                    type: integer
                reserved:
                    description: Held by paid but unconfirmed items. Moved by checkout and by cancellation and never set directly.
                    format: int64
                    type: integer
                sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
                stock:
                    description: Total on hand.
                    format: int64
                    type: integer
            required:
                - sku_id
                - stock
                - reserved
                - available
            type: object
        SubmitFeedbackRequest:
            description: |
                The direction and the ratee both follow from the order, so neither is sent.
            properties:
                comment:
                    maxLength: 2000
                    type: string
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            required:
                - rating
            type: object
        SubmitReportRequest:
            properties:
                detail:
                    maxLength: 2000
                    type: string
                reason:
                    $ref: '#/components/schemas/ReportReason'
                ref_id:
                    pattern: ^[a-z]{3}_[0-9a-hjkmnp-tv-z]{13}$
                    type: string
                ref_type:
                    $ref: '#/components/schemas/ReportRefType'
            required:
                - ref_type
                - ref_id
                - reason
            type: object
        SubmitReviewReplyRequest:
            properties:
                body:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - body
            type: object
        SubmitReviewRequest:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                body:
                    maxLength: 2000
                    type: string
                order_id:
                    $ref: '#/components/schemas/OrderID'
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            required:
                - order_id
                - rating
            type: object
        SuspendAccountRequest:
            properties:
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
                until:
                    description: Omit for a permanent suspension.
                    format: date-time
                    type: string
            required:
                - reason
            type: object
        Tag:
            properties:
                description:
                    nullable: true
                    type: string
                slug:
                    $ref: '#/components/schemas/TagSlug'
            required:
                - slug
            type: object
        TagList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Tag'
                    type: array
            required:
                - items
            type: object
        TagSlug:
            description: A tag's id is its slug. A natural key, so it is never encoded.
            example: handmade
            maxLength: 100
            pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
            type: string
        TakedownRequest:
            properties:
                notify_seller:
                    default: true
                    type: boolean
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - reason
            type: object
        TaxCodeType:
            enum:
                - individual
                - business
                - household
            type: string
        TaxInfo:
            properties:
                created_at:
                    format: date-time
                    type: string
                legal_name:
                    type: string
                tax_code:
                    example: "0123456789"
                    type: string
                tax_code_type:
                    $ref: '#/components/schemas/TaxCodeType'
                verification_status:
                    $ref: '#/components/schemas/TaxVerificationStatus'
                verified_at:
                    format: date-time
                    nullable: true
                    type: string
            required:
                - tax_code
                - tax_code_type
                - legal_name
                - verification_status
                - created_at
            type: object
        TaxVerificationRequest:
            properties:
                note:
                    maxLength: 2000
                    type: string
                source:
                    description: What the verdict was based on
                    maxLength: 200
                    type: string
                status:
                    description: A verdict, so `pending` is not a choice
                    enum:
                        - verified
                        - rejected
                    type: string
            required:
                - status
                - source
            type: object
        TaxVerificationStatus:
            enum:
                - pending
                - verified
                - rejected
            type: string
        Transaction:
            description: One movement on an external rail. Append-only.
            properties:
                amount:
                    description: Signed — positive is a charge, negative is a reversal.
                    format: int64
                    type: integer
                checkout_url:
                    description: Where to send the payer for this leg. Null once the rail no longer needs it, or for rails that have none.
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                currency:
                    allOf:
                        - $ref: '#/components/schemas/CurrencyCode'
                    description: What the rail actually debits, which split tender may make differ from the session's currency.
                error:
                    nullable: true
                    type: string
                expired_at:
                    description: Gateway URL expiry
                    format: date-time
                    nullable: true
                    type: string
                id:
                    $ref: '#/components/schemas/TransactionID'
                note:
                    type: string
                payment_option:
                    description: A payment option slug owned by the common module
                    type: string
                reverses_id:
                    allOf:
                        - $ref: '#/components/schemas/TransactionID'
                    description: The leg this one reverses. Set on reversals, null on originals.
                    nullable: true
                session_id:
                    $ref: '#/components/schemas/PaymentSessionID'
                settled_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/TransactionStatus'
            required:
                - id
                - session_id
                - status
                - payment_option
                - amount
                - currency
                - note
                - created_at
            type: object
        TransactionID:
            example: txn_5wtc4mx8jd2qr
            pattern: ^txn_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        TransactionList:
            description: Unpaginated — a session has as many legs as rails it was split across, which is a handful.
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Transaction'
                    type: array
            required:
                - items
            type: object
        TransactionStatus:
            description: A leg goes pending → success or failed, and success is terminal. Deliberately narrower than a session's status.
            enum:
                - pending
                - success
                - failed
            type: string
        Transport:
            properties:
                created_at:
                    format: date-time
                    type: string
                events:
                    items:
                        $ref: '#/components/schemas/TransportEvent'
                    type: array
                id:
                    $ref: '#/components/schemas/TransportID'
                label_url:
                    nullable: true
                    type: string
                option:
                    description: A transport option slug owned by the common module
                    type: string
                status:
                    $ref: '#/components/schemas/TransportStatus'
                tracking_number:
                    nullable: true
                    type: string
            required:
                - id
                - option
                - status
                - events
                - created_at
            type: object
        TransportEvent:
            description: One carrier-reported checkpoint.
            properties:
                note:
                    type: string
                occurred_at:
                    format: date-time
                    type: string
                status:
                    $ref: '#/components/schemas/TransportStatus'
            required:
                - status
                - occurred_at
            type: object
        TransportID:
            example: trp_d6ptz3n8kvq1w
            pattern: ^trp_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        TransportStatus:
            description: Shipment lifecycle as the carrier reports it.
            enum:
                - pending
                - picked-up
                - in-transit
                - delivered
                - returned
                - failed
                - cancelled
            type: string
        UnreadCount:
            properties:
                unread:
                    format: int64
                    type: integer
            required:
                - unread
            type: object
        UpdateAccountRequest:
            description: Every field optional. Setting one to null removes it, which is refused when it is the last identifier.
            properties:
                email:
                    format: email
                    maxLength: 255
                    nullable: true
                    type: string
                phone:
                    nullable: true
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                username:
                    maxLength: 100
                    minLength: 3
                    nullable: true
                    pattern: ^[a-z0-9._-]+$
                    type: string
            type: object
        UpdateBankAccountRequest:
            properties:
                is_default:
                    description: The only mutable field
                    enum:
                        - true
                    type: boolean
            required:
                - is_default
            type: object
        UpdateCartItemRequest:
            properties:
                quantity:
                    format: int64
                    minimum: 1
                    type: integer
            required:
                - quantity
            type: object
        UpdateCategoryRequest:
            description: Every field optional. Setting the parent to null makes it a root.
            properties:
                description:
                    maxLength: 2000
                    type: string
                name:
                    maxLength: 100
                    minLength: 1
                    type: string
                parent_id:
                    allOf:
                        - $ref: '#/components/schemas/CategoryID'
                    nullable: true
            type: object
        UpdateContactRequest:
            description: Every field optional.
            properties:
                address:
                    maxLength: 255
                    type: string
                address_detail:
                    maxLength: 255
                    nullable: true
                    type: string
                address_type:
                    $ref: '#/components/schemas/ContactAddressType'
                district_code:
                    maxLength: 20
                    nullable: true
                    type: string
                district_name:
                    maxLength: 100
                    nullable: true
                    type: string
                full_name:
                    maxLength: 100
                    minLength: 1
                    type: string
                is_default_delivery:
                    type: boolean
                is_default_pickup:
                    type: boolean
                latitude:
                    format: double
                    nullable: true
                    type: number
                longitude:
                    format: double
                    nullable: true
                    type: number
                phone:
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                postal_code:
                    maxLength: 20
                    nullable: true
                    type: string
                province_code:
                    maxLength: 20
                    type: string
                province_name:
                    maxLength: 100
                    type: string
                ward_code:
                    maxLength: 20
                    type: string
                ward_name:
                    maxLength: 100
                    type: string
            type: object
        UpdateListingRequest:
            description: Every field optional. Variants are edited through their own endpoints.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                category_id:
                    $ref: '#/components/schemas/CategoryID'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                description:
                    maxLength: 20000
                    type: string
                featured_sku_id:
                    $ref: '#/components/schemas/ProductSKUID'
                name:
                    maxLength: 200
                    minLength: 1
                    type: string
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                shipping_paid_by:
                    $ref: '#/components/schemas/ShippingPaidBy'
                specifications:
                    additionalProperties: true
                    type: object
                tags:
                    items:
                        $ref: '#/components/schemas/TagSlug'
                    type: array
            type: object
        UpdateMessageRequest:
            properties:
                body:
                    maxLength: 4000
                    minLength: 1
                    type: string
            required:
                - body
            type: object
        UpdateNotificationPreferencesRequest:
            properties:
                items:
                    items:
                        properties:
                            category:
                                $ref: '#/components/schemas/NotificationCategory'
                            channel:
                                $ref: '#/components/schemas/NotificationChannel'
                            is_enabled:
                                type: boolean
                        required:
                            - category
                            - channel
                            - is_enabled
                        type: object
                    minItems: 1
                    type: array
            required:
                - items
            type: object
        UpdateOfferRequest:
            properties:
                quantity:
                    format: int64
                    minimum: 1
                    type: integer
                reason:
                    maxLength: 500
                    type: string
                total:
                    format: int64
                    minimum: 1
                    type: integer
            required:
                - quantity
                - total
            type: object
        UpdateOptionRequest:
            description: |
                Every field optional; the slug, type and provider are fixed at creation.
            properties:
                data:
                    additionalProperties: true
                    type: object
                description:
                    maxLength: 2000
                    type: string
                is_enabled:
                    type: boolean
                logo_resource_id:
                    allOf:
                        - $ref: '#/components/schemas/ResourceID'
                    nullable: true
                name:
                    maxLength: 200
                    minLength: 1
                    type: string
                priority:
                    type: integer
            type: object
        UpdateProfileRequest:
            description: Every field optional.
            properties:
                avatar_resource_id:
                    allOf:
                        - $ref: '#/components/schemas/ResourceID'
                    nullable: true
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                date_of_birth:
                    format: date
                    nullable: true
                    type: string
                description:
                    maxLength: 2000
                    nullable: true
                    type: string
                gender:
                    allOf:
                        - $ref: '#/components/schemas/ProfileGender'
                    nullable: true
                locale:
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                name:
                    maxLength: 100
                    minLength: 1
                    type: string
                timezone:
                    maxLength: 64
                    type: string
            type: object
        UpdateReviewRequest:
            description: Every field optional.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                body:
                    maxLength: 2000
                    type: string
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            type: object
        UpdateSkuRequest:
            description: Every field optional.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                attributes:
                    additionalProperties: true
                    type: object
                package_details:
                    additionalProperties: true
                    type: object
                price:
                    format: int64
                    minimum: 0
                    type: integer
            type: object
        UploadTicket:
            properties:
                resource:
                    $ref: '#/components/schemas/Resource'
                upload_expires_at:
                    format: date-time
                    nullable: true
                    type: string
                upload_url:
                    description: |
                        PUT the bytes here, then confirm. Null when a checksum match meant there was nothing to upload.
                    nullable: true
                    type: string
            required:
                - resource
            type: object
        UpsertTaxInfoRequest:
            properties:
                legal_name:
                    maxLength: 200
                    minLength: 1
                    type: string
                tax_code:
                    description: Vietnamese MST — ten digits, or ten plus a three-digit branch.
                    pattern: ^\d{10}(-\d{3})?$
                    type: string
                tax_code_type:
                    $ref: '#/components/schemas/TaxCodeType'
            required:
                - tax_code
                - tax_code_type
                - legal_name
            type: object
        VoteReviewRequest:
            properties:
                vote:
                    description: |
                        Helpful or not. There is no neutral value — withdrawing a vote is a delete.
                    enum:
                        - -1
                        - 1
                    type: integer
            required:
                - vote
            type: object
        Wallet:
            properties:
                account_id:
                    $ref: '#/components/schemas/AccountID'
                available_balance:
                    description: Spendable and withdrawable
                    format: int64
                    type: integer
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                held_balance:
                    description: Locked in escrow; not the owner's to draw yet
                    format: int64
                    type: integer
            required:
                - account_id
                - currency
                - available_balance
                - held_balance
                - created_at
            type: object
        WalletAdjustmentRequest:
            properties:
                available_delta:
                    format: int64
                    type: integer
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                held_delta:
                    format: int64
                    type: integer
                idempotency_key:
                    description: Required. A retried correction that posts twice is money invented.
                    maxLength: 200
                    minLength: 1
                    type: string
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - currency
                - reason
                - idempotency_key
            type: object
        WalletList:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Wallet'
                    type: array
            required:
                - items
            type: object
        WalletTransaction:
            description: One balance change with its before/after snapshot. Append-only, and the only record of a movement that stays inside a wallet.
            properties:
                available_after:
                    format: int64
                    type: integer
                available_delta:
                    description: Signed change to the available balance
                    format: int64
                    type: integer
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                group_id:
                    description: Shared by every leg of one logical movement — a checkout is a debit plus an escrow hold plus a fee.
                    nullable: true
                    type: string
                held_after:
                    format: int64
                    type: integer
                held_delta:
                    description: Signed change to the held balance
                    format: int64
                    type: integer
                kind:
                    $ref: '#/components/schemas/WalletTransactionKind'
                note:
                    type: string
                ref_id:
                    description: Opaque id of the referenced entity, its prefix given by `ref_type`.
                    nullable: true
                    type: string
                ref_type:
                    description: What this movement was for. Null on an adjustment.
                    enum:
                        - order
                        - payment-session
                    nullable: true
                    type: string
                seq:
                    description: |
                        Position in this wallet's ledger, from 1. Identifies the row and orders it; a gap means a missing movement. Per wallet, so each currency counts from 1 again.
                    format: int64
                    type: integer
            required:
                - seq
                - currency
                - kind
                - available_delta
                - held_delta
                - available_after
                - held_after
                - note
                - created_at
            type: object
        WalletTransactionKind:
            enum:
                - topup
                - escrow-hold
                - escrow-release
                - payout
                - refund
                - withdrawal
                - fee
                - adjustment
            type: string
        WalletTransactionPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/WalletTransaction'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        Withdrawal:
            description: A payment session of kind `withdrawal`, projected with its destination and the admin's resolution.
            properties:
                amount:
                    format: int64
                    type: integer
                bank_account:
                    $ref: '#/components/schemas/BankAccount'
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                id:
                    $ref: '#/components/schemas/PaymentSessionID'
                resolution_note:
                    nullable: true
                    type: string
                resolved_at:
                    format: date-time
                    nullable: true
                    type: string
                resolved_by_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    nullable: true
                status:
                    $ref: '#/components/schemas/PaymentSessionStatus'
            required:
                - id
                - status
                - currency
                - amount
                - bank_account
                - created_at
            type: object
        WithdrawalApprovalRequest:
            properties:
                note:
                    maxLength: 2000
                    type: string
                provider_ref:
                    description: The bank's or gateway's reference for the outbound transfer
                    maxLength: 200
                    type: string
            required:
                - note
            type: object
        WithdrawalPage:
            properties:
                items:
                    items:
                        $ref: '#/components/schemas/Withdrawal'
                    type: array
                next_cursor:
                    nullable: true
                    type: string
            required:
                - items
            type: object
        WithdrawalRejectionRequest:
            properties:
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - reason
            type: object
    securitySchemes:
        bearerAuth:
            bearerFormat: JWT
            scheme: bearer
            type: http
info:
    description: |
        Public REST surface of the ShopNexus C2C marketplace gateway. This file is the base document (info, servers, security, shared parameters and responses). Per-module paths and schemas live in internal/module/<module>/api/openapi.yaml and are merged into api/openapi.gen.yaml by `go generate ./...` (cmd/specgen). Edit the fragments, not the generated file.
        Only `paths` and `components.schemas` are merged from a fragment, so everything reusable across modules — parameters, responses, security schemes — has to be defined here.
    title: ShopNexus Gateway API
    version: 0.1.0
openapi: 3.0.3
paths:
    /accounts/{accountID}/feedback:
        get:
            description: Published rows only. A blind submission is not visible to anyone but its author until the window closes.
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - description: Which side the account was on.
                  in: query
                  name: role
                  schema:
                    $ref: '#/components/schemas/ReputationRole'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/FeedbackPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Published feedback received by an account
            tags:
                - trust
    /accounts/{accountID}/reputation:
        get:
            description: |
                Transaction ratings and product-review ratings are reported separately rather than merged. Only a seller can have review ratings.
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - description: Defaults to seller.
                  in: query
                  name: role
                  schema:
                    $ref: '#/components/schemas/ReputationRole'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Reputation'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read an account's reputation for one role
            tags:
                - trust
    /accounts/{id}:
        get:
            description: |
                The seller page. Deliberately narrow: a display name, an avatar, a blurb and a join date. Email, phone, birth date and saved addresses are never part of it.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PublicAccount'
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read another account's public profile
            tags:
                - account
    /accounts/{id}/followers:
        get:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AccountSummaryPage'
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Who follows this seller
            tags:
                - account
    /admin/accounts:
        get:
            parameters:
                - description: Matches email or phone or username or display name.
                  in: query
                  name: q
                  schema:
                    type: string
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/AccountStatus'
                - in: query
                  name: role
                  schema:
                    $ref: '#/components/schemas/AccountRole'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminAccountPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: Search accounts
            tags:
                - admin
    /admin/accounts/{id}/suspension:
        delete:
            description: Clears the reason and the deadline with it. Past suspensions stay in the audit log rather than on the row.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminAccount'
                    description: Reinstated
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Lift a suspension
            tags:
                - admin
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/AccountID'
        post:
            description: |
                The outcome of upholding a report against a seller. Omit `until` for a permanent suspension.

                Suspending also drops the account's sessions. A suspended row does not stop an access token that is already in circulation, so the two have to happen together.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SuspendAccountRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminAccount'
                    description: Suspended
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Suspend an account
            tags:
                - admin
    /admin/categories:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateCategoryRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Category'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Name already taken
            security:
                - bearerAuth: []
            summary: Create a category
            tags:
                - admin
    /admin/categories/{id}:
        delete:
            description: |
                Children are promoted to roots rather than deleted with it, so a branch is never lost by accident. A category still holding listings cannot be deleted at all — move them first.
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Listings still reference this category
            security:
                - bearerAuth: []
            summary: Delete a category
            tags:
                - admin
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/CategoryID'
        patch:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateCategoryRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Category'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Update a category
            tags:
                - admin
    /admin/disputes:
        get:
            parameters:
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/DisputeStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundDisputePage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The moderator's dispute queue
            tags:
                - admin
    /admin/disputes/{id}/ruling:
        post:
            description: |
                Decided on whose evidence is more convincing, and final — there is no appeal route. `buyer-wins` drives the refund to accepted and releases the money back; `seller-wins` drives it to rejected and lets the payout stand.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundDisputeID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisputeRulingRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundDispute'
                    description: Ruled
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Dispute is already resolved
            security:
                - bearerAuth: []
            summary: Rule on a dispute (moderator)
            tags:
                - admin
    /admin/identity-documents:
        get:
            parameters:
                - description: Defaults to pending.
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/IdentityStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/IdentityDocumentPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The identity review queue
            tags:
                - admin
    /admin/identity-documents/{id}/verdict:
        post:
            description: |
                `verified` requires `expires_at` when the document type carries one, because the payout gate reads the expiry and not only the status. `rejected` requires a reason.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/IdentityDocumentID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/IdentityVerdictRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/IdentityDocument'
                    description: Recorded
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already decided or the account already has a verified document
            security:
                - bearerAuth: []
            summary: Record an identity verdict
            tags:
                - admin
    /admin/listings:
        get:
            description: |
                Defaults to the pending ones — those the automatic scan flagged or that enough reports pushed into review — oldest first, which is the order they should be worked.
            parameters:
                - description: Defaults to pending.
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ListingStatus'
                - in: query
                  name: seller_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The listing moderation queue
            tags:
                - admin
    /admin/listings/{id}/approval:
        post:
            description: Applies any held pending edit and makes it live.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ProductSPUID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ModerationNoteRequest'
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Live
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Not awaiting moderation
            security:
                - bearerAuth: []
            summary: Clear a listing for publication
            tags:
                - admin
    /admin/listings/{id}/takedown:
        post:
            description: |
                The moderator's verdict against a listing that breaks policy. Removes it from the marketplace and records the reason. Suspending the seller as well is a separate decision and a separate call.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ProductSPUID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/TakedownRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Taken down
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Take a listing down
            tags:
                - admin
    /admin/moderators:
        post:
            description: |
                Only an admin may do this and there is no self-service path — a moderator decides disputes and takes listings down, so the role is granted, never claimed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateModeratorRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminAccount'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Email or phone or username already taken
            security:
                - bearerAuth: []
            summary: Provision a moderator account
            tags:
                - admin
    /admin/moderators/{id}:
        delete:
            description: Demotes to a plain user and drops the account's sessions. The account itself survives, since it may have traded.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "204":
                    description: Revoked
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Revoke the moderator role
            tags:
                - admin
    /admin/options:
        get:
            description: The operator's view. `GET /options` only ever shows enabled rows, so this is the only way to see one that was switched off.
            parameters:
                - in: query
                  name: type
                  schema:
                    $ref: '#/components/schemas/OptionType'
                - in: query
                  name: is_enabled
                  schema:
                    type: boolean
                - in: query
                  name: owner_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OptionList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
            security:
                - bearerAuth: []
            summary: List every integration, disabled ones included
            tags:
                - admin
    /admin/payment-sessions:
        get:
            description: Filter by kind, status and a settlement window. Intended for tying platform totals to what the rails report.
            parameters:
                - in: query
                  name: kind
                  schema:
                    $ref: '#/components/schemas/PaymentSessionKind'
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - in: query
                  name: account_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - description: Inclusive lower bound on creation time
                  in: query
                  name: from
                  schema:
                    format: date-time
                    type: string
                - description: Exclusive upper bound on creation time
                  in: query
                  name: to
                  schema:
                    format: date-time
                    type: string
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PaymentSessionPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
            security:
                - bearerAuth: []
            summary: Reconciliation view over all payment sessions
            tags:
                - admin
    /admin/reports:
        get:
            description: |
                Defaults to the open and under-review ones, oldest first — that is the small hot slice the queue is worked from, and the index behind it covers exactly that predicate.
            parameters:
                - description: Defaults to open and reviewing.
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ReportStatus'
                - in: query
                  name: ref_type
                  schema:
                    $ref: '#/components/schemas/ReportRefType'
                - in: query
                  name: reason
                  schema:
                    $ref: '#/components/schemas/ReportReason'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminReportPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The report queue
            tags:
                - admin
    /admin/reports/{id}:
        get:
            description: |
                Includes the reported content itself and how many other open reports name the same target, since a moderator decides on the pattern and not one complaint.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReportID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminReport'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a report with its target
            tags:
                - admin
    /admin/reports/{id}/claim:
        post:
            description: Moves it out of the open queue so two moderators do not work the same case.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReportID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Report'
                    description: Claimed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already claimed or resolved
            security:
                - bearerAuth: []
            summary: Take a report for review
            tags:
                - admin
    /admin/reports/{id}/resolution:
        post:
            description: |
                Records the verdict and what was done about it. `dismissed` takes the action `none`; upholding it names the action instead — the listing was removed, the message was removed, the account was suspended, a warning was issued.

                Recording the action here does not carry it out. Taking a listing down and suspending a seller are separate calls to the modules that own them, so the decision and its effects each stay where they can be audited.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReportID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ResolveReportRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Report'
                    description: Resolved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved
            security:
                - bearerAuth: []
            summary: Resolve a report
            tags:
                - admin
    /admin/tags:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateTagRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Tag'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Slug already taken
            security:
                - bearerAuth: []
            summary: Create a tag
            tags:
                - admin
    /admin/tags/{slug}:
        delete:
            description: Removing it also detaches it from every listing that carried it.
            parameters:
                - in: path
                  name: slug
                  required: true
                  schema:
                    $ref: '#/components/schemas/TagSlug'
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Delete a tag
            tags:
                - admin
    /admin/tax-info/{accountID}/verification:
        post:
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/TaxVerificationRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TaxInfo'
                    description: Recorded
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This tax code is already verified under another account
            security:
                - bearerAuth: []
            summary: Record the verdict on a tax registration
            tags:
                - admin
    /admin/wallets/{accountID}:
        get:
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read another account's balances
            tags:
                - admin
    /admin/wallets/{accountID}/adjustments:
        post:
            description: |
                The one way a balance changes without a business event behind it, so it is the most auditable: a reason is mandatory and an `idempotency_key` is required, because a retried correction that posts twice is money invented. Reusing a key returns the movement already recorded instead of making a second one.
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/WalletAdjustmentRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletTransaction'
                    description: This idempotency key was already applied; the existing movement is returned
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletTransaction'
                    description: Adjusted
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The adjustment would drive a balance negative
            security:
                - bearerAuth: []
            summary: Correct a balance by hand
            tags:
                - admin
    /admin/withdrawals:
        get:
            parameters:
                - description: Defaults to pending
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WithdrawalPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
            security:
                - bearerAuth: []
            summary: The withdrawal review queue
            tags:
                - admin
    /admin/withdrawals/{id}/approval:
        post:
            description: The wallet was already debited when the request was made, so approving does not move the balance again — it settles the outbound leg.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/WithdrawalApprovalRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Withdrawal'
                    description: Approved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved
            security:
                - bearerAuth: []
            summary: Approve a withdrawal and release the transfer
            tags:
                - admin
    /admin/withdrawals/{id}/rejection:
        post:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/WithdrawalRejectionRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Withdrawal'
                    description: Rejected and the debit reversed
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved
            security:
                - bearerAuth: []
            summary: Reject a withdrawal and return the money to the balance
            tags:
                - admin
    /bank-accounts:
        get:
            description: Account numbers come back masked. The full number is only ever used to move money, never to display it back.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BankAccountList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's payout destinations
            tags:
                - finance
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateBankAccountRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BankAccount'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    $ref: '#/components/responses/Conflict'
            security:
                - bearerAuth: []
            summary: Register a payout destination
            tags:
                - finance
    /bank-accounts/{id}:
        delete:
            description: Soft delete. A completed withdrawal names this row as the place real money went, so financial history cannot lose its payee.
            responses:
                "204":
                    description: Removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A withdrawal against it is still unresolved
            security:
                - bearerAuth: []
            summary: Remove a payout destination
            tags:
                - finance
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/BankAccountID'
        patch:
            description: At most one default per account, so this clears the previous one. Bank details themselves are not editable — register another destination and remove this one, so a completed withdrawal never names a row whose details have since changed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateBankAccountRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BankAccount'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Make this the default payout destination
            tags:
                - finance
    /cart-items:
        get:
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CartItemPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's cart
            tags:
                - order
        post:
            description: One row per (account, SKU). Adding a SKU already in the cart is a conflict — change the quantity instead.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AddCartItemRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CartItem'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    $ref: '#/components/responses/Conflict'
            security:
                - bearerAuth: []
            summary: Add a SKU to the cart
            tags:
                - order
    /cart-items/{id}:
        delete:
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Remove a cart row
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/CartItemID'
        patch:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateCartItemRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CartItem'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Change the quantity of a cart row
            tags:
                - order
    /categories:
        get:
            description: |
                Flat list with a parent reference on each row; a client assembles the tree. A category tree is small so this is unpaginated.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CategoryList'
                    description: OK
            summary: The category tree
            tags:
                - catalog
    /contacts:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ContactList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's saved addresses
            tags:
                - account
        post:
            description: |
                The administrative codes are what a carrier is called with, so they are the routing source of truth and the names beside them are display snapshots. A coordinate is optional: geocoding may fail and the address still has to be saveable.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateContactRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Contact'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Save an address
            tags:
                - account
    /contacts/{id}:
        delete:
            responses:
                "204":
                    description: Removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Remove a saved address
            tags:
                - account
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ContactID'
        patch:
            description: |
                Setting a default clears the previous one. Delivery and pickup defaults are independent flags — the same address is usually both.

                An order already placed keeps its own snapshot of the address, so editing this row never rewrites where a shipment was going.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateContactRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Contact'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Update a saved address
            tags:
                - account
    /conversations:
        get:
            description: |
                Threads by latest activity. A participant sits on either side of the stored pair, so this is answered as two ordered index scans merged together rather than one scan that sorts.
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ConversationPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's inbox
            tags:
                - chat
        post:
            description: |
                Idempotent by design: there is exactly one thread per pair, so this returns the existing one when it exists and creates it otherwise. A client does not have to track whether it has messaged someone before.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartConversationRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Conversation'
                    description: The thread already existed
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Conversation'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An account cannot open a thread with itself
            security:
                - bearerAuth: []
            summary: Open the thread with another account
            tags:
                - chat
    /conversations/{id}:
        get:
            description: Carries the counterparty rather than both sides — the caller already knows who they are.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ConversationID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Conversation'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a thread
            tags:
                - chat
    /conversations/{id}/messages:
        get:
            description: |
                Newest first, cursor only. Offset paging would defeat the chunk exclusion the time partitioning exists for.

                A redacted message is still returned, with its body emptied and `deleted_at` set: a gap in a thread that a dispute may hinge on is worse than a visible removal.
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessagePage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a thread's messages
            tags:
                - chat
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ConversationID'
        post:
            description: |
                Text, attachments or both. `metadata` carries references to a listing or a variant or an order, which is how a product is pointed at inside a thread that is not itself listing-scoped.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SendMessageRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                    description: Sent
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A message needs a body or an attachment
            security:
                - bearerAuth: []
            summary: Send a message
            tags:
                - chat
    /conversations/{id}/read:
        post:
            description: |
                Marks the counterparty's messages read. Takes a time bound rather than a list of ids, for the same reason as the notification feed: a bound reads one range of a partitioned table instead of searching all of it. Omit `before` to mark the whole thread read.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ConversationID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MarkConversationReadRequest'
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ChatUnreadCount'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Mark a thread read up to a point in time
            tags:
                - chat
    /conversations/unread-count:
        get:
            description: |
                Read state is currently tracked per message rather than per participant, so this counts unread rows across the whole partitioned table and cannot be bounded by time — an unread message may be arbitrarily old. It is the one read here that does not benefit from chunk exclusion.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ChatUnreadCount'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The unread badge across every thread
            tags:
                - chat
    /devices:
        put:
            description: |
                An upsert on the push token, not a create. The token identifies an install and the platform reissues the same one when a different user signs in on that phone, so the row moves to the new account. Without that, the previous owner keeps receiving that phone's notifications.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterDeviceRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Device'
                    description: Registered or moved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Register or move a push device
            tags:
                - account
    /devices/{id}:
        delete:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/DeviceID'
            responses:
                "204":
                    description: Unregistered
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Unregister a push device
            tags:
                - account
    /disputes/{id}:
        get:
            description: Each side's evidence stays in its own list — the buyer's on the refund, the seller's on the dispute — so a moderator can tell who submitted what.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundDisputeID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundDispute'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a dispute
            tags:
                - order
    /disputes/{id}/attachments:
        post:
            description: Either party may add evidence until the moderator rules. The caller's side of the case decides which list it lands in.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundDisputeID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AddAttachmentsRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundDispute'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Dispute is already resolved
            security:
                - bearerAuth: []
            summary: Add evidence to a dispute
            tags:
                - order
    /drafts:
        get:
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
                - in: query
                  name: state
                  schema:
                    default: live
                    enum:
                        - live
                        - cancelled
                        - expired
                    type: string
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DraftOrderPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's purchase sessions
            tags:
                - order
        post:
            description: |
                Freezes the listing terms so a price the seller edits afterwards cannot be charged. The server snapshots the SPU together with all of its SKUs — price and package_details live on the SKU, and those are exactly what must not move under the buyer — and sets `valid_until`.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateDraftRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DraftOrder'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Listing not found, deleted, or not active
            security:
                - bearerAuth: []
            summary: Open a purchase session for one listing
            tags:
                - order
    /drafts/{id}:
        delete:
            description: Sets `cancelled_at`. A session that already produced paid items cannot be cancelled.
            responses:
                "204":
                    description: Cancelled
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    $ref: '#/components/responses/Conflict'
            security:
                - bearerAuth: []
            summary: Cancel a purchase session
            tags:
                - order
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DraftOrder'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a purchase session and its frozen terms
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/DraftOrderID'
    /drafts/{id}/checkout:
        post:
            description: |
                Creates one `item` per line plus a single buyer-checkout payment session, and returns both. Line prices come from the session's frozen snapshot, never from the live catalog. `contact_id` is snapshotted into every line's delivery address: one session covers one SPU and therefore one seller, so a single address is correct.

                A SKU whose listing is priced `negotiable` is rejected with 422 unless the caller has an accepted offer for it — a negotiated listing must go through the offer flow.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/DraftOrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CheckoutRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CheckoutResult'
                    description: Items created and a payment session opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Session expired, cancelled, or already checked out
                "422":
                    $ref: '#/components/responses/UnprocessableEntity'
            security:
                - bearerAuth: []
            summary: Check out a purchase session (pay-first)
            tags:
                - order
    /email/verification-requests:
        post:
            responses:
                "202":
                    description: Verification message sent
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already verified
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No email on this account
            security:
                - bearerAuth: []
            summary: Send a verification link to the caller's email
            tags:
                - account
    /email/verifications:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/EmailVerificationRequest'
                required: true
            responses:
                "204":
                    description: Verified
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Token is unknown or already used or expired
            summary: Confirm an email address
            tags:
                - account
    /favorites:
        get:
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/FavoritePage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's wishlist
            tags:
                - account
    /favorites/{spuID}:
        delete:
            responses:
                "204":
                    description: Removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Unsave a listing
            tags:
                - account
        parameters:
            - in: path
              name: spuID
              required: true
              schema:
                $ref: '#/components/schemas/ProductSPUID'
        put:
            description: Idempotent. The account and listing pair is the whole row so saving twice is the same as saving once.
            responses:
                "204":
                    description: Saved
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Save a listing
            tags:
                - account
    /feed:
        get:
            description: |
                Nearest-neighbour search over listing embeddings from the caller's interest slots. Falls back to the newest active listings for an account with no interests computed yet.
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's personalised feed
            tags:
                - catalog
    /follows/{accountID}:
        delete:
            responses:
                "204":
                    description: Unfollowed
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Unfollow a seller
            tags:
                - account
        parameters:
            - in: path
              name: accountID
              required: true
              schema:
                $ref: '#/components/schemas/AccountID'
        put:
            description: Idempotent. Following yourself is refused.
            responses:
                "204":
                    description: Following
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An account cannot follow itself
            security:
                - bearerAuth: []
            summary: Follow a seller
            tags:
                - account
    /identity-documents:
        post:
            description: |
                Opens a case with the KYC vendor and returns the vendor's session URL for the caller to finish in. Neither a document number nor a scan is stored here — only the vendor, its case reference and the verdict — so a leak of this data cannot impersonate anyone.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartIdentityVerificationRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/IdentityVerificationTicket'
                    description: Case opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This account already holds a live verified document
            security:
                - bearerAuth: []
            summary: Start identity verification
            tags:
                - account
    /items:
        get:
            description: '`role=seller&state=pending` is the seller''s confirmation inbox: paid items that no order covers yet.'
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - buyer
                        - seller
                    type: string
                - in: query
                  name: state
                  schema:
                    $ref: '#/components/schemas/OrderItemState'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OrderItemPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List checkout items, as buyer or as seller
            tags:
                - order
    /items/{id}/cancellation:
        post:
            description: Either party may cancel while the item is not yet part of an order. The cancelling account is recorded.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderItemID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CancellationRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OrderItem'
                    description: Cancelled
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already confirmed into an order, or already cancelled
            security:
                - bearerAuth: []
            summary: Cancel an unconfirmed item
            tags:
                - order
    /listings:
        get:
            description: |
                Live active listings only. `sort=price-asc` and `price-desc` are answered through the SKU price index rather than a cached price on the listing, so the `price` shown is that of the cheapest matching variant.
            parameters:
                - in: query
                  name: category_id
                  schema:
                    $ref: '#/components/schemas/CategoryID'
                - in: query
                  name: tag
                  schema:
                    $ref: '#/components/schemas/TagSlug'
                - in: query
                  name: seller_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - in: query
                  name: condition
                  schema:
                    $ref: '#/components/schemas/ListingCondition'
                - in: query
                  name: min_price
                  schema:
                    format: int64
                    minimum: 0
                    type: integer
                - in: query
                  name: max_price
                  schema:
                    format: int64
                    minimum: 0
                    type: integer
                - in: query
                  name: sort
                  schema:
                    default: newest
                    enum:
                        - newest
                        - rating
                        - price-asc
                        - price-desc
                    type: string
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
            summary: Browse listings
            tags:
                - catalog
        post:
            description: |
                Creates the listing and its variants together. At least one variant is required because price and shipping weight live there.

                Starts as a draft and is not public until published. The seller also chooses here whether the buyer or the seller carries the shipping fee.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateListingRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Category or tag or image not found
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Slug already taken
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Identity verification is required before selling
            security:
                - bearerAuth: []
            summary: Create a listing
            tags:
                - catalog
    /listings/{id}:
        delete:
            description: |
                Soft delete, and distinct from hiding it. Order history holds the listing and variant ids without a foreign key, so a past order has to stay resolvable after the seller removes the listing.
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An open order still covers this listing
            security:
                - bearerAuth: []
            summary: Delete a listing
            tags:
                - catalog
        get:
            description: Carries the variants with their prices and stock and the seller summary. Only the owner and staff can read one that is not active.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read a listing
            tags:
                - catalog
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ProductSPUID'
        patch:
            description: |
                An edit to a draft applies immediately. An edit to a live listing that trips the prohibited-goods filter is held as a pending edit and applied only when a moderator approves it, so the version buyers see never changes into unreviewed content.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateListingRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Applied
                "202":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Held as a pending edit awaiting moderation
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Edit a listing
            tags:
                - catalog
    /listings/{id}/publication:
        delete:
            description: |
                Takes it out of every feed while leaving it alive — the seller is away, or the item is temporarily out of stock. Not the same as deleting it.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Hidden
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Hide a live listing
            tags:
                - catalog
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ProductSPUID'
        post:
            description: |
                Every new listing is scanned automatically for prohibited-goods signals first. A clean one goes live; a suspicious one goes to the moderation queue and stays invisible until a moderator clears it. The response says which happened.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Live
                "202":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingDetail'
                    description: Queued for moderation and not yet public
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already live or under moderation
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A listing needs at least one variant with a price
            security:
                - bearerAuth: []
            summary: Publish a listing
            tags:
                - catalog
    /listings/{id}/skus:
        post:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ProductSPUID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateSkuRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Sku'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Add a variant to a listing
            tags:
                - catalog
    /listings/{spuID}/reviews:
        get:
            description: Public, with the seller's reply and the vote tally on each one.
            parameters:
                - description: Restrict to one star rating.
                  in: query
                  name: rating
                  schema:
                    maximum: 5
                    minimum: 1
                    type: integer
                - in: query
                  name: sort
                  schema:
                    default: newest
                    enum:
                        - newest
                        - helpful
                    type: string
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReviewPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Reviews of a listing
            tags:
                - trust
        parameters:
            - in: path
              name: spuID
              required: true
              schema:
                $ref: '#/components/schemas/ProductSPUID'
        post:
            description: |
                An order is required — no purchase, no review — and one review per listing per order, so buying twice earns a second review but one purchase does not earn two.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitReviewRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Review'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The order does not belong to the caller
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already reviewed for this order
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The order did not include this listing or is not finished
            security:
                - bearerAuth: []
            summary: Review a listing you bought
            tags:
                - trust
    /login:
        post:
            description: The identifier is an email or a phone or a username. Which one it is does not change the answer and is not reported back.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AuthResult'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Wrong credentials
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Account suspended
            summary: Sign in with a password
            tags:
                - account
    /login/oauth:
        post:
            description: |
                Links to an existing account when the provider's subject id is already known, and otherwise creates one. A provider that returns no email — Apple with "hide my email" — gets a generated username, because the account still has to carry an identifier.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/OAuthLoginRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AuthResult'
                    description: Signed in to an existing account
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AuthResult'
                    description: A new account was created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Provider rejected the credential
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Account suspended
            summary: Sign in through a federated provider
            tags:
                - account
    /logout:
        post:
            description: Also unregisters the caller's push device when one is supplied so the phone stops receiving notifications for an account nobody is signed in to.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LogoutRequest'
                required: false
            responses:
                "204":
                    description: Session revoked
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Revoke the current session
            tags:
                - account
    /me:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Me'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Read the caller's own account and profile
            tags:
                - account
        patch:
            description: |
                Changing the email clears `email_verified` and sends a fresh verification; changing the phone clears its verified flag the same way. The last remaining identifier cannot be removed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateAccountRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Me'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Email or phone or username already taken
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: That would leave the account with no identifier
            security:
                - bearerAuth: []
            summary: Change the caller's identifiers
            tags:
                - account
    /me/devices:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeviceList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's push devices
            tags:
                - account
    /me/following:
        get:
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AccountSummaryPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Sellers the caller follows
            tags:
                - account
    /me/identity-documents:
        get:
            description: Includes `expires_at` — a passport runs out and a payout gate has to look at more than the status.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/IdentityDocumentList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's verification history
            tags:
                - account
    /me/listings:
        get:
            description: Unlike browsing this shows every state — draft, pending, active, hidden — because a seller has to see what is not public.
            parameters:
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ListingStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's own listings
            tags:
                - catalog
    /me/oauth-identities:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OAuthIdentityList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's linked providers
            tags:
                - account
    /me/oauth-identities/{provider}:
        delete:
            description: Refused when it would leave the account with no way to sign in — no password and no other linked provider.
            parameters:
                - in: path
                  name: provider
                  required: true
                  schema:
                    $ref: '#/components/schemas/OAuthProvider'
            responses:
                "204":
                    description: Unlinked
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This is the only way left to sign in
            security:
                - bearerAuth: []
            summary: Unlink a provider
            tags:
                - account
    /me/profile:
        patch:
            description: This is the shop front. `locale` and `timezone` also decide how notifications are written and when they are sent.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateProfileRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Profile'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Update the caller's public profile
            tags:
                - account
    /messages/{id}:
        delete:
            description: |
                The sender unsending, or a moderator acting on an upheld report. The row survives with its body cleared: a thread is evidence in a refund dispute, so removal is recorded rather than erased.
            responses:
                "204":
                    description: Redacted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Only the sender or a moderator may redact a message
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Redact a message
            tags:
                - chat
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/MessageID'
        patch:
            description: |
                Sender only, and it records that the message was edited — the counterparty has to be able to see that what they read has changed. A system message cannot be edited because it has no author.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateMessageRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Only the sender may edit a message
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A system message cannot be edited
            security:
                - bearerAuth: []
            summary: Edit a message
            tags:
                - chat
    /notification-preferences:
        get:
            description: |
                Returns every category and channel pair with the value in force. Stored rows are sparse — one exists only where the account differs from the default — and the defaults live in the domain, so resolving them here keeps the client from having to know them.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/NotificationPreferenceList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Read the effective notification settings
            tags:
                - account
        put:
            description: |
                Setting a pair back to its default deletes the row rather than storing the default again.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateNotificationPreferencesRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/NotificationPreferenceList'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Change notification settings
            tags:
                - account
    /notifications:
        get:
            description: |
                Newest first, cursor only. The table is chunked by time, so a cursor is what lets the reader stop at a chunk boundary instead of touching every chunk.
            parameters:
                - in: query
                  name: category
                  schema:
                    $ref: '#/components/schemas/NotificationCategory'
                - description: Restrict to unread ones.
                  in: query
                  name: unread
                  schema:
                    type: boolean
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/NotificationPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's notification feed
            tags:
                - account
    /notifications/read:
        post:
            description: |
                Takes a time bound rather than a list of ids. Marking individual rows by id would have to search every chunk of a time-partitioned table; a bound reads one range. Omit `before` to mark everything read.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MarkNotificationsReadRequest'
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UnreadCount'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Mark notifications read up to a point in time
            tags:
                - account
    /notifications/unread-count:
        get:
            description: A separate endpoint because it is read far more often than the feed and answers from one partial index.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UnreadCount'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The unread badge
            tags:
                - account
    /offers:
        get:
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - buyer
                        - seller
                    type: string
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/OfferStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OfferPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List negotiations, as buyer or as seller
            tags:
                - order
        post:
            description: Either side may open it. One active negotiation per (buyer, SKU); the terms are then revised in place rather than by stacking rows.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateOfferRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Offer'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An active negotiation already exists for this (buyer, SKU)
            security:
                - bearerAuth: []
            summary: Open a price negotiation on a SKU
            tags:
                - order
    /offers/{id}:
        delete:
            responses:
                "204":
                    description: Cancelled
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Negotiation is no longer active
            security:
                - bearerAuth: []
            summary: Cancel a negotiation
            tags:
                - order
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Offer'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a negotiation
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/OfferID'
        patch:
            description: Revises the current terms in place and moves authorship to the caller. Only the party that does not own the standing proposal may counter.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateOfferRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Offer'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller already owns the standing proposal, or is not a party
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Negotiation is no longer active
            security:
                - bearerAuth: []
            summary: Counter-offer
            tags:
                - order
    /offers/{id}/acceptance:
        post:
            description: |
                Only the buyer closes a negotiation, whichever side proposed the standing terms — an order may not appear without the buyer's explicit acceptance. Opens the checkout payment session at the agreed total and creates the item, returning both.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OfferID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AcceptOfferRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CheckoutResult'
                    description: Accepted; item created and a payment session opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the buyer of this negotiation
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Negotiation is expired, cancelled, or already accepted
            security:
                - bearerAuth: []
            summary: Accept the negotiated terms (buyer)
            tags:
                - order
    /options:
        get:
            description: |
                The checkout list: payment rails, carriers, notification channels. Enabled options only, ordered by `priority` then `id` — priority ties are possible, so the id is the tiebreaker that keeps the order stable between calls.

                Public: which rails a marketplace accepts is not a secret, and a buyer sees them before signing in.
            parameters:
                - in: query
                  name: type
                  required: true
                  schema:
                    $ref: '#/components/schemas/OptionType'
                - description: Restrict to one seller's own options.
                  in: query
                  name: owner_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OptionList'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
            summary: List enabled integrations of one type
            tags:
                - common
        post:
            description: |
                A seller configuring their own carriers or payment rails. `id` is a marketplace-wide slug shared with the system-provided options, so it is supplied by the caller and collides loudly rather than being silently renamed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateOptionRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Option'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This slug is taken
            security:
                - bearerAuth: []
            summary: Register an integration the caller owns
            tags:
                - common
    /options/{id}:
        delete:
            responses:
                "204":
                    description: Removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Still named by an unsettled payment or an in-flight shipment
            security:
                - bearerAuth: []
            summary: Remove an integration
            tags:
                - common
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Option'
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read one integration
            tags:
                - common
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/OptionSlug'
        patch:
            description: |
                The owner, or an admin for a system-provided one. The slug is not editable: other modules store it as a plain string with no foreign key (`item.transport_option`, `transaction.payment_option`), so renaming it would orphan every row that named it.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateOptionRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Option'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Update an integration
            tags:
                - common
    /orders:
        get:
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - buyer
                        - seller
                    type: string
                - in: query
                  name: state
                  schema:
                    $ref: '#/components/schemas/OrderState'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OrderPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List orders, as buyer or as seller
            tags:
                - order
        post:
            description: |
                The seller accepts the paid items of one purchase session. Creates the shipment, then the order, then links the items to it. One order per session is enforced by a unique constraint, so a retried confirmation cannot mint a second order. When the listing says the seller pays shipping, a seller-confirmation-fee payment session is opened too and returned in `shipping_fee_session`.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ConfirmOrderRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ConfirmOrderResult'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the seller of these items
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Session already confirmed, items unpaid, or items cancelled
                "422":
                    $ref: '#/components/responses/UnprocessableEntity'
            security:
                - bearerAuth: []
            summary: Confirm paid items into an order (seller)
            tags:
                - order
    /orders/{id}:
        get:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Order'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read an order
            tags:
                - order
    /orders/{id}/cancellation:
        post:
            description: Either party may cancel while the shipment has not left `pending`.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CancellationRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Order'
                    description: Cancelled
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Shipment already moved, or the order is closed
            security:
                - bearerAuth: []
            summary: Cancel an order
            tags:
                - order
    /orders/{id}/receipt:
        post:
            description: |
                The buyer confirms delivery and attaches unboxing evidence — at least one photo or video is mandatory, because this evidence is what a later refund or dispute is judged on. Sets `received_at` and starts the 72-hour payout countdown in `payout_deadline_at`.

                Confirming twice is a conflict rather than a no-op: a retried upload must not be able to restart the clock and move the payout date.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ConfirmReceiptRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Order'
                    description: Receipt recorded
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the buyer of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already confirmed, or the order is cancelled
            security:
                - bearerAuth: []
            summary: Confirm receipt of the goods (buyer)
            tags:
                - order
    /orders/{id}/refunds:
        post:
            description: |
                Evidence is mandatory. The request goes straight to seller review with a 48-hour deadline in `review_deadline_at`; if the seller neither accepts nor rejects before it passes, a job refunds the buyer in full. One open refund per order.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateRefundRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Refund'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the buyer of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A refund is already open on this order
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Order is not in a refundable state
            security:
                - bearerAuth: []
            summary: Open a refund request (buyer)
            tags:
                - order
    /orders/{id}/transport:
        get:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Transport'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read the shipment and its tracking events
            tags:
                - order
    /orders/{orderID}/feedback:
        get:
            description: |
                Returns the caller's own submission always, and the counterparty's only once it has been published. An unpublished direction comes back as `submitted` without its rating.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OrderFeedback'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read the feedback on one order
            tags:
                - trust
        parameters:
            - in: path
              name: orderID
              required: true
              schema:
                $ref: '#/components/schemas/OrderID'
        post:
            description: |
                One submission per direction per order, so this cannot be revised — a rating that could be edited after seeing the other side is not blind.

                The direction is derived from which side of the order the caller is on rather than sent, so nobody can file feedback as the other party.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitFeedbackRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Feedback'
                    description: Submitted
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not a party to this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already submitted for this direction
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The order is not finished yet
            security:
                - bearerAuth: []
            summary: Rate the counterparty of a completed order
            tags:
                - trust
    /password:
        put:
            description: Requires the current password even though the caller is already authenticated — a stolen access token must not be enough to take the account.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ChangePasswordRequest'
                required: true
            responses:
                "204":
                    description: Changed and every other session revoked
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Current password is wrong
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This account signs in through a provider and has no password
            security:
                - bearerAuth: []
            summary: Change the caller's password
            tags:
                - account
    /password/reset-requests:
        post:
            description: |
                Always answers 202, whether or not the identifier exists. Reporting that an address is unknown turns this endpoint into a way to enumerate accounts.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PasswordResetRequest'
                required: true
            responses:
                "202":
                    description: If the identifier exists a reset message was sent
                "400":
                    $ref: '#/components/responses/BadRequest'
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Too many requests for this identifier
            summary: Ask for a password reset
            tags:
                - account
    /password/resets:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PasswordResetConfirmRequest'
                required: true
            responses:
                "204":
                    description: Password set and every session revoked
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Token is unknown or already used or expired
            summary: Set a new password with a reset token
            tags:
                - account
    /payment-sessions:
        get:
            description: '`role=payer` is money owed or sent, `role=payee` is money received — seller payouts and refunds land on that side.'
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - payer
                        - payee
                    type: string
                - in: query
                  name: kind
                  schema:
                    $ref: '#/components/schemas/PaymentSessionKind'
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PaymentSessionPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's payment sessions
            tags:
                - finance
    /payment-sessions/{id}:
        get:
            description: |
                `amount_paid` is the sum of the settled legs, so a client can tell a partly-tendered session from an unpaid one without fetching the legs.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PaymentSession'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a payment session
            tags:
                - finance
    /payment-sessions/{id}/cancellation:
        post:
            description: Only while nothing has settled. A session with a settled leg is closed by refunding, not by cancelling.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PaymentSession'
                    description: Cancelled
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A leg has already settled
            security:
                - bearerAuth: []
            summary: Abandon an unpaid payment session
            tags:
                - finance
    /payment-sessions/{id}/payments:
        post:
            description: |
                Creates a ledger leg on the chosen rail and returns it with the gateway redirect in `checkout_url`. Omit `amount` to tender the whole outstanding balance; pass it to split the session across several rails, calling this again per rail until the total is covered.

                The leg starts `pending` and only the provider's webhook settles it — this response is not a receipt. Poll the session or the leg for the outcome.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartPaymentRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Transaction'
                    description: Leg created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the payer of this session
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Session or payment option not found
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Session is already settled, cancelled, or expired
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Amount exceeds the session's outstanding balance
            security:
                - bearerAuth: []
            summary: Pay a session through one rail
            tags:
                - finance
    /payment-sessions/{id}/transactions:
        get:
            description: Append-only. A reversal is a new leg with a negative amount pointing at the leg it reverses, never an edit of the original.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TransactionList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: List the legs of a payment session
            tags:
                - finance
    /refunds:
        get:
            description: '`role=seller` resolves through the order''s seller; a refund row itself only records the buyer.'
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - buyer
                        - seller
                    type: string
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/RefundStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List refunds, as buyer or as seller
            tags:
                - order
    /refunds/{id}:
        delete:
            responses:
                "204":
                    description: Withdrawn
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already decided or disputed
            security:
                - bearerAuth: []
            summary: Withdraw a refund request (buyer)
            tags:
                - order
        get:
            description: Carries `review_deadline_at` for the countdown, and `dispute_id` once a rejection opened one — that is how either party reaches the dispute.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Refund'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a refund
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/RefundID'
    /refunds/{id}/acceptance:
        post:
            description: Releases the held money back to the buyer. Finance writes the reversal leg and the refund records it.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Refund'
                    description: Accepted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the seller of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Refund is not awaiting review
            security:
                - bearerAuth: []
            summary: Accept a refund (seller)
            tags:
                - order
    /refunds/{id}/attachments:
        post:
            description: The buyer may keep supplementing evidence while the refund is under seller review or in dispute, up to the moderator's ruling.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AddAttachmentsRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Refund'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Refund is already closed
            security:
                - bearerAuth: []
            summary: Add evidence to a refund (buyer)
            tags:
                - order
    /refunds/{id}/rejection:
        post:
            description: |
                A rejection *is* the dispute: refusing costs the seller a reason and counter-evidence, and the case goes to a moderator immediately. The response is the dispute, not the refund.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RejectRefundRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundDispute'
                    description: Dispute opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the seller of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Refund is not awaiting review
            security:
                - bearerAuth: []
            summary: Reject a refund and open the dispute (seller)
            tags:
                - order
    /register:
        post:
            description: |
                At least one of email, phone or username is required — an account nobody can be addressed by cannot sign in. The display profile is created in the same transaction, so an account without a name is never a reachable state.

                Always creates a plain user. Moderators come from `POST /admin/moderators`.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AuthResult'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Email or phone or username already taken
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No identifier supplied
            summary: Create an account
            tags:
                - account
    /reports:
        get:
            description: A reporter sees the status of their own reports but not who else reported the same target.
            parameters:
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ReportStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReportPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Reports the caller filed
            tags:
                - trust
        post:
            description: |
                A listing, an account, a message, a review or a reply. `ref_id` is the opaque id of that thing and its prefix has to agree with `ref_type`.

                One open report per target per reporter, so re-reporting while the first is still being looked at is a conflict rather than a second row. Enough distinct reports against one listing move it into the moderation queue on their own.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitReportRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Report'
                    description: Filed
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The reported thing does not exist
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: You already have an open report against this target
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The id does not match the declared target type
            security:
                - bearerAuth: []
            summary: Report something
            tags:
                - trust
    /resources:
        post:
            description: |
                Returns the resource row together with a presigned URL to PUT the bytes to. The resource stays incomplete — and unusable as an attachment — until `POST /resources/{id}/completion` confirms it.

                Pass `checksum` when it is known up front: if the same bytes are already stored, the existing resource comes back with no `upload_url` and there is nothing to upload. Several sellers listing the same photo then share one object.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateUploadRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UploadTicket'
                    description: These bytes are already stored so nothing needs uploading
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UploadTicket'
                    description: Slot reserved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: MIME type or declared size outside what this account may upload
            security:
                - bearerAuth: []
            summary: Reserve an upload slot
            tags:
                - common
    /resources/{id}:
        delete:
            description: Soft delete. The row is kept until a reaper has removed the object from the storage backend, because the row holds the only handle on it.
            responses:
                "204":
                    description: Marked for deletion
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Delete a resource
            tags:
                - common
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Resource'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a resource and get a URL to fetch it
            tags:
                - common
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ResourceID'
    /resources/{id}/completion:
        post:
            description: |
                The server reads the object back from storage and records the size, MIME type and checksum it actually has, rather than what the client said it would be. Until this succeeds the resource cannot be attached to anything.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ResourceID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Resource'
                    description: Confirmed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already confirmed
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No object at the reserved key or it does not match what was reserved
            security:
                - bearerAuth: []
            summary: Confirm an upload finished
            tags:
                - common
    /review-replies/{id}:
        delete:
            description: The author, or a moderator acting on an upheld report.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReviewReplyID'
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Delete a reply
            tags:
                - trust
    /reviews/{id}:
        delete:
            description: |
                The author, or a moderator acting on an upheld report. Removal drops it from the seller's rating too, so the recomputed reputation follows.
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Delete a review
            tags:
                - trust
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Review'
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read a review
            tags:
                - trust
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ReviewID'
        patch:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateReviewRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Review'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Edit your own review
            tags:
                - trust
    /reviews/{id}/replies:
        post:
            description: |
                Mainly the seller answering, though anyone may add to the thread. There is no per-thread limit — a conversation that stops after one reply each is not a conversation.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReviewID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitReviewReplyRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReviewReply'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Reply to a review
            tags:
                - trust
    /reviews/{id}/vote:
        delete:
            description: |
                Removes the row rather than storing a neutral one. A stored zero is a row that says nothing.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReviewVoteTally'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Withdraw your vote
            tags:
                - trust
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ReviewID'
        put:
            description: One vote per account per review, replaced in place. Voting on your own review is refused.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/VoteReviewRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReviewVoteTally'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An account cannot vote on its own review
            security:
                - bearerAuth: []
            summary: Vote a review helpful or not
            tags:
                - trust
    /search/listings:
        get:
            description: |
                `lexical` matches the listing name by trigram and tolerates missing diacritics. `semantic` runs an approximate nearest-neighbour search over the dense embedding. `hybrid` combines the dense vector with the sparse lexical one, which is what the embedding model is built for.

                `score` is always returned so that higher is better, whichever mode ran. A listing whose embedding has not been computed yet can only be found lexically.
            parameters:
                - in: query
                  name: q
                  required: true
                  schema:
                    maxLength: 200
                    minLength: 1
                    type: string
                - in: query
                  name: mode
                  schema:
                    default: hybrid
                    enum:
                        - lexical
                        - semantic
                        - hybrid
                    type: string
                - in: query
                  name: category_id
                  schema:
                    $ref: '#/components/schemas/CategoryID'
                - in: query
                  name: min_price
                  schema:
                    format: int64
                    minimum: 0
                    type: integer
                - in: query
                  name: max_price
                  schema:
                    format: int64
                    minimum: 0
                    type: integer
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingSearchResult'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
            summary: Search listings
            tags:
                - catalog
    /skus/{id}:
        delete:
            description: Soft delete for the same reason as a listing. Refused for the last remaining variant of a live listing.
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This is the only variant of a live listing
            security:
                - bearerAuth: []
            summary: Delete a variant
            tags:
                - catalog
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ProductSKUID'
        patch:
            description: |
                A price change does not reach a buyer who already opened a purchase session: that session froze the terms it was opened with.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateSkuRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Sku'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Edit a variant
            tags:
                - catalog
    /skus/{id}/stock:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Stock'
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read a variant's stock
            tags:
                - catalog
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ProductSKUID'
        put:
            description: |
                Sets the total on hand. The reserved count is not settable here — it is moved by checkout and by cancellation — and a total below what is already reserved is refused, because an oversold row must not be representable.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SetStockRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Stock'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Below the quantity already reserved
            security:
                - bearerAuth: []
            summary: Set a variant's stock
            tags:
                - catalog
    /tags:
        get:
            description: A tag id is its slug and a natural key, so it is readable on the wire rather than encoded.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TagList'
                    description: OK
            summary: The tag dictionary
            tags:
                - catalog
    /tax-info:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TaxInfo'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Nothing registered yet
            security:
                - bearerAuth: []
            summary: Read the caller's tax registration
            tags:
                - finance
        put:
            description: |
                One registration per account, so this is an upsert rather than a create. Replacing it resets verification to `pending`: the previous verdict was about the previous tax code. A code already verified under another account is rejected.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpsertTaxInfoRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TaxInfo'
                    description: Registered
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This tax code is already verified under another account
            security:
                - bearerAuth: []
            summary: Register or replace the caller's tax registration
            tags:
                - finance
    /token/refresh:
        post:
            description: The refresh token is a Redis key. One that has been revoked or has expired is indistinguishable from one that never existed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RefreshRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AuthResult'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Unknown or revoked refresh token
            summary: Exchange a refresh token for a new access token
            tags:
                - account
    /wallets:
        get:
            description: |
                One wallet per currency, so this is a list rather than a single object. It is short and unpaginated: a balance is opened by receiving money in that currency, not by browsing.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's wallet balances
            tags:
                - finance
    /wallets/{currency}:
        get:
            parameters:
                - in: path
                  name: currency
                  required: true
                  schema:
                    $ref: '#/components/schemas/CurrencyCode'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Wallet'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No balance has been opened in this currency
            security:
                - bearerAuth: []
            summary: Read one currency balance
            tags:
                - finance
    /wallets/{currency}/transactions:
        get:
            description: |
                Every balance change with its before/after snapshot, newest first. Ordered by `seq`, not by time: two movements can share a timestamp, and a ledger that cannot be totally ordered cannot be replayed or shown to be complete. A gap in `seq` means a missing row.

                Legs of one logical movement share a `group_id` — a checkout is a debit plus an escrow hold plus a fee — which is what makes "does this add up" answerable.
            parameters:
                - in: path
                  name: currency
                  required: true
                  schema:
                    $ref: '#/components/schemas/CurrencyCode'
                - in: query
                  name: kind
                  schema:
                    $ref: '#/components/schemas/WalletTransactionKind'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletTransactionPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No balance has been opened in this currency
            security:
                - bearerAuth: []
            summary: Read one wallet's ledger
            tags:
                - finance
    /withdrawals:
        get:
            parameters:
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WithdrawalPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's withdrawals
            tags:
                - finance
        post:
            description: |
                Debits the available balance and opens a withdrawal for review; an admin resolves it before money leaves the platform. Only the available balance can be drawn — money still held in escrow is not the caller's yet.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateWithdrawalRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Withdrawal'
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Bank account or currency balance not found
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Insufficient available balance, or the payee identity is unverified
            security:
                - bearerAuth: []
            summary: Request a cash-out to a bank account
            tags:
                - finance
    /withdrawals/{id}:
        delete:
            responses:
                "204":
                    description: Cancelled and the debit reversed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved by an admin
            security:
                - bearerAuth: []
            summary: Withdraw the request and return the money to the balance
            tags:
                - finance
        get:
            description: A withdrawal is a payment session of kind `withdrawal`, so its id carries the session prefix.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Withdrawal'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a withdrawal
            tags:
                - finance
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/PaymentSessionID'
servers:
    - description: Gateway (relative to host)
      url: /api/v1
tags:
    - name: account
    - name: catalog
    - name: order
    - name: finance
    - name: chat
    - name: trust
    - name: common
    - description: Moderator and admin console surface. Separate from the user-facing routes because the same URL must never return two different populations depending on the caller's role.
      name: admin