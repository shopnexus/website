components:
    parameters:
        Cursor:
            description: Opaque cursor from the previous page's `next_cursor`. Omit for the first page.
            in: query
            name: cursor
            required: false
            schema:
                type: string
        Limit:
            in: query
            name: limit
            required: false
            schema:
                default: 20
                maximum: 100
                minimum: 1
                type: integer
        Page:
            description: 1-based page number.
            in: query
            name: page
            required: false
            schema:
                default: 1
                minimum: 1
                type: integer
    responses:
        BadRequest:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Validation or malformed request
        Conflict:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Conflicts with the current state (already exists, already done, wrong state)
        Forbidden:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Authenticated but not allowed to act on this resource
        NotFound:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Resource not found
        Unauthorized:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Missing or invalid credentials
        UnprocessableEntity:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Well-formed but rejected by a business rule
    schemas:
        AccountCreateUploadRequest:
            description: |
                Like `CreateUploadRequest`, plus `kind`: the same store serves an avatar and an identity scan, so the service has to be told which it is presigning.
            properties:
                filename:
                    description: |
                        Kept only for its extension. The stored key is generated — a path a client chose is a directory traversal waiting for a backend that resolves one.
                    example: front.jpg
                    maxLength: 255
                    type: string
                kind:
                    enum:
                        - avatar
                        - identity
                    example: avatar
                    type: string
                mime:
                    example: image/jpeg
                    maxLength: 100
                    type: string
                size:
                    example: 204800
                    format: int64
                    maximum: 10485760
                    minimum: 1
                    type: integer
            required:
                - kind
                - filename
                - mime
                - size
            type: object
        AccountID:
            example: acc_62mxefynht57b
            pattern: ^acc_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        AccountRole:
            description: |
                `user` self-registers and both buys and sells. `moderator` is granted by an admin. `admin` is configured, not created.
            enum:
                - user
                - moderator
                - admin
            type: string
        AccountStatus:
            enum:
                - active
                - suspended
            type: string
        AccountSummary:
            description: The compact form used in follower and following lists.
            properties:
                avatar:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                id:
                    $ref: '#/components/schemas/AccountID'
                name:
                    example: Nguyễn Văn An
                    type: string
            required:
                - id
                - name
            type: object
        AccountSummaryPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/AccountSummary'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        AddAttachmentsRequest:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    maxItems: 10
                    minItems: 1
                    type: array
            required:
                - attachments
            type: object
        AddCartItemRequest:
            properties:
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
                variant_id:
                    $ref: '#/components/schemas/VariantID'
            required:
                - variant_id
                - quantity
            type: object
        AdminAccount:
            description: |
                The staff view of an account. Carries the identifiers and the suspension state that `PublicAccount` withholds.
            properties:
                created_at:
                    format: date-time
                    type: string
                email:
                    nullable: true
                    type: string
                email_verified:
                    type: boolean
                id:
                    $ref: '#/components/schemas/AccountID'
                identity_verified:
                    type: boolean
                name:
                    type: string
                phone:
                    nullable: true
                    type: string
                role:
                    $ref: '#/components/schemas/AccountRole'
                status:
                    $ref: '#/components/schemas/AccountStatus'
                suspended_until:
                    description: Null while suspended means the suspension is permanent.
                    format: date-time
                    nullable: true
                    type: string
                suspension_reason:
                    nullable: true
                    type: string
                username:
                    nullable: true
                    type: string
            required:
                - id
                - email_verified
                - name
                - role
                - status
                - identity_verified
                - created_at
            type: object
        AdminAccountPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/AdminAccount'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        AdminIdentityDocument:
            description: A queue entry, which needs the subject alongside the document.
            properties:
                account:
                    $ref: '#/components/schemas/AccountSummary'
                document:
                    $ref: '#/components/schemas/IdentityDocument'
            required:
                - document
                - account
            type: object
        AdminReport:
            description: A queue entry. A moderator needs the reporter and the target beside the report itself.
            properties:
                open_reports_against_target:
                    description: |
                        How many other unresolved reports name the same target. A pattern is what a decision rests on rather than a single complaint.
                    format: int64
                    minimum: 0
                    type: integer
                report:
                    $ref: '#/components/schemas/Report'
                reporter:
                    $ref: '#/components/schemas/AccountSummary'
                resolved_by:
                    allOf:
                        - $ref: '#/components/schemas/AccountSummary'
                    nullable: true
                target:
                    additionalProperties: true
                    description: |
                        The reported content, shaped by `ref_type`. Fetched from the module that owns it.
                    type: object
            required:
                - report
                - reporter
                - open_reports_against_target
            type: object
        AdminReportPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/AdminReport'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        AuthResult:
            properties:
                access_token:
                    type: string
                account:
                    $ref: '#/components/schemas/Me'
                expires_in:
                    description: Access token lifetime in seconds.
                    example: 900
                    minimum: 0
                    type: integer
                refresh_token:
                    type: string
            required:
                - access_token
                - refresh_token
                - expires_in
                - account
            type: object
        BankAccount:
            properties:
                account_holder:
                    type: string
                account_number_masked:
                    description: Only the last digits. The full number leaves the system solely to move money.
                    example: '****1234'
                    type: string
                bank_code:
                    description: Bank identifier, e.g. `vcb`
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/BankAccountID'
                is_default:
                    type: boolean
            required:
                - id
                - bank_code
                - account_number_masked
                - account_holder
                - is_default
                - created_at
            type: object
        BankAccountID:
            example: bnk_2qr5wtc4mx8jd
            pattern: ^bnk_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        BankAccountList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/BankAccount'
                    type: array
            required:
                - data
            type: object
        CartItem:
            description: |
                Carries the listing id as well as the variant's, because a variant has no read of its own in the catalog: rendering the cart is one `GET /listings?ids=` over the `listing_id`s, and the row would otherwise be unresolvable.
            properties:
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/CartItemID'
                listing_id:
                    $ref: '#/components/schemas/ListingID'
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
                variant_id:
                    $ref: '#/components/schemas/VariantID'
            required:
                - id
                - listing_id
                - variant_id
                - quantity
                - created_at
            type: object
        CartItemID:
            example: crt_3n8kvq1wd6ptz
            pattern: ^crt_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        Category:
            properties:
                description:
                    example: Hand-thrown and slipcast pottery.
                    type: string
                id:
                    $ref: '#/components/schemas/CategoryID'
                name:
                    example: Ceramics
                    type: string
                parent_id:
                    allOf:
                        - $ref: '#/components/schemas/CategoryID'
                    description: Null for a root category.
                    nullable: true
                score:
                    $ref: '#/components/schemas/SimilarityScore'
            required:
                - id
                - name
                - description
            type: object
        CategoryID:
            example: cat_7v2p41ryaj811
            pattern: ^cat_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        CategoryList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Category'
                    type: array
            required:
                - data
            type: object
        ChangePasswordRequest:
            properties:
                current_password:
                    type: string
                new_password:
                    maxLength: 72
                    minLength: 8
                    type: string
            required:
                - current_password
                - new_password
            type: object
        ChatUnreadCount:
            properties:
                conversations:
                    description: How many threads have anything unread.
                    example: 2
                    format: int64
                    minimum: 0
                    type: integer
                unread:
                    example: 3
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - unread
                - conversations
            type: object
        CheckoutLine:
            description: One variant and how many of it.
            properties:
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
                variant_id:
                    $ref: '#/components/schemas/VariantID'
            required:
                - variant_id
                - quantity
            type: object
        CheckoutOfferRequest:
            description: |
                The address and the carrier, exactly as a fixed-price checkout takes them. The negotiation settled price and quantity; only delivery is decided here, and the buyer pays it either way, so the carrier is their trade-off to make.
            properties:
                contact_id:
                    $ref: '#/components/schemas/ContactID'
                note:
                    maxLength: 500
                    type: string
                transport_option:
                    description: A carrier slug from `POST /shipping-quotes`; one nobody enabled is refused with 422.
                    example: standard-delivery
                    maxLength: 100
                    type: string
            required:
                - contact_id
                - transport_option
            type: object
        CheckoutRequest:
            description: |
                The carrier and the note are per checkout, not per line: one session covers one listing and therefore one seller and one parcel.
            properties:
                contact_id:
                    $ref: '#/components/schemas/ContactID'
                currency:
                    allOf:
                        - $ref: '#/components/schemas/CurrencyCode'
                    description: Must match the listing's currency.
                lines:
                    items:
                        $ref: '#/components/schemas/CheckoutLine'
                    minItems: 1
                    type: array
                note:
                    example: Please leave at reception
                    maxLength: 500
                    type: string
                transport_option:
                    description: |
                        An enabled transport option's slug. The buyer pays delivery, so the trade-off between price and speed is theirs.
                    example: ghn-express
                    maxLength: 100
                    type: string
            required:
                - lines
                - contact_id
                - transport_option
                - currency
            type: object
        CheckoutResult:
            description: |
                The lines and the single session that pays for them. Nothing is settled yet: the order appears when that session completes, and no seller confirms anything.
            properties:
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                goods_total:
                    description: The items alone, smallest currency unit.
                    example: 598000
                    format: int64
                    minimum: 0
                    type: integer
                items:
                    items:
                        $ref: '#/components/schemas/OrderItem'
                    type: array
                payment_session_id:
                    $ref: '#/components/schemas/PaymentSessionID'
                shipping_fee:
                    description: |
                        What the chosen carrier quoted for this parcel to this address. The buyer pays it on a fixed-price sale and a negotiated one alike, and it is quoted here rather than sent, so a client cannot decide what delivery costs.
                    example: 15000
                    format: int64
                    minimum: 0
                    type: integer
                total:
                    description: 'What the session asks for: goods plus delivery'
                    example: 613000
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - items
                - payment_session_id
                - goods_total
                - shipping_fee
                - total
                - currency
            type: object
        ConfirmReceiptRequest:
            properties:
                attachments:
                    description: |
                        Unboxing photos or video. At least one is mandatory — a later refund or dispute is judged on this evidence.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    maxItems: 10
                    minItems: 1
                    type: array
            required:
                - attachments
            type: object
        Contact:
            properties:
                address:
                    description: Street or house-number line below ward level.
                    type: string
                address_detail:
                    description: Unit or floor or notes.
                    nullable: true
                    type: string
                address_type:
                    $ref: '#/components/schemas/ContactAddressType'
                country:
                    maxLength: 2
                    minLength: 2
                    type: string
                created_at:
                    format: date-time
                    type: string
                district_code:
                    description: |
                        Null where the country has no district tier. Vietnam dropped it in 2025 and now goes province to ward.
                    nullable: true
                    type: string
                district_name:
                    nullable: true
                    type: string
                full_name:
                    type: string
                id:
                    $ref: '#/components/schemas/ContactID'
                is_default_delivery:
                    type: boolean
                is_default_pickup:
                    type: boolean
                latitude:
                    example: 10.7769
                    format: double
                    maximum: 90
                    minimum: -90
                    nullable: true
                    type: number
                longitude:
                    example: 106.7009
                    format: double
                    maximum: 180
                    minimum: -180
                    nullable: true
                    type: number
                phone:
                    description: E.164.
                    type: string
                phone_verified:
                    type: boolean
                postal_code:
                    nullable: true
                    type: string
                provider_codes:
                    additionalProperties: true
                    description: |
                        Per-carrier territory ids. Carriers number territories their own way and some still require a district.
                    type: object
                province_code:
                    type: string
                province_name:
                    type: string
                ward_code:
                    type: string
                ward_name:
                    type: string
            required:
                - id
                - full_name
                - phone
                - phone_verified
                - address_type
                - is_default_delivery
                - is_default_pickup
                - country
                - province_code
                - province_name
                - ward_code
                - ward_name
                - address
                - created_at
            type: object
        ContactAddressType:
            enum:
                - home
                - work
            type: string
        ContactID:
            example: ctc_kvq1wd6ptz3n8
            pattern: ^ctc_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ContactList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Contact'
                    type: array
            required:
                - data
            type: object
        Conversation:
            properties:
                counterparty:
                    $ref: '#/components/schemas/AccountSummary'
                counterparty_read_at:
                    description: |
                        How far the other side has read. This is the read receipt: a message the caller sent is seen when this is at or past its `created_at`, which is why no message carries a delivery status of its own.
                    format: date-time
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ConversationID'
                last_message:
                    allOf:
                        - $ref: '#/components/schemas/Message'
                    nullable: true
                last_message_at:
                    description: |
                        Starts at the creation time so an empty thread still sorts predictably in the inbox.
                    format: date-time
                    type: string
                read_at:
                    description: The caller's own read mark. Null while they have read nothing.
                    format: date-time
                    nullable: true
                    type: string
                unread:
                    description: The counterparty's messages after the caller's read mark.
                    example: 3
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - id
                - counterparty
                - last_message_at
                - unread
                - created_at
            type: object
        ConversationID:
            example: cnv_6ptz3n8kvq1wd
            pattern: ^cnv_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ConversationPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Conversation'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        CreateBankAccountRequest:
            properties:
                account_holder:
                    maxLength: 100
                    minLength: 1
                    type: string
                account_number:
                    maxLength: 50
                    minLength: 1
                    type: string
                bank_code:
                    maxLength: 20
                    minLength: 1
                    type: string
                is_default:
                    default: false
                    type: boolean
            required:
                - bank_code
                - account_number
                - account_holder
            type: object
        CreateCategoryRequest:
            properties:
                description:
                    example: Hand-thrown and slipcast pottery.
                    maxLength: 2000
                    type: string
                name:
                    example: Ceramics
                    maxLength: 100
                    minLength: 1
                    type: string
                parent_id:
                    $ref: '#/components/schemas/CategoryID'
            required:
                - name
            type: object
        CreateContactRequest:
            properties:
                address:
                    maxLength: 255
                    minLength: 1
                    type: string
                address_detail:
                    maxLength: 255
                    type: string
                address_type:
                    $ref: '#/components/schemas/ContactAddressType'
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                district_code:
                    description: Send both district fields or neither.
                    maxLength: 20
                    type: string
                district_name:
                    maxLength: 100
                    type: string
                full_name:
                    maxLength: 100
                    minLength: 1
                    type: string
                is_default_delivery:
                    default: false
                    type: boolean
                is_default_pickup:
                    default: false
                    type: boolean
                latitude:
                    example: 10.7769
                    format: double
                    maximum: 90
                    minimum: -90
                    type: number
                longitude:
                    example: 106.7009
                    format: double
                    maximum: 180
                    minimum: -180
                    type: number
                phone:
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                postal_code:
                    maxLength: 20
                    type: string
                province_code:
                    maxLength: 20
                    minLength: 1
                    type: string
                province_name:
                    maxLength: 100
                    minLength: 1
                    type: string
                ward_code:
                    maxLength: 20
                    minLength: 1
                    type: string
                ward_name:
                    maxLength: 100
                    minLength: 1
                    type: string
            required:
                - full_name
                - phone
                - address_type
                - country
                - province_code
                - province_name
                - ward_code
                - ward_name
                - address
            type: object
        CreateDraftRequest:
            properties:
                listing_id:
                    $ref: '#/components/schemas/ListingID'
            required:
                - listing_id
            type: object
        CreateListingRequest:
            properties:
                attachments:
                    description: Ordered. The first becomes the cover.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                category_id:
                    $ref: '#/components/schemas/CategoryID'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                description:
                    maxLength: 20000
                    type: string
                name:
                    maxLength: 200
                    minLength: 1
                    type: string
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                specifications:
                    additionalProperties: true
                    type: object
                tags:
                    items:
                        $ref: '#/components/schemas/TagSlug'
                    type: array
                variants:
                    description: At least one. Price and shipping weight live on the variant.
                    items:
                        $ref: '#/components/schemas/CreateVariantRequest'
                    minItems: 1
                    type: array
            required:
                - name
                - category_id
                - condition
                - price_mode
                - currency
                - variants
            type: object
        CreateModeratorRequest:
            properties:
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                email:
                    format: email
                    maxLength: 255
                    type: string
                locale:
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                name:
                    maxLength: 100
                    minLength: 1
                    type: string
                password:
                    maxLength: 72
                    minLength: 8
                    type: string
                timezone:
                    maxLength: 64
                    type: string
            required:
                - email
                - password
                - name
                - country
                - locale
                - timezone
            type: object
        CreateOfferRequest:
            properties:
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
                reason:
                    maxLength: 500
                    type: string
                total:
                    example: 598000
                    format: int64
                    minimum: 1
                    type: integer
                variant_id:
                    $ref: '#/components/schemas/VariantID'
            required:
                - variant_id
                - quantity
                - total
            type: object
        CreateRefundRequest:
            description: |
                No item selection and no amount: a refund is for the whole order. Nothing about shipping either — the goods stay with the buyer until the refund is granted, and a granted refund returns the goods' money, not the carriage: that parcel was carried. Delivery comes back only where it never happened, which is a cancellation before pickup rather than a refund. Who bears the return leg is the verdict's business.
            properties:
                attachments:
                    description: Evidence, and it can be topped up until the case closes.
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    maxItems: 10
                    type: array
                reason:
                    example: Not as described
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - reason
            type: object
        CreateUploadRequest:
            properties:
                filename:
                    description: |
                        Kept only for its extension. The stored key is generated — a path a client chose is a directory traversal waiting for a backend that resolves one.
                    example: front.jpg
                    maxLength: 255
                    type: string
                mime:
                    example: image/jpeg
                    maxLength: 100
                    type: string
                size:
                    example: 204800
                    format: int64
                    maximum: 10485760
                    minimum: 1
                    type: integer
            required:
                - filename
                - mime
                - size
            type: object
        CreateVariantRequest:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                attributes:
                    additionalProperties: true
                    type: object
                package_details:
                    additionalProperties: true
                    type: object
                price:
                    example: 299000
                    format: int64
                    minimum: 1
                    type: integer
                quantity:
                    default: 0
                    description: Units on hand at creation.
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - price
                - attributes
                - package_details
            type: object
        CreateWithdrawalRequest:
            properties:
                amount:
                    example: 500000
                    format: int64
                    minimum: 1
                    type: integer
                bank_account_id:
                    $ref: '#/components/schemas/BankAccountID'
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
            required:
                - currency
                - amount
                - bank_account_id
            type: object
        CurrencyCode:
            description: |
                ISO 4217. Never inferred from the account's country — one currency spans many countries, and a seller may want a balance in a foreign one.
            example: VND
            maxLength: 3
            minLength: 3
            pattern: ^[A-Z]{3}$
            type: string
        CursorMeta:
            description: How to continue a cursor-paginated read.
            properties:
                next_cursor:
                    description: |
                        Pass as `cursor` to get the next page. Null on the last one. Always present so "no more pages" is a value rather than a missing key.
                    nullable: true
                    type: string
            required:
                - next_cursor
            type: object
        Device:
            properties:
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/DeviceID'
                last_seen_at:
                    format: date-time
                    type: string
                platform:
                    $ref: '#/components/schemas/DevicePlatform'
                push_token_suffix:
                    description: |
                        The tail of the token, enough for a client to recognise its own install. The whole token is a delivery credential and is never returned.
                    type: string
            required:
                - id
                - platform
                - push_token_suffix
                - last_seen_at
                - created_at
            type: object
        DeviceID:
            example: dvc_ynht57b62mxef
            pattern: ^dvc_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        DeviceList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Device'
                    type: array
            required:
                - data
            type: object
        DevicePlatform:
            enum:
                - ios
                - android
                - web
            type: string
        DisputeRulingRequest:
            description: A ruling, so there is no "still open" choice — which is why the verdict is a boolean rather than the status enum.
            properties:
                buyer_wins:
                    description: What that does depends on the round; see the endpoint.
                    example: true
                    type: boolean
                note:
                    description: Recorded with the verdict; the ruling is final
                    example: Evidence is clear
                    maxLength: 2000
                    type: string
            required:
                - buyer_wins
            type: object
        DisputeStatus:
            description: |
                The winner of one round, or `open` while it is still with a moderator. What winning does depends on the round — see the ruling endpoint.
            enum:
                - open
                - seller-wins
                - buyer-wins
            type: string
        DraftOrder:
            description: |
                The frozen terms, flat rather than nested under a `snapshot`: what a buyer needs is the price they were shown, and the listing's own read answers everything else.
            properties:
                cancelled_at:
                    description: |
                        Set when the buyer closed it, the expiry did, or a checkout spent it — a session is claimed as it is checked out, so exactly one checkout can succeed.
                    format: date-time
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                id:
                    $ref: '#/components/schemas/DraftOrderID'
                listing_id:
                    $ref: '#/components/schemas/ListingID'
                name:
                    example: Ao thun cotton
                    type: string
                price_mode:
                    enum:
                        - fixed
                        - negotiable
                    example: fixed
                    type: string
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                valid_until:
                    format: date-time
                    type: string
                variants:
                    items:
                        $ref: '#/components/schemas/DraftVariantSnapshot'
                    type: array
            required:
                - id
                - listing_id
                - seller_id
                - name
                - currency
                - price_mode
                - variants
                - created_at
                - valid_until
            type: object
        DraftOrderID:
            example: drf_8kvq1wd6ptz3n
            pattern: ^drf_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        DraftOrderPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/DraftOrder'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        DraftVariantSnapshot:
            description: |
                One purchasable variant at the price this session froze. Package details are held in the row but not published: they are what the shipping quote is computed from, not something a buyer picks.
            properties:
                attributes:
                    additionalProperties: true
                    type: object
                price:
                    description: Smallest currency unit
                    example: 299000
                    format: int64
                    minimum: 1
                    type: integer
                variant_id:
                    $ref: '#/components/schemas/VariantID'
            required:
                - variant_id
                - price
            type: object
        EmailVerificationRequest:
            properties:
                token:
                    type: string
            required:
                - token
            type: object
        Error:
            description: |
                The only failure shape in this API, at every status. `data` is absent whenever this is present.
            properties:
                error:
                    properties:
                        code:
                            description: |
                                Stable machine-readable code — switch on this, never on `message`. The cross-cutting ones live in `shared/errx`; the rest belong to the module that raised them.
                            example: validation
                            type: string
                        fields:
                            description: |
                                Per-field detail, present when the request failed validation and absent otherwise. Without it a form with twelve inputs and three problems gets one sentence and cannot mark a single box red.
                            items:
                                $ref: '#/components/schemas/ErrorField'
                            type: array
                        message:
                            description: Human-readable, for a developer or a log. Not a UI string.
                            example: price must be greater than 0
                            type: string
                        request_id:
                            description: |
                                The id of this request, the same value as the `X-Request-Id` response header and the one on every log line for it. Present so a user reporting a failure hands over something that can be grepped, instead of a sentence.
                            example: dkav6vyqeqm3
                            type: string
                    required:
                        - code
                        - message
                        - request_id
                    type: object
            required:
                - error
            type: object
        ErrorField:
            description: One field that failed validation.
            properties:
                field:
                    description: |
                        Dotted path from the request body's root, so a nested or repeated field is addressable by the same client code that rendered it.
                    example: skus[0].price
                    type: string
                message:
                    type: string
                rule:
                    description: |
                        Which constraint failed, as the validate tag that raised it. Machine-readable, so a client can localise the message instead of showing this one.
                    example: required
                    type: string
            required:
                - field
                - rule
                - message
            type: object
        Feedback:
            properties:
                comment:
                    type: string
                created_at:
                    format: date-time
                    type: string
                direction:
                    $ref: '#/components/schemas/FeedbackDirection'
                id:
                    $ref: '#/components/schemas/FeedbackID'
                order_id:
                    $ref: '#/components/schemas/OrderID'
                published_at:
                    description: |
                        Null while the rating is still blind. Only published feedback is visible to anyone but its author and counted towards reputation.
                    format: date-time
                    nullable: true
                    type: string
                ratee_id:
                    $ref: '#/components/schemas/AccountID'
                rater:
                    $ref: '#/components/schemas/AccountSummary'
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            required:
                - id
                - order_id
                - rater
                - ratee_id
                - direction
                - rating
                - comment
                - created_at
            type: object
        FeedbackDirection:
            enum:
                - buyer-to-seller
                - seller-to-buyer
            type: string
        FeedbackID:
            example: fbk_2h9qk4mfx7bd3
            pattern: ^fbk_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        FeedbackPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Feedback'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        IdentityDocument:
            description: |
                A verification outcome. No document number and no scan: the vendor performs the check and only its verdict is kept.
            properties:
                created_at:
                    format: date-time
                    type: string
                doc_type:
                    $ref: '#/components/schemas/IdentityDocumentType'
                expires_at:
                    description: |
                        When the document itself runs out. A payout gate reads this as well as the status.
                    format: date-time
                    nullable: true
                    type: string
                id:
                    $ref: '#/components/schemas/IdentityDocumentID'
                provider:
                    description: KYC vendor, kebab-case.
                    type: string
                rejection_reason:
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/IdentityStatus'
                verified_at:
                    format: date-time
                    nullable: true
                    type: string
            required:
                - id
                - doc_type
                - provider
                - status
                - created_at
            type: object
        IdentityDocumentID:
            example: idd_57b62mxefynht
            pattern: ^idd_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        IdentityDocumentList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/IdentityDocument'
                    type: array
            required:
                - data
            type: object
        IdentityDocumentPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/AdminIdentityDocument'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        IdentityDocumentType:
            enum:
                - national-id
                - passport
                - driver-license
            type: string
        IdentityStatus:
            enum:
                - pending
                - verified
                - rejected
            type: string
        IdentityVerdictRequest:
            properties:
                expires_at:
                    description: When the document runs out.
                    format: date-time
                    type: string
                rejection_reason:
                    description: Required when rejecting.
                    maxLength: 2000
                    type: string
                status:
                    description: A verdict so pending is not a choice.
                    enum:
                        - verified
                        - rejected
                    type: string
            required:
                - status
            type: object
        IdentityVerificationTicket:
            properties:
                document:
                    $ref: '#/components/schemas/IdentityDocument'
                vendor_session_expires_at:
                    format: date-time
                    nullable: true
                    type: string
                vendor_session_url:
                    description: Where the caller finishes the check with the vendor.
                    nullable: true
                    type: string
            required:
                - document
            type: object
        Listing:
            description: The card shown in a feed. `GET /listings/{id}` returns the full form.
            properties:
                category_id:
                    $ref: '#/components/schemas/CategoryID'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                cover:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                deleted_at:
                    description: |
                        Set on a listing the seller removed. Only an `ids` lookup returns one, so that an order or a cart referencing it can still be rendered.
                    format: date-time
                    nullable: true
                    type: string
                favorited:
                    description: |
                        Whether the caller has this on their wishlist. False for an anonymous request. A plain join, because the wishlist is a catalog table — the client never has to fetch its own saves to know which hearts to fill in.
                    type: boolean
                id:
                    $ref: '#/components/schemas/ListingID'
                name:
                    example: Áo thun Uniqlo cổ tròn xanh navy
                    type: string
                price:
                    description: |
                        The featured variant's price, or the cheapest one when a price sort is in force. Not stored on the listing.
                    example: 299000
                    format: int64
                    minimum: 1
                    type: integer
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                rating:
                    description: |
                        Average review rating from 1 to 5, or 0 when there are none yet. Maintained from review events because reviews live in another schema and cannot be joined.
                    example: 4.6
                    format: double
                    maximum: 5
                    minimum: 0
                    type: number
                review_count:
                    description: |
                        How many reviews the rating averages. Beside it rather than derivable from it, because a 5.0 from one review and a 5.0 from two hundred are not the same claim.
                    example: 64
                    format: int64
                    minimum: 0
                    type: integer
                score:
                    $ref: '#/components/schemas/SimilarityScore'
                seller:
                    $ref: '#/components/schemas/AccountSummary'
                slug:
                    description: URL-friendly, globally unique and fixed at creation.
                    example: ao-thun-uniqlo-xanh
                    type: string
                sold:
                    description: |
                        Completed sales across the variants. An open checkout does not count and a cancelled one never did, so the number only goes up. Read from `cached_sold` on the listing rather than summed per request: a feed page would otherwise aggregate the stock rows of every card on it.
                    example: 128
                    format: int64
                    minimum: 0
                    type: integer
                status:
                    $ref: '#/components/schemas/ListingStatus'
            required:
                - id
                - slug
                - name
                - status
                - condition
                - price_mode
                - currency
                - price
                - sold
                - rating
                - review_count
                - category_id
                - seller
                - favorited
                - created_at
            type: object
        ListingCondition:
            description: Item condition. This is a marketplace for used goods so it is listing-level and not optional.
            enum:
                - new
                - used
                - damaged
            type: string
        ListingDetail:
            properties:
                category:
                    $ref: '#/components/schemas/Category'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                deleted_at:
                    description: Set on a listing the seller removed.
                    format: date-time
                    nullable: true
                    type: string
                description:
                    type: string
                favorite_count:
                    description: |
                        How many accounts saved it — social proof on the product page. Only here and not on the card: it is a count per listing, cheap for one row and N counts for a page of them.
                    example: 340
                    format: int64
                    minimum: 0
                    type: integer
                favorited:
                    description: Whether the caller has this saved. False for an anonymous request.
                    type: boolean
                featured_variant_id:
                    allOf:
                        - $ref: '#/components/schemas/VariantID'
                    nullable: true
                id:
                    $ref: '#/components/schemas/ListingID'
                images:
                    description: Ordered. The first is the cover.
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                name:
                    example: Áo thun Uniqlo cổ tròn xanh navy
                    type: string
                pending_edit:
                    allOf:
                        - $ref: '#/components/schemas/PendingEdit'
                    description: |
                        An edit waiting on moderation, null when there is none. Visible to the owner and to staff only; buyers see the published version until it is approved.
                    nullable: true
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                rating:
                    example: 4.6
                    format: double
                    maximum: 5
                    minimum: 0
                    type: number
                review_count:
                    description: How many reviews the rating averages — see Listing.review_count.
                    example: 64
                    format: int64
                    minimum: 0
                    type: integer
                seller:
                    $ref: '#/components/schemas/AccountSummary'
                slug:
                    example: ao-thun-uniqlo-xanh
                    type: string
                sold:
                    description: Completed sales across the variants — see Listing.sold.
                    example: 128
                    format: int64
                    minimum: 0
                    type: integer
                specifications:
                    additionalProperties: true
                    description: Structured attributes specific to the product type.
                    type: object
                status:
                    $ref: '#/components/schemas/ListingStatus'
                tags:
                    items:
                        $ref: '#/components/schemas/TagSlug'
                    type: array
                variants:
                    items:
                        $ref: '#/components/schemas/Variant'
                    type: array
            required:
                - id
                - slug
                - name
                - description
                - status
                - condition
                - price_mode
                - currency
                - specifications
                - images
                - category
                - tags
                - variants
                - sold
                - rating
                - review_count
                - seller
                - favorited
                - favorite_count
                - created_at
            type: object
        ListingID:
            example: lst_5y5w68r4918v8
            pattern: ^lst_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ListingPage:
            description: |
                One page shape for every listing query, ranked or not. Page-paginated, because a catalog is browsed with a pager and a relevance order has no stored column to seek into; `total` is null for a ranked query, where the top-K is all the search ever visited.
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Listing'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        ListingStatus:
            description: |
                Lifecycle and moderation in one column. `hidden` is a live listing the seller took down for now; deletion is a separate flag entirely.
            enum:
                - draft
                - pending
                - active
                - hidden
            type: string
        LoginRequest:
            properties:
                identifier:
                    description: An email or a phone or a username.
                    type: string
                password:
                    type: string
            required:
                - identifier
                - password
            type: object
        LogoutRequest:
            properties:
                device_id:
                    $ref: '#/components/schemas/DeviceID'
            type: object
        MarkConversationReadRequest:
            properties:
                before:
                    description: |
                        Mark everything sent at or before this instant read. Omit to mark the whole thread read.
                    format: date-time
                    type: string
            type: object
        MarkNotificationsReadRequest:
            properties:
                before:
                    description: |
                        Mark everything created at or before this instant read. Omit to mark the whole feed read.
                    format: date-time
                    type: string
            type: object
        Me:
            description: The caller's own view. Everything here is private to them.
            properties:
                created_at:
                    format: date-time
                    type: string
                email:
                    format: email
                    nullable: true
                    type: string
                email_verified:
                    type: boolean
                has_password:
                    description: False on a provider-only account. Unlinking the last provider is refused when this is false.
                    type: boolean
                id:
                    $ref: '#/components/schemas/AccountID'
                identity_verified:
                    description: Whether a live verified identity document exists. The payout gate reads this.
                    type: boolean
                phone:
                    nullable: true
                    type: string
                profile:
                    $ref: '#/components/schemas/Profile'
                role:
                    $ref: '#/components/schemas/AccountRole'
                status:
                    $ref: '#/components/schemas/AccountStatus'
                username:
                    nullable: true
                    type: string
            required:
                - id
                - email_verified
                - role
                - status
                - has_password
                - identity_verified
                - profile
                - created_at
            type: object
        Message:
            properties:
                attachments:
                    description: Empty on a redacted message.
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                body:
                    description: Empty on a redacted message.
                    type: string
                card:
                    additionalProperties: true
                    description: |
                        What a system message renders, and for a price negotiation that is `{"offer_id": "ofr_…"}` and nothing else — the terms are read from the offer, so a counter-offer cannot leave an old price on screen. Written only by the backend and rejected on a `user` message, otherwise anyone could send a card that looks like an offer the seller accepted.
                    nullable: true
                    type: object
                conversation_id:
                    $ref: '#/components/schemas/ConversationID'
                created_at:
                    format: date-time
                    type: string
                deleted_at:
                    description: Set on a redacted message. The row stays so a thread has no unexplained gaps.
                    format: date-time
                    nullable: true
                    type: string
                edited_at:
                    format: date-time
                    nullable: true
                    type: string
                id:
                    $ref: '#/components/schemas/MessageID'
                refs:
                    additionalProperties: true
                    description: |
                        What the sender pointed at — a listing, a variant, an order. Client-supplied and validated against the caller's own access to each id, so it can carry a reference but never assert anything about it.
                    type: object
                sender_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: Null on a system message.
                    nullable: true
                type:
                    $ref: '#/components/schemas/MessageType'
            required:
                - id
                - conversation_id
                - type
                - body
                - attachments
                - refs
                - created_at
            type: object
        MessageID:
            example: msg_agnc1pe4pjb4k
            pattern: ^msg_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        MessagePage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Message'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        MessageType:
            description: |
                A `system` message is produced by the backend — an offer was accepted, an order shipped — and belongs to no participant, which is why it has no sender.
            enum:
                - user
                - system
            type: string
        ModerationNoteRequest:
            properties:
                note:
                    maxLength: 2000
                    type: string
            type: object
        Notification:
            properties:
                category:
                    $ref: '#/components/schemas/NotificationCategory'
                created_at:
                    description: |
                        Identifies the row together with the feed order, and it is what `POST /notifications/read` is given a bound against.
                    format: date-time
                    type: string
                payload:
                    additionalProperties: true
                    description: Structured content such as deep links and images.
                    type: object
                read_at:
                    format: date-time
                    nullable: true
                    type: string
                title:
                    type: string
            required:
                - category
                - title
                - payload
                - created_at
            type: object
        NotificationCategory:
            description: Also the preference key, which is why it is an enum — a typo would read as no preference and quietly send anyway.
            enum:
                - order
                - promotion
                - system
                - chat
                - social
            type: string
        NotificationChannel:
            enum:
                - in-app
                - push
                - email
                - sms
            type: string
        NotificationPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Notification'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        NotificationPreference:
            properties:
                category:
                    $ref: '#/components/schemas/NotificationCategory'
                channel:
                    $ref: '#/components/schemas/NotificationChannel'
                is_default:
                    description: |
                        True when no stored row exists and this is the domain default. Shown so a client can tell an explicit choice from an inherited one.
                    type: boolean
                is_enabled:
                    type: boolean
            required:
                - category
                - channel
                - is_enabled
                - is_default
            type: object
        NotificationPreferenceList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/NotificationPreference'
                    type: array
            required:
                - data
            type: object
        OAuthIdentity:
            properties:
                created_at:
                    format: date-time
                    type: string
                provider:
                    $ref: '#/components/schemas/OAuthProvider'
            required:
                - provider
                - created_at
            type: object
        OAuthIdentityList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/OAuthIdentity'
                    type: array
            required:
                - data
            type: object
        OAuthLoginRequest:
            properties:
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                credential:
                    description: The provider's authorization code or id token.
                    type: string
                locale:
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                provider:
                    $ref: '#/components/schemas/OAuthProvider'
                timezone:
                    maxLength: 64
                    type: string
            required:
                - provider
                - credential
            type: object
        OAuthProvider:
            example: google
            pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
            type: string
        Offer:
            properties:
                author_id:
                    $ref: '#/components/schemas/AccountID'
                buyer_id:
                    $ref: '#/components/schemas/AccountID'
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                expires_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/OfferID'
                listing_id:
                    allOf:
                        - $ref: '#/components/schemas/ListingID'
                    description: The listing whose card the offer is shown on.
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
                reason:
                    description: Shown on the offer card beside the proposed price
                    type: string
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                status:
                    $ref: '#/components/schemas/OfferStatus'
                total:
                    description: Currently proposed total, smallest currency unit
                    example: 598000
                    format: int64
                    minimum: 1
                    type: integer
                variant_id:
                    $ref: '#/components/schemas/VariantID'
            required:
                - id
                - listing_id
                - variant_id
                - buyer_id
                - seller_id
                - author_id
                - status
                - quantity
                - total
                - currency
                - created_at
                - expires_at
            type: object
        OfferID:
            example: ofr_ptz3n8kvq1wd6
            pattern: ^ofr_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        OfferPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Offer'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        OfferStatus:
            description: |
                `active` — a proposal waiting for the other side. `accepted` — agreed, the price frozen for a short window while the buyer creates the order. `checked-out` — the buyer's payment session is open, so these terms are off the table. `cancelled` — withdrawn or lapsed.
            enum:
                - active
                - accepted
                - checked-out
                - cancelled
            example: accepted
            type: string
        OpenDisputeRequest:
            description: |
                The grounds for the round being opened — the buyer's objection to a refusal, or the seller's account of what actually arrived. Supporting files go on the case through `POST /refunds/{id}/attachments`, which is where all of a party's evidence lives.
            properties:
                reason:
                    example: The photos show a different item
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - reason
            type: object
        Order:
            properties:
                address:
                    $ref: '#/components/schemas/OrderAddressSnapshot'
                buyer:
                    $ref: '#/components/schemas/AccountSummary'
                cancelled_at:
                    format: date-time
                    nullable: true
                    type: string
                completed_at:
                    description: Set when the payout is claimed
                    format: date-time
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                draft_id:
                    allOf:
                        - $ref: '#/components/schemas/DraftOrderID'
                    description: |
                        The checkout this came from. Exactly one of `draft_id` and `offer_id` is set: a fixed-price sale is checked out from a draft, a negotiated one from the offer both sides accepted.
                    nullable: true
                id:
                    $ref: '#/components/schemas/OrderID'
                items:
                    items:
                        $ref: '#/components/schemas/OrderItem'
                    type: array
                offer_id:
                    allOf:
                        - $ref: '#/components/schemas/OfferID'
                    description: The accepted negotiation this came from. Null on a fixed-price sale.
                    nullable: true
                payout_deadline_at:
                    description: |
                        `received_at` + 72h, computed rather than stored. The seller is paid out at this point unless a refund still has a claim on the escrow.
                    format: date-time
                    nullable: true
                    type: string
                payout_released_at:
                    description: |
                        When the escrow reached the seller. Null on a completed order means the release has not landed yet — the platform owes the seller and knows it, and a retry pass keeps trying until it does.
                    format: date-time
                    nullable: true
                    type: string
                pickup_address:
                    $ref: '#/components/schemas/OrderAddressSnapshot'
                receipt_attachments:
                    description: |
                        Unboxing evidence, captured with the receipt confirmation and never added to afterwards — a refund is judged on what the buyer showed at the moment of unboxing.
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                received_at:
                    description: When the buyer confirmed receipt. Null means the payout clock has not started.
                    format: date-time
                    nullable: true
                    type: string
                seller:
                    $ref: '#/components/schemas/AccountSummary'
                state:
                    $ref: '#/components/schemas/OrderState'
                total:
                    description: |
                        Summed from the live lines rather than stored: the lines are the record of what was paid, and a column would be a second answer to the same question.
                    example: 598000
                    format: int64
                    minimum: 0
                    type: integer
                transport:
                    allOf:
                        - $ref: '#/components/schemas/Transport'
                    description: |
                        The shipment, carried in full rather than as an id: it has no read of its own outside the order it belongs to.
            required:
                - id
                - buyer
                - seller
                - address
                - pickup_address
                - state
                - total
                - currency
                - receipt_attachments
                - created_at
            type: object
        OrderAddressSnapshot:
            description: |
                A contact copied into the row, not a pointer to one. The administrative codes are what a carrier is called with, and the saved contact may have changed since the order was placed.
            properties:
                address_detail:
                    description: The street line and anything below it
                    example: Apt 4B, 12 Nguyen Hue
                    nullable: true
                    type: string
                country:
                    description: ISO 3166-1 alpha-2
                    example: VN
                    maxLength: 2
                    minLength: 2
                    type: string
                district_code:
                    description: Null where the country has no district tier
                    example: "760"
                    nullable: true
                    type: string
                full_name:
                    example: Nguyen Van A
                    type: string
                phone:
                    description: E.164
                    example: "+84900000001"
                    type: string
                province_code:
                    example: "79"
                    type: string
                ward_code:
                    example: "26743"
                    type: string
            required:
                - full_name
                - phone
                - country
            type: object
        OrderFeedback:
            description: |
                Both directions on one order, as far as the caller is allowed to see them.
            properties:
                mine:
                    allOf:
                        - $ref: '#/components/schemas/Feedback'
                    nullable: true
                reveal_at:
                    description: |
                        When the blind window closes and whatever has been submitted becomes visible, whether or not both sides rated. Null once both are published. Exposed for the same reason the order module exposes its deadlines: a client that cannot count down has to guess.
                    format: date-time
                    nullable: true
                    type: string
                theirs:
                    allOf:
                        - $ref: '#/components/schemas/Feedback'
                    description: Present only once published.
                    nullable: true
                theirs_submitted:
                    description: |
                        Whether the counterparty has rated at all. Shown while their rating is still blind so the caller knows a reveal is coming.
                    type: boolean
            required:
                - theirs_submitted
            type: object
        OrderID:
            example: ord_fv2cpg50vkrfp
            pattern: ^ord_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        OrderItem:
            description: |
                One purchased line. It exists from checkout, before the money lands: that window is the whole of what a null `order_id` means.
            properties:
                cancelled_at:
                    format: date-time
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                id:
                    $ref: '#/components/schemas/OrderItemID'
                listing_id:
                    allOf:
                        - $ref: '#/components/schemas/ListingID'
                    description: |
                        The listing the variant belongs to, denormalized the same way `seller_id` is, so order history can resolve it through `GET /listings?ids=` — which answers for a listing the seller has since hidden or deleted.
                note:
                    description: Buyer note, per checkout
                    example: Please leave at reception
                    type: string
                order_id:
                    allOf:
                        - $ref: '#/components/schemas/OrderID'
                    description: |
                        Null until the payment session completes and the webhook writes the order. Nobody confirms it — a seller can refuse a price, never a sale.
                    nullable: true
                payment_session_id:
                    allOf:
                        - $ref: '#/components/schemas/PaymentSessionID'
                    description: The single session covering every line of this checkout.
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
                seller_id:
                    $ref: '#/components/schemas/AccountID'
                total_amount:
                    description: Paid amount in the smallest unit of `currency`
                    example: 598000
                    format: int64
                    minimum: 0
                    type: integer
                transport_option:
                    description: An enabled transport option's slug
                    example: ghn-express
                    type: string
                variant_id:
                    $ref: '#/components/schemas/VariantID'
            required:
                - id
                - listing_id
                - variant_id
                - seller_id
                - quantity
                - currency
                - total_amount
                - transport_option
                - payment_session_id
                - created_at
            type: object
        OrderItemID:
            example: itm_q1wd6ptz3n8kv
            pattern: ^itm_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        OrderItemPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/OrderItem'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        OrderPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Order'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        OrderState:
            description: |
                Derived from the two outcome timestamps: `open` means neither is set, which is also the predicate of the partial indexes behind this filter.
            enum:
                - open
                - completed
                - cancelled
            type: string
        PageMeta:
            description: Where this page sits in the collection, for a page-paginated read.
            properties:
                limit:
                    example: 20
                    maximum: 100
                    minimum: 1
                    type: integer
                page:
                    description: The 1-based page this is.
                    example: 1
                    minimum: 1
                    type: integer
                total_count:
                    description: |
                        Rows matching the filter, so a client can draw a pager. Explicitly null — not absent — when the result is a ranked top-K: an approximate nearest-neighbour search never visits the rows it did not return, so there is no total to report, and a client has to render "more results" rather than "page 3 of 12".

                        Named `total_count` rather than `total` because `total` is already a money field in this document (an offer's proposed total), and a generated client would end up with two `total`s meaning different things.
                    example: 137
                    format: int64
                    minimum: 0
                    nullable: true
                    type: integer
            required:
                - page
                - limit
                - total_count
            type: object
        PasswordResetConfirmRequest:
            properties:
                new_password:
                    maxLength: 72
                    minLength: 8
                    type: string
                token:
                    type: string
            required:
                - token
                - new_password
            type: object
        PasswordResetRequest:
            properties:
                identifier:
                    description: An email or a phone.
                    type: string
            required:
                - identifier
            type: object
        PaymentSession:
            properties:
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                expired_at:
                    description: A session still unsettled past this point is voided by a job.
                    format: date-time
                    type: string
                from_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: The payer. Null means the platform itself.
                    nullable: true
                id:
                    $ref: '#/components/schemas/PaymentSessionID'
                kind:
                    $ref: '#/components/schemas/PaymentSessionKind'
                note:
                    type: string
                outstanding:
                    description: |
                        The total less what has already settled on a rail: what a further payment may still tender. Computed, because a stored copy would be a second fact to keep in step with every leg.
                    example: 299000
                    format: int64
                    minimum: 0
                    type: integer
                paid_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                to_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: The payee. Null means the platform itself.
                    nullable: true
                total_amount:
                    description: Amount owed, smallest unit of `currency`
                    example: 598000
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - id
                - kind
                - status
                - currency
                - total_amount
                - outstanding
                - created_at
                - expired_at
            type: object
        PaymentSessionID:
            example: pay_7bq2xn4tw9yef
            pattern: ^pay_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        PaymentSessionKind:
            description: |
                What the money is for; it is what routes the flow. A client cannot pick this — the module that owes the money does.
            enum:
                - buyer-checkout
                - seller-payout
                - withdrawal
            type: string
        PaymentSessionPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/PaymentSession'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        PaymentSessionStatus:
            enum:
                - pending
                - processing
                - success
                - cancelled
                - failed
            type: string
        PendingEdit:
            description: |
                The editable subset of a listing, held until moderation applies it. Same fields as UpdateListingRequest and only those: an edit that could carry anything would be a blob nobody can diff or review.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                category_id:
                    $ref: '#/components/schemas/CategoryID'
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                description:
                    maxLength: 20000
                    type: string
                name:
                    example: Áo thun Uniqlo cổ tròn xanh navy
                    maxLength: 200
                    minLength: 1
                    type: string
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                specifications:
                    additionalProperties: true
                    type: object
                tags:
                    items:
                        $ref: '#/components/schemas/TagSlug'
                    type: array
            type: object
        PriceMode:
            description: |
                How a sale on this listing starts, and it is the only thing that differs between the two.

                `fixed` is bought straight from the listing page: the buyer checks out, pays the item plus the shipping quote, and the order and its shipment exist as soon as the money lands. The seller confirms nothing.

                `negotiable` cannot be checked out. The buyer opens a negotiation, which is a thread in chat where either side revises the terms, and accepting them is what starts the same checkout. Either way the seller never approves an order — the only thing they can refuse is a price.
            enum:
                - fixed
                - negotiable
            type: string
        Profile:
            properties:
                avatar:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                country:
                    maxLength: 2
                    minLength: 2
                    type: string
                created_at:
                    format: date-time
                    type: string
                date_of_birth:
                    format: date
                    nullable: true
                    type: string
                description:
                    nullable: true
                    type: string
                gender:
                    allOf:
                        - $ref: '#/components/schemas/ProfileGender'
                    nullable: true
                locale:
                    example: vi-VN
                    type: string
                name:
                    example: Nguyễn Văn An
                    type: string
                timezone:
                    example: Asia/Ho_Chi_Minh
                    type: string
            required:
                - name
                - country
                - locale
                - timezone
                - created_at
            type: object
        ProfileGender:
            enum:
                - male
                - female
                - other
            type: string
        PublicAccount:
            description: |
                What anyone may see about an account. The seller page: no email, no phone, no birth date, no addresses.
            properties:
                avatar:
                    allOf:
                        - $ref: '#/components/schemas/Resource'
                    nullable: true
                created_at:
                    description: Member since.
                    format: date-time
                    type: string
                description:
                    nullable: true
                    type: string
                follower_count:
                    example: 1240
                    format: int64
                    minimum: 0
                    type: integer
                id:
                    $ref: '#/components/schemas/AccountID'
                identity_verified:
                    description: Shown as a trust signal.
                    type: boolean
                name:
                    example: Nguyễn Văn An
                    type: string
            required:
                - id
                - name
                - identity_verified
                - follower_count
                - created_at
            type: object
        PutTagRequest:
            description: The slug is in the path, so this carries only what can change.
            properties:
                description:
                    example: Made by hand, one at a time.
                    maxLength: 255
                    type: string
            type: object
        RefreshRequest:
            properties:
                refresh_token:
                    type: string
            required:
                - refresh_token
            type: object
        Refund:
            description: |
                The case. Always for one entire order, so it carries no amount of its own — what is owed is the order's checkout total.
            properties:
                attachments:
                    description: The buyer's evidence. Each dispute round holds its opener's.
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                buyer_id:
                    $ref: '#/components/schemas/AccountID'
                created_at:
                    format: date-time
                    type: string
                deadline_at:
                    description: |
                        When the party named by `status` runs out of time, and missing it is itself a move. Null while the case waits on a moderator or a carrier, and in the terminal states.
                    format: date-time
                    nullable: true
                    type: string
                id:
                    $ref: '#/components/schemas/RefundID'
                order_id:
                    $ref: '#/components/schemas/OrderID'
                reason:
                    example: Not as described
                    type: string
                rejection_reason:
                    description: |
                        The seller's grounds for refusing. Null in `awaiting-buyer-action` when the seller never answered at all, which is how the two paths into that state are told apart.
                    nullable: true
                    type: string
                returned_at:
                    description: When the return reached the seller and the inspection window opened.
                    format: date-time
                    nullable: true
                    type: string
                seller_decided_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/RefundStatus'
            required:
                - id
                - order_id
                - buyer_id
                - status
                - reason
                - attachments
                - created_at
            type: object
        RefundDispute:
            description: |
                One round of moderation on one refund. A second round rather than a second verdict on the first, so ruling for the buyer and then for the seller stays legible as two decisions instead of one that changed its mind.
            properties:
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/RefundDisputeID'
                note:
                    description: The moderator's, recorded with the verdict
                    example: Evidence is clear
                    type: string
                opened_by:
                    $ref: '#/components/schemas/AccountID'
                reason:
                    description: The grounds of whoever opened this round.
                    example: The photos show a different item
                    type: string
                refund_id:
                    $ref: '#/components/schemas/RefundID'
                round:
                    description: 1 = the buyer escalating, 2 = the seller appealing the return.
                    enum:
                        - 1
                        - 2
                    type: integer
                ruled_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/DisputeStatus'
            required:
                - id
                - refund_id
                - round
                - opened_by
                - status
                - reason
                - created_at
            type: object
        RefundDisputeID:
            example: dsp_n8kvq1wd6ptz3
            pattern: ^dsp_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        RefundDisputePage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/RefundDispute'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        RefundID:
            example: rfd_z3n8kvq1wd6pt
            pattern: ^rfd_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        RefundPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Refund'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        RefundStatus:
            description: |
                Every live value names the party whose move the refund is waiting on, and each of those carries a `deadline_at` that the party can miss:

                `awaiting-seller-review` — the seller accepts or rejects. Missing it hands the case to the buyer. `awaiting-buyer-action` — the buyer escalates to a moderator or lets the refund lapse; `rejection_reason` says whether the seller refused or just went quiet. `disputed` — a moderator has an open round. `returning` — the goods are on their way back; only a granted refund ever reaches here. `returned` — the seller inspects and may appeal until the window closes.

                Then three terminals: `accepted` is money back to the buyer and the order closed with it, `rejected` is no refund and the payout stands, `cancelled` is the buyer withdrawing before the seller decided. Only the last two give the escrow up — `accepted` keeps its claim, because that money has already gone to the buyer.
            enum:
                - awaiting-seller-review
                - awaiting-buyer-action
                - disputed
                - returning
                - returned
                - accepted
                - rejected
                - cancelled
            type: string
        RegisterDeviceRequest:
            properties:
                platform:
                    $ref: '#/components/schemas/DevicePlatform'
                push_token:
                    maxLength: 4096
                    minLength: 1
                    type: string
            required:
                - platform
                - push_token
            type: object
        RegisterRequest:
            description: |
                At least one of email, phone or username is required. The profile fields are part of the same request because the account and its profile are created together.
            properties:
                country:
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                email:
                    format: email
                    maxLength: 255
                    type: string
                locale:
                    example: vi-VN
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                name:
                    description: Display name.
                    maxLength: 100
                    minLength: 1
                    type: string
                password:
                    maxLength: 72
                    minLength: 8
                    type: string
                phone:
                    description: E.164.
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                timezone:
                    example: Asia/Ho_Chi_Minh
                    maxLength: 64
                    type: string
                username:
                    maxLength: 100
                    minLength: 3
                    pattern: ^[a-z0-9._-]+$
                    type: string
            required:
                - password
                - name
                - country
                - locale
                - timezone
            type: object
        RejectRefundRequest:
            description: |
                A reason is mandatory: it is what the buyer decides whether to escalate on. Counter-evidence is not required here, because a rejection is not yet a case — the seller supplies it if the buyer escalates.
            properties:
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - reason
            type: object
        Report:
            properties:
                action_taken:
                    allOf:
                        - $ref: '#/components/schemas/ReportAction'
                    description: Null until the report is resolved.
                    nullable: true
                created_at:
                    format: date-time
                    type: string
                detail:
                    type: string
                id:
                    $ref: '#/components/schemas/ReportID'
                reason:
                    $ref: '#/components/schemas/ReportReason'
                ref_id:
                    description: |
                        Opaque id of the reported thing. Which prefix is valid follows from `ref_type`, which is why the two are validated together.
                    example: lst_1ryaj8117v2p4
                    pattern: ^[a-z]{3}_[0-9a-hjkmnp-tv-z]{13}$
                    type: string
                ref_type:
                    $ref: '#/components/schemas/ReportRefType'
                resolution_note:
                    nullable: true
                    type: string
                resolved_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/ReportStatus'
            required:
                - id
                - ref_type
                - ref_id
                - reason
                - detail
                - status
                - created_at
            type: object
        ReportAction:
            description: What a moderator did about an upheld report. `none` goes with a dismissal.
            enum:
                - none
                - listing-removed
                - message-removed
                - account-suspended
                - warning
            type: string
        ReportID:
            example: rpt_9c4vt6bkn1pzs
            pattern: ^rpt_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ReportPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Report'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        ReportReason:
            enum:
                - scam
                - counterfeit
                - prohibited
                - harassment
                - spam
                - inappropriate
                - other
            type: string
        ReportRefType:
            enum:
                - listing
                - account
                - message
                - review
                - review-reply
            type: string
        ReportStatus:
            enum:
                - open
                - reviewing
                - actioned
                - dismissed
            type: string
        Reputation:
            description: |
                Recomputed, never written directly. Transaction ratings and product-review ratings stay apart: one order can produce both and summing them would count it twice.
            properties:
                account_id:
                    $ref: '#/components/schemas/AccountID'
                cancelled_orders:
                    example: 3
                    format: int64
                    minimum: 0
                    type: integer
                completed_orders:
                    example: 152
                    format: int64
                    minimum: 0
                    type: integer
                rating_average:
                    description: Average transaction feedback from 1 to 5. Zero when there is none.
                    example: 4.6
                    format: double
                    maximum: 5
                    minimum: 0
                    type: number
                rating_count:
                    example: 87
                    format: int64
                    minimum: 0
                    type: integer
                review_rating_average:
                    description: Average product-review rating. Always zero for the buyer role.
                    example: 4.6
                    format: double
                    maximum: 5
                    minimum: 0
                    type: number
                review_rating_count:
                    example: 64
                    format: int64
                    minimum: 0
                    type: integer
                role:
                    $ref: '#/components/schemas/ReputationRole'
                updated_at:
                    description: When it was last recomputed.
                    format: date-time
                    type: string
            required:
                - account_id
                - role
                - rating_average
                - rating_count
                - review_rating_average
                - review_rating_count
                - completed_orders
                - cancelled_orders
                - updated_at
            type: object
        ReputationRole:
            enum:
                - seller
                - buyer
            type: string
        ResolveReportRequest:
            properties:
                action_taken:
                    allOf:
                        - $ref: '#/components/schemas/ReportAction'
                    description: |
                        Required. `none` goes with a dismissal; upholding the report names what was done.
                note:
                    maxLength: 2000
                    type: string
                status:
                    description: A verdict so open and reviewing are not choices.
                    enum:
                        - actioned
                        - dismissed
                    type: string
            required:
                - status
                - action_taken
            type: object
        Resource:
            properties:
                checksum:
                    description: Content hash, read back from storage at completion.
                    example: 9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08
                    type: string
                id:
                    $ref: '#/components/schemas/ResourceID'
                mime:
                    example: image/jpeg
                    type: string
                object_key:
                    description: Provider-specific path to the bytes.
                    example: listings/2026/07/9f86d081.jpg
                    type: string
                provider:
                    description: Storage backend, kebab-case — `s3`, `minio`, `local`.
                    example: minio
                    type: string
                size:
                    description: Bytes, as read back from storage rather than as declared.
                    example: 248132
                    format: int64
                    minimum: 0
                    type: integer
                url:
                    description: |
                        Short-lived URL to fetch the bytes. Absent until a module can presign one, so a consumer that needs the bytes has to treat it as "not available yet" rather than as an empty object. Not a stable address: store the id, not this.
                    example: https://cdn.shopnexus.test/o/9f86d081?expires=1785000000
                    format: uri
                    type: string
                url_expires_at:
                    format: date-time
                    type: string
            required:
                - id
                - provider
                - object_key
                - mime
                - size
            type: object
        ResourceID:
            example: res_4mx8jd2qr5wtc
            pattern: ^res_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        Review:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                author:
                    $ref: '#/components/schemas/AccountSummary'
                body:
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ReviewID'
                listing_id:
                    $ref: '#/components/schemas/ListingID'
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
                replies:
                    description: |
                        The first few, oldest first. Replies are unlimited, so a page of reviews cannot carry all of them — `reply_count` says how many there are and `GET /reviews/{id}` returns the rest.
                    items:
                        $ref: '#/components/schemas/ReviewReply'
                    maxItems: 3
                    type: array
                reply_count:
                    example: 2
                    format: int64
                    minimum: 0
                    type: integer
                updated_at:
                    description: |
                        Null until the author edits it. A review rewritten after the seller answered it should say so, and the reply thread cannot say it on its own.
                    format: date-time
                    nullable: true
                    type: string
                votes:
                    $ref: '#/components/schemas/ReviewVoteTally'
            required:
                - id
                - listing_id
                - author
                - rating
                - body
                - attachments
                - replies
                - reply_count
                - votes
                - created_at
            type: object
        ReviewID:
            example: rvw_qk4mfx7bd32h9
            pattern: ^rvw_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ReviewPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Review'
                    type: array
                meta:
                    $ref: '#/components/schemas/CursorMeta'
            required:
                - data
                - meta
            type: object
        ReviewReply:
            properties:
                author:
                    $ref: '#/components/schemas/AccountSummary'
                body:
                    type: string
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/ReviewReplyID'
                is_seller:
                    description: Whether the author owns the listing.
                    type: boolean
            required:
                - id
                - author
                - is_seller
                - body
                - created_at
            type: object
        ReviewReplyID:
            example: rpl_mfx7bd32h9qk4
            pattern: ^rpl_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        ReviewVoteTally:
            description: |
                Counters on the review, not a count of `review_vote` rows per request — that is what lets `sort=helpful` be an index scan. Flipping a vote moves one unit between the two totals, so both sides have to change in the same transaction as the vote row itself; a tally that has drifted from the rows it summarises is recomputable, but a half-applied flip is visible.
            properties:
                helpful:
                    example: 12
                    format: int64
                    minimum: 0
                    type: integer
                my_vote:
                    description: The caller's own vote. Null when they have not voted.
                    enum:
                        - -1
                        - 1
                    nullable: true
                    type: integer
                not_helpful:
                    example: 1
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - helpful
                - not_helpful
            type: object
        SemanticSeed:
            description: |
                A tag slug or a category id, told apart the same way a listing ref is: an opaque id always carries an underscore after its prefix and a slug never does.

                Both are legal seeds for the same query because every embedding column in this schema is `vector(1024)` indexed with `vector_cosine_ops` — one BGE-M3 space — so the distance between a category and a tag means the same thing as the distance between two tags. A seed whose vector the cron has not computed yet is rejected rather than silently ignored, since dropping it would answer a different question than the one asked.
            oneOf:
                - $ref: '#/components/schemas/TagSlug'
                - $ref: '#/components/schemas/CategoryID'
        SendMessageRequest:
            description: A body or at least one attachment is required.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    maxItems: 10
                    type: array
                body:
                    maxLength: 4000
                    type: string
                refs:
                    additionalProperties: true
                    description: |
                        References to point at, each checked against the caller's access to it. There is no way to set `card` from here: a system payload is the backend's to write.
                    type: object
            type: object
        ShippingQuote:
            properties:
                fee:
                    description: What the buyer would pay this carrier. Re-quoted at checkout, so it is an estimate a client renders rather than a price it can hold.
                    example: 15000
                    format: int64
                    minimum: 0
                    type: integer
                name:
                    example: GHN Express
                    type: string
                option:
                    example: ghn-express
                    type: string
            required:
                - option
                - name
                - fee
            type: object
        ShippingQuotes:
            properties:
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                options:
                    items:
                        $ref: '#/components/schemas/ShippingQuote'
                    type: array
            required:
                - options
                - currency
            type: object
        ShippingQuotesRequest:
            description: |
                Exactly one of `draft_id` or `offer_id` — they are the two things that freeze a price, and the parcel is whichever of them is about to be checked out.
            properties:
                contact_id:
                    $ref: '#/components/schemas/ContactID'
                draft_id:
                    $ref: '#/components/schemas/DraftOrderID'
                lines:
                    description: |
                        The draft's variants and quantities, as a checkout would send them. Ignored for an offer, whose quantity was negotiated.
                    items:
                        $ref: '#/components/schemas/CheckoutLine'
                    maxItems: 50
                    type: array
                offer_id:
                    $ref: '#/components/schemas/OfferID'
            required:
                - contact_id
            type: object
        SimilarityScore:
            description: |
                Relevance, always oriented so that higher is closer — for a vector ranking that is `1 - (a <=> b)`, the cosine similarity, because the operator itself returns a distance. Null unless the request asked for a ranking.
            example: 0.87
            format: double
            maximum: 1
            minimum: -1
            nullable: true
            type: number
        StartConversationRequest:
            properties:
                account_id:
                    $ref: '#/components/schemas/AccountID'
            required:
                - account_id
            type: object
        StartIdentityVerificationRequest:
            description: |
                The scans are uploaded first through `POST /resources` and named here by id: a photo of a government ID does not belong in a JSON body, and the upload has its own retry story.
            properties:
                back_resource_id:
                    allOf:
                        - $ref: '#/components/schemas/ResourceID'
                    description: The reverse, for a document that has one. Omit for a passport.
                doc_type:
                    $ref: '#/components/schemas/IdentityDocumentType'
                front_resource_id:
                    allOf:
                        - $ref: '#/components/schemas/ResourceID'
                    description: The data page, or the front of the card.
                selfie_resource_id:
                    allOf:
                        - $ref: '#/components/schemas/ResourceID'
                    description: |
                        A live photo, matched against the portrait on the document. Required: without it the document is verified but nothing ties it to the person holding the account.
            required:
                - doc_type
                - front_resource_id
                - selfie_resource_id
            type: object
        StartPaymentRequest:
            properties:
                amount:
                    description: Omit to tender the whole outstanding balance. Pass it to split the session across rails.
                    example: 299000
                    format: int64
                    minimum: 1
                    type: integer
                payment_option:
                    description: A payment option slug from the common module.
                    maxLength: 100
                    pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
                    type: string
                return_url:
                    description: |
                        Where the gateway sends the payer back. Checked against the platform's own allowlist of hosts and rejected otherwise — a URL that reaches a redirect unvalidated is an open redirect wearing a payment flow.
                    maxLength: 2048
                    type: string
            required:
                - payment_option
            type: object
        Stock:
            description: |
                Always read inside its own variant, so it does not repeat the variant id.
            properties:
                available:
                    description: quantity − reserved − sold.
                    example: 39
                    format: int64
                    minimum: 0
                    type: integer
                quantity:
                    description: Total on hand.
                    example: 42
                    format: int64
                    minimum: 0
                    type: integer
                reserved:
                    description: |
                        Held by a checkout that has not completed. Moved by checkout and by cancellation, never set directly, and it comes back when a session expires.
                    example: 3
                    format: int64
                    minimum: 0
                    type: integer
                sold:
                    description: |
                        Completed sales. Only ever goes up, which is what makes it usable as social proof — a reservation counts here only once the purchase completes.
                    example: 128
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - quantity
                - reserved
                - sold
                - available
            type: object
        SubmitFeedbackRequest:
            description: |
                The direction and the ratee both follow from the order, so neither is sent.
            properties:
                comment:
                    maxLength: 2000
                    type: string
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            required:
                - rating
            type: object
        SubmitReportRequest:
            properties:
                detail:
                    maxLength: 2000
                    type: string
                reason:
                    $ref: '#/components/schemas/ReportReason'
                ref_id:
                    pattern: ^[a-z]{3}_[0-9a-hjkmnp-tv-z]{13}$
                    type: string
                ref_type:
                    $ref: '#/components/schemas/ReportRefType'
            required:
                - ref_type
                - ref_id
                - reason
            type: object
        SubmitReviewReplyRequest:
            properties:
                body:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - body
            type: object
        SubmitReviewRequest:
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    maxItems: 10
                    type: array
                body:
                    maxLength: 2000
                    type: string
                order_id:
                    $ref: '#/components/schemas/OrderID'
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            required:
                - order_id
                - rating
            type: object
        SuspendAccountRequest:
            properties:
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
                until:
                    description: Omit for a permanent suspension.
                    format: date-time
                    type: string
            required:
                - reason
            type: object
        Tag:
            properties:
                description:
                    example: Made by hand, one at a time.
                    nullable: true
                    type: string
                score:
                    $ref: '#/components/schemas/SimilarityScore'
                slug:
                    $ref: '#/components/schemas/TagSlug'
            required:
                - slug
            type: object
        TagPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Tag'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        TagSlug:
            description: A tag's id is its slug. A natural key, so it is never encoded.
            example: handmade
            maxLength: 100
            pattern: ^[a-z0-9]+(-[a-z0-9]+)*$
            type: string
        TakedownRequest:
            properties:
                notify_seller:
                    default: true
                    type: boolean
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - reason
            type: object
        TaxCodeType:
            enum:
                - individual
                - business
                - household
            type: string
        TaxInfo:
            properties:
                created_at:
                    format: date-time
                    type: string
                legal_name:
                    type: string
                tax_code:
                    example: "0123456789"
                    type: string
                tax_code_type:
                    $ref: '#/components/schemas/TaxCodeType'
                updated_at:
                    description: When the registration was last replaced, which also resets the verdict.
                    format: date-time
                    type: string
                verification_status:
                    $ref: '#/components/schemas/TaxVerificationStatus'
                verified_at:
                    format: date-time
                    nullable: true
                    type: string
            required:
                - tax_code
                - tax_code_type
                - legal_name
                - verification_status
                - created_at
                - updated_at
            type: object
        TaxVerificationRequest:
            properties:
                note:
                    maxLength: 2000
                    type: string
                source:
                    description: What the verdict was based on
                    maxLength: 200
                    type: string
                status:
                    description: A verdict, so `pending` is not a choice
                    enum:
                        - verified
                        - rejected
                    type: string
            required:
                - status
                - source
            type: object
        TaxVerificationStatus:
            enum:
                - pending
                - verified
                - rejected
            type: string
        Transaction:
            description: One movement on an external rail. Append-only.
            properties:
                amount:
                    description: Signed — positive is a charge, negative is a reversal.
                    example: 299000
                    format: int64
                    type: integer
                checkout_url:
                    description: Where to send the payer for this leg. Null once the rail no longer needs it, or for rails that have none.
                    nullable: true
                    type: string
                created_at:
                    format: date-time
                    type: string
                currency:
                    allOf:
                        - $ref: '#/components/schemas/CurrencyCode'
                    description: What the rail actually debits, which split tender may make differ from the session's currency.
                error:
                    nullable: true
                    type: string
                expired_at:
                    description: Gateway URL expiry
                    format: date-time
                    nullable: true
                    type: string
                id:
                    $ref: '#/components/schemas/TransactionID'
                note:
                    type: string
                payment_option:
                    description: A payment option slug owned by the common module
                    type: string
                reverses_id:
                    allOf:
                        - $ref: '#/components/schemas/TransactionID'
                    description: The leg this one reverses. Set on reversals, null on originals.
                    nullable: true
                session_id:
                    $ref: '#/components/schemas/PaymentSessionID'
                settled_at:
                    format: date-time
                    nullable: true
                    type: string
                status:
                    $ref: '#/components/schemas/TransactionStatus'
            required:
                - id
                - session_id
                - status
                - payment_option
                - amount
                - currency
                - note
                - created_at
            type: object
        TransactionID:
            example: txn_5wtc4mx8jd2qr
            pattern: ^txn_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        TransactionList:
            description: Unpaginated — a session has as many legs as rails it was split across, which is a handful.
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Transaction'
                    type: array
            required:
                - data
            type: object
        TransactionStatus:
            description: A leg goes pending → success or failed, and success is terminal. Deliberately narrower than a session's status.
            enum:
                - pending
                - success
                - failed
            type: string
        Transport:
            description: |
                One shipment as the platform knows it. The carrier's own view — tracking number, label, raw webhook payloads — is held in the row but not published: it is provider-shaped, and nothing a client renders depends on it.
            properties:
                created_at:
                    format: date-time
                    type: string
                fee:
                    description: |
                        What the buyer paid for delivery, quoted from this carrier when the checkout opened. The buyer bears carriage on both a fixed-price and a negotiated sale, so a seller is never charged for it.
                    example: 15000
                    format: int64
                    minimum: 0
                    type: integer
                id:
                    $ref: '#/components/schemas/TransportID'
                option:
                    description: An enabled transport option's slug
                    example: ghn-express
                    type: string
                status:
                    $ref: '#/components/schemas/TransportStatus'
            required:
                - id
                - option
                - status
                - fee
                - created_at
            type: object
        TransportCheckpoint:
            description: |
                A position a checkpoint may report, so `pending` is not one of them — that is where a shipment starts, not somewhere it can be moved to. The last three are outcomes and are reachable from anywhere: a parcel can fail before it is ever collected.
            enum:
                - picked-up
                - in-transit
                - delivered
                - returned
                - failed
            type: string
        TransportCheckpointRequest:
            description: One carrier-reported position. The same shape for the outbound leg and for the leg carrying a refund's goods back.
            properties:
                status:
                    $ref: '#/components/schemas/TransportCheckpoint'
            required:
                - status
            type: object
        TransportID:
            example: trp_d6ptz3n8kvq1w
            pattern: ^trp_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        TransportStatus:
            description: Where the shipment is. `pending` is created but not yet handed over.
            enum:
                - pending
                - picked-up
                - in-transit
                - delivered
                - returned
                - failed
                - cancelled
            type: string
        UnreadCount:
            properties:
                unread:
                    example: 3
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - unread
            type: object
        UpdateAccountRequest:
            description: Every field optional. Omit one to leave it alone, send a value to replace it, send its `clear_*` flag to remove it — which is refused when it is the last identifier (422 `no_identifier`).
            properties:
                clear_email:
                    example: false
                    type: boolean
                clear_phone:
                    example: false
                    type: boolean
                clear_username:
                    example: false
                    type: boolean
                email:
                    example: alice@example.com
                    format: email
                    maxLength: 255
                    type: string
                phone:
                    example: "+84901234567"
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                username:
                    example: alice
                    maxLength: 100
                    minLength: 3
                    pattern: ^[a-z0-9._-]+$
                    type: string
            type: object
        UpdateBankAccountRequest:
            properties:
                is_default:
                    description: The only mutable field
                    enum:
                        - true
                    type: boolean
            required:
                - is_default
            type: object
        UpdateCartItemRequest:
            properties:
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
            required:
                - quantity
            type: object
        UpdateCategoryRequest:
            description: |
                Every field optional. Omit a field to leave it, send a value to replace it, send `clear_parent_id` to make the category a root.
            properties:
                clear_parent_id:
                    description: Promotes the category to a root.
                    example: false
                    type: boolean
                description:
                    example: Hand-thrown and slipcast pottery.
                    maxLength: 2000
                    type: string
                name:
                    example: Ceramics
                    maxLength: 100
                    minLength: 1
                    type: string
                parent_id:
                    $ref: '#/components/schemas/CategoryID'
            type: object
        UpdateContactRequest:
            description: Every field optional. Changing `phone` clears `phone_verified`. The `clear_*` flags remove a nullable field; the district pair and the coordinate travel together, so they clear together.
            properties:
                address:
                    example: 1 Lê Lợi
                    maxLength: 255
                    type: string
                address_detail:
                    example: Tầng 3
                    maxLength: 255
                    type: string
                address_type:
                    $ref: '#/components/schemas/ContactAddressType'
                clear_address_detail:
                    example: false
                    type: boolean
                clear_district:
                    example: false
                    type: boolean
                clear_location:
                    example: false
                    type: boolean
                clear_postal_code:
                    example: false
                    type: boolean
                country:
                    example: VN
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                district_code:
                    example: "760"
                    maxLength: 20
                    type: string
                district_name:
                    example: Quận 1
                    maxLength: 100
                    type: string
                full_name:
                    example: Nguyễn An
                    maxLength: 100
                    minLength: 1
                    type: string
                is_default_delivery:
                    example: true
                    type: boolean
                is_default_pickup:
                    example: false
                    type: boolean
                latitude:
                    example: 10.7769
                    format: double
                    maximum: 90
                    minimum: -90
                    type: number
                longitude:
                    example: 106.7009
                    format: double
                    maximum: 180
                    minimum: -180
                    type: number
                phone:
                    example: "+84901234567"
                    pattern: ^\+[1-9][0-9]{7,14}$
                    type: string
                postal_code:
                    example: "700000"
                    maxLength: 20
                    type: string
                province_code:
                    example: "79"
                    maxLength: 20
                    type: string
                province_name:
                    example: TP Hồ Chí Minh
                    maxLength: 100
                    type: string
                ward_code:
                    example: "26734"
                    maxLength: 20
                    type: string
                ward_name:
                    example: Bến Nghé
                    maxLength: 100
                    type: string
            type: object
        UpdateListingRequest:
            description: Every field optional. Variants are edited through their own endpoints.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                category_id:
                    $ref: '#/components/schemas/CategoryID'
                clear_featured_variant_id:
                    description: Leaves the listing with no featured variant; the card then shows the cheapest one.
                    example: false
                    type: boolean
                condition:
                    $ref: '#/components/schemas/ListingCondition'
                description:
                    maxLength: 20000
                    type: string
                featured_variant_id:
                    $ref: '#/components/schemas/VariantID'
                name:
                    maxLength: 200
                    minLength: 1
                    type: string
                price_mode:
                    $ref: '#/components/schemas/PriceMode'
                specifications:
                    additionalProperties: true
                    type: object
                tags:
                    items:
                        $ref: '#/components/schemas/TagSlug'
                    type: array
            type: object
        UpdateMessageRequest:
            properties:
                body:
                    maxLength: 4000
                    minLength: 1
                    type: string
            required:
                - body
            type: object
        UpdateNotificationPreferencesRequest:
            properties:
                items:
                    items:
                        properties:
                            category:
                                $ref: '#/components/schemas/NotificationCategory'
                            channel:
                                $ref: '#/components/schemas/NotificationChannel'
                            is_enabled:
                                type: boolean
                        required:
                            - category
                            - channel
                            - is_enabled
                        type: object
                    minItems: 1
                    type: array
            required:
                - items
            type: object
        UpdateOfferRequest:
            properties:
                quantity:
                    example: 2
                    format: int64
                    minimum: 1
                    type: integer
                reason:
                    maxLength: 500
                    type: string
                total:
                    example: 598000
                    format: int64
                    minimum: 1
                    type: integer
            required:
                - quantity
                - total
            type: object
        UpdateProfileRequest:
            description: Every field optional. `name`, `country`, `locale` and `timezone` are required columns, so they can be replaced but not cleared.
            properties:
                avatar_resource_id:
                    $ref: '#/components/schemas/ResourceID'
                clear_avatar_resource_id:
                    example: false
                    type: boolean
                clear_date_of_birth:
                    example: false
                    type: boolean
                clear_description:
                    example: false
                    type: boolean
                clear_gender:
                    example: false
                    type: boolean
                country:
                    example: VN
                    maxLength: 2
                    minLength: 2
                    pattern: ^[A-Z]{2}$
                    type: string
                date_of_birth:
                    example: "2001-02-03"
                    format: date
                    type: string
                description:
                    example: Bán đồ cũ
                    maxLength: 2000
                    type: string
                gender:
                    $ref: '#/components/schemas/ProfileGender'
                locale:
                    example: vi-VN
                    pattern: ^[a-z]{2}(-[A-Z]{2})?$
                    type: string
                name:
                    example: Alice
                    maxLength: 100
                    minLength: 1
                    type: string
                timezone:
                    example: Asia/Ho_Chi_Minh
                    maxLength: 64
                    type: string
            type: object
        UpdateReviewRequest:
            description: |
                Every field optional. `attachments` replaces the whole set and is capped at the same ten the submission is: an edit is not a way around a limit.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    maxItems: 10
                    type: array
                body:
                    maxLength: 2000
                    type: string
                rating:
                    maximum: 5
                    minimum: 1
                    type: integer
            type: object
        UpdateVariantRequest:
            description: Every field optional.
            properties:
                attachments:
                    items:
                        $ref: '#/components/schemas/ResourceID'
                    type: array
                attributes:
                    additionalProperties: true
                    type: object
                is_featured:
                    description: |
                        True moves the card's variant to this one. False is ignored: which variant is featured is cleared through `clear_featured_variant_id` on the listing, so there is one way to spell it.
                    example: true
                    type: boolean
                package_details:
                    additionalProperties: true
                    type: object
                price:
                    example: 299000
                    format: int64
                    minimum: 1
                    type: integer
                quantity:
                    description: The new total on hand. Refused below `reserved + sold`.
                    example: 42
                    format: int64
                    minimum: 0
                    type: integer
            type: object
        UploadSlot:
            properties:
                expires_at:
                    format: date-time
                    type: string
                headers:
                    additionalProperties:
                        type: string
                    description: Send these with the PUT verbatim, when the signature covers any.
                    type: object
                resource_id:
                    $ref: '#/components/schemas/ResourceID'
                url:
                    description: PUT the bytes here. Short-lived, and signed for this key and method only.
                    example: https://store.example/objects/catalog/listing/9f2c?signature=...
                    type: string
            required:
                - resource_id
                - url
                - expires_at
            type: object
        UpsertTaxInfoRequest:
            properties:
                legal_name:
                    maxLength: 200
                    minLength: 1
                    type: string
                tax_code:
                    description: Vietnamese MST — ten digits, or ten plus a three-digit branch.
                    pattern: ^\d{10}(-\d{3})?$
                    type: string
                tax_code_type:
                    $ref: '#/components/schemas/TaxCodeType'
            required:
                - tax_code
                - tax_code_type
                - legal_name
            type: object
        Variant:
            description: A purchasable variant. Price and package details live here rather than on the listing.
            properties:
                attributes:
                    additionalProperties: true
                    description: Variant attributes such as size and colour.
                    type: object
                created_at:
                    format: date-time
                    type: string
                id:
                    $ref: '#/components/schemas/VariantID'
                images:
                    description: Empty means fall back to the listing gallery.
                    items:
                        $ref: '#/components/schemas/Resource'
                    type: array
                is_featured:
                    description: The variant the card and search results show. At most one per listing.
                    example: true
                    type: boolean
                package_details:
                    additionalProperties: true
                    description: Weight and dimensions. A shipping quote is computed from these.
                    type: object
                price:
                    description: Smallest unit of the listing's currency.
                    example: 299000
                    format: int64
                    minimum: 1
                    type: integer
                stock:
                    $ref: '#/components/schemas/Stock'
            required:
                - id
                - price
                - attributes
                - package_details
                - images
                - is_featured
                - stock
                - created_at
            type: object
        VariantID:
            example: vrn_vq1wd6ptz3n8k
            pattern: ^vrn_[0-9a-hjkmnp-tv-z]{13}$
            type: string
        VerifyContactPhoneRequest:
            properties:
                code:
                    maxLength: 10
                    minLength: 4
                    type: string
            required:
                - code
            type: object
        VoteReviewRequest:
            properties:
                vote:
                    description: |
                        Helpful or not. There is no neutral value — withdrawing a vote is a delete.
                    enum:
                        - -1
                        - 1
                    type: integer
            required:
                - vote
            type: object
        Wallet:
            properties:
                account_id:
                    $ref: '#/components/schemas/AccountID'
                available_balance:
                    description: Spendable and withdrawable
                    example: 1250000
                    format: int64
                    minimum: 0
                    type: integer
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                held_balance:
                    description: Locked in escrow; not the owner's to draw yet
                    example: 299000
                    format: int64
                    minimum: 0
                    type: integer
            required:
                - account_id
                - currency
                - available_balance
                - held_balance
                - created_at
            type: object
        WalletAdjustmentRequest:
            description: |
                At least one delta must be non-zero. A movement that moves nothing is refused rather than recorded: the ledger rejects it at the database, and a row that says nothing is worse than an error.
            properties:
                available_delta:
                    default: 0
                    example: 250000
                    format: int64
                    type: integer
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                held_delta:
                    default: 0
                    example: -250000
                    format: int64
                    type: integer
                idempotency_key:
                    description: Required. A retried correction that posts twice is money invented.
                    maxLength: 200
                    minLength: 1
                    type: string
                reason:
                    maxLength: 2000
                    minLength: 1
                    type: string
            required:
                - currency
                - reason
                - idempotency_key
            type: object
        WalletList:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Wallet'
                    type: array
            required:
                - data
            type: object
        WalletTransaction:
            description: One balance change with its before/after snapshot. Append-only, and the only record of a movement that stays inside a wallet.
            properties:
                available_after:
                    example: 1250000
                    format: int64
                    minimum: 0
                    type: integer
                available_delta:
                    description: Signed change to the available balance
                    example: 250000
                    format: int64
                    type: integer
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                group_id:
                    description: |
                        Shared by every leg of one logical movement — a checkout is a debit plus an escrow hold plus a fee. An opaque token for grouping rows in a response, not an address: nothing accepts it as input and no endpoint resolves it.
                    nullable: true
                    type: string
                held_after:
                    example: 299000
                    format: int64
                    minimum: 0
                    type: integer
                held_delta:
                    description: Signed change to the held balance
                    example: -250000
                    format: int64
                    type: integer
                kind:
                    $ref: '#/components/schemas/WalletTransactionKind'
                note:
                    type: string
                ref_id:
                    description: Opaque id of the referenced entity, its prefix given by `ref_type`.
                    nullable: true
                    type: string
                ref_type:
                    description: What this movement was for. Null on an adjustment.
                    enum:
                        - order
                        - payment-session
                    nullable: true
                    type: string
                seq:
                    description: |
                        Position in this wallet's ledger, from 1. Identifies the row and orders it; a gap means a missing movement. Per wallet, so each currency counts from 1 again.
                    example: 42
                    format: int64
                    minimum: 1
                    type: integer
            required:
                - seq
                - currency
                - kind
                - available_delta
                - held_delta
                - available_after
                - held_after
                - note
                - created_at
            type: object
        WalletTransactionKind:
            enum:
                - topup
                - escrow-hold
                - escrow-release
                - payout
                - refund
                - withdrawal
                - fee
                - adjustment
            type: string
        WalletTransactionPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/WalletTransaction'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        Withdrawal:
            description: A payment session of kind `withdrawal`, projected with its destination and the admin's resolution.
            properties:
                amount:
                    example: 500000
                    format: int64
                    minimum: 1
                    type: integer
                bank_account:
                    $ref: '#/components/schemas/BankAccount'
                created_at:
                    format: date-time
                    type: string
                currency:
                    $ref: '#/components/schemas/CurrencyCode'
                id:
                    $ref: '#/components/schemas/PaymentSessionID'
                outcome:
                    $ref: '#/components/schemas/WithdrawalOutcome'
                resolution_note:
                    nullable: true
                    type: string
                resolved_at:
                    format: date-time
                    nullable: true
                    type: string
                resolved_by_id:
                    allOf:
                        - $ref: '#/components/schemas/AccountID'
                    description: The admin who decided. Null on one the owner cancelled themselves.
                    nullable: true
                status:
                    allOf:
                        - $ref: '#/components/schemas/PaymentSessionStatus'
                    description: |
                        The underlying session status. `outcome` is what a client should render; this is here because a withdrawal is a payment session and hiding that would make its id unexplainable.
            required:
                - id
                - outcome
                - status
                - currency
                - amount
                - bank_account
                - created_at
            type: object
        WithdrawalApprovalRequest:
            properties:
                provider_ref:
                    description: The bank's or gateway's reference for the outbound transfer
                    maxLength: 200
                    type: string
                reason:
                    description: A note for the payee, recorded on the request.
                    maxLength: 500
                    type: string
            type: object
        WithdrawalOutcome:
            description: |
                How a withdrawal ended, stated rather than inferred. The underlying session status cannot say it on its own: a cash-out the owner called off and one an admin refused both leave the money back in the wallet and the session not-successful, and telling them apart by whether `resolved_by_id` happens to be set is a rule every client would have to know. `awaiting-review` covers both `pending` and `processing`.
            enum:
                - awaiting-review
                - approved
                - rejected
                - cancelled
            type: string
        WithdrawalPage:
            properties:
                data:
                    items:
                        $ref: '#/components/schemas/Withdrawal'
                    type: array
                meta:
                    $ref: '#/components/schemas/PageMeta'
            required:
                - data
                - meta
            type: object
        WithdrawalRejectionRequest:
            properties:
                reason:
                    maxLength: 500
                    minLength: 1
                    type: string
            required:
                - reason
            type: object
    securitySchemes:
        bearerAuth:
            bearerFormat: JWT
            scheme: bearer
            type: http
info:
    description: |
        Public REST surface of the ShopNexus C2C marketplace gateway. This file is the base document (info, servers, security, shared parameters and responses). Paths and schemas live in one fragment per aggregate, internal/module/<module>/api/openapi/<aggregate>.yaml, and are merged into api/openapi.gen.yaml by `go generate ./...` (cmd/specgen). Edit the fragments, not the generated file.
        Only `paths` and `components.schemas` are merged from a fragment, so everything reusable across modules — parameters, responses, security schemes — has to be defined here.
    title: ShopNexus Gateway API
    version: 0.1.0
openapi: 3.0.3
paths:
    /accounts/{accountID}/feedback:
        get:
            description: Published rows only. A blind submission is not visible to anyone but its author until the window closes.
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - description: Which side the account was on.
                  in: query
                  name: role
                  schema:
                    $ref: '#/components/schemas/ReputationRole'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/FeedbackPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Published feedback received by an account
            tags:
                - trust
    /accounts/{accountID}/reputation:
        get:
            description: |
                Transaction ratings and product-review ratings are reported separately rather than merged. Only a seller can have review ratings.
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - description: Defaults to seller.
                  in: query
                  name: role
                  schema:
                    $ref: '#/components/schemas/ReputationRole'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Reputation'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read an account's reputation for one role
            tags:
                - trust
    /accounts/{id}:
        get:
            description: |
                The seller page. Deliberately narrow: a display name, an avatar, a blurb and a join date. Email, phone, birth date and saved addresses are never part of it.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/PublicAccount'
                                required:
                                    - data
                                type: object
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read another account's public profile
            tags:
                - account
    /accounts/{id}/followers:
        get:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AccountSummaryPage'
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Who follows this seller
            tags:
                - account
    /admin/accounts:
        get:
            description: |
                `q` is an **exact** match on email, phone or username — each is unique, so that is a key lookup and is how a moderator arrives here from a report — or a **fragment** match on the display name, which is answered by a trigram index. Whichever it is, it is one query; the caller does not choose.
            parameters:
                - description: An exact identifier, or part of a display name.
                  in: query
                  name: q
                  schema:
                    maxLength: 255
                    type: string
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/AccountStatus'
                - in: query
                  name: role
                  schema:
                    $ref: '#/components/schemas/AccountRole'
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminAccountPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: Search accounts
            tags:
                - admin
    /admin/accounts/{id}/suspension:
        delete:
            description: Clears the reason and the deadline with it. Past suspensions stay in the audit log rather than on the row.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AdminAccount'
                                required:
                                    - data
                                type: object
                    description: Reinstated
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Lift a suspension
            tags:
                - admin
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/AccountID'
        post:
            description: |
                The outcome of upholding a report against a seller. Omit `until` for a permanent suspension.

                Suspending also drops the account's sessions. A suspended row does not stop an access token that is already in circulation, so the two have to happen together.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SuspendAccountRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AdminAccount'
                                required:
                                    - data
                                type: object
                    description: Suspended
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Suspend an account
            tags:
                - admin
    /admin/categories:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateCategoryRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Category'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The named parent does not exist
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Name already taken
            security:
                - bearerAuth: []
            summary: Create a category
            tags:
                - admin
    /admin/categories/{id}:
        delete:
            description: |
                Children are promoted to roots rather than deleted with it, so a branch is never lost by accident. A category still holding listings cannot be deleted at all — move them first.
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Listings still reference this category
            security:
                - bearerAuth: []
            summary: Delete a category
            tags:
                - admin
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/CategoryID'
        patch:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateCategoryRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Category'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Name already taken
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A category cannot be its own descendant
            security:
                - bearerAuth: []
            summary: Update a category
            tags:
                - admin
    /admin/disputes:
        get:
            description: |
                Open rounds, oldest first, which is the order they should be worked. A round names the refund it is about; the case itself is read through `GET /refunds/{id}`.
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundDisputePage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The moderator's dispute queue
            tags:
                - admin
    /admin/disputes/{id}/ruling:
        post:
            description: |
                Decided on whose evidence is more convincing. What a verdict does depends on the round, and the round is a property of the case rather than something the moderator picks:

                Round 1 — `buyer_wins: true` grants the refund and books the return shipment, so the money is not released yet; `false` ends the refund and lets the payout stand.

                Round 2, the seller's appeal on what arrived — `buyer_wins: false` upholds it, ends the refund and lets the payout stand; `true` denies it, so the escrow goes back to the buyer and the order closes with it.

                The money moves before either row is written, and the verdict and the refund it moved are written together: a ruled round over a case still `disputed` is a state nothing can get out of, and a settled refund over an order the payout can still see is money paid twice.

                There is no third round. Round 2 is where an appeal already lives, so a ruling on it is final.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundDisputeID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DisputeRulingRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/RefundDispute'
                                required:
                                    - data
                                type: object
                    description: Ruled
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Dispute is already resolved
            security:
                - bearerAuth: []
            summary: Rule on the open round of a dispute (moderator)
            tags:
                - admin
    /admin/identity-documents:
        get:
            parameters:
                - description: Defaults to pending.
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/IdentityStatus'
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/IdentityDocumentPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The identity review queue
            tags:
                - admin
    /admin/identity-documents/{id}/verdict:
        post:
            description: |
                `verified` requires `expires_at` when the document type carries one, because the payout gate reads the expiry and not only the status. `rejected` requires a reason.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/IdentityDocumentID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/IdentityVerdictRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/IdentityDocument'
                                required:
                                    - data
                                type: object
                    description: Recorded
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already decided or the account already has a verified document
            security:
                - bearerAuth: []
            summary: Record an identity verdict
            tags:
                - admin
    /admin/listings:
        get:
            description: |
                Every listing awaiting a decision, oldest first, which is the order they should be worked: one waiting for its first publication, and one that is live while holding an edit its seller submitted. Defaults to both.
            parameters:
                - description: Defaults to everything awaiting a decision.
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ListingStatus'
                - in: query
                  name: seller_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The listing moderation queue
            tags:
                - admin
    /admin/listings/{id}/approval:
        post:
            description: Applies any held pending edit and makes it live.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ListingID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ModerationNoteRequest'
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Live
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Not awaiting moderation
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The approved edit names an image that no longer exists
            security:
                - bearerAuth: []
            summary: Clear a listing for publication
            tags:
                - admin
    /admin/listings/{id}/takedown:
        post:
            description: |
                The moderator's verdict against a listing that breaks policy. Removes it from the marketplace and records the reason. Suspending the seller as well is a separate decision and a separate call.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ListingID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/TakedownRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Taken down
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already hidden or still a draft, so there is nothing to take down
            security:
                - bearerAuth: []
            summary: Take a listing down
            tags:
                - admin
    /admin/moderators:
        post:
            description: |
                Only an admin may do this and there is no self-service path — a moderator decides disputes and takes listings down, so the role is granted, never claimed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateModeratorRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AdminAccount'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Email or phone or username already taken
            security:
                - bearerAuth: []
            summary: Provision a moderator account
            tags:
                - admin
    /admin/moderators/{id}:
        delete:
            description: Demotes to a plain user and drops the account's sessions. The account itself survives, since it may have traded.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "204":
                    description: Revoked
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Revoke the moderator role
            tags:
                - admin
    /admin/payment-sessions:
        get:
            description: Filter by kind, status and a settlement window. Intended for tying platform totals to what the rails report.
            parameters:
                - in: query
                  name: kind
                  schema:
                    $ref: '#/components/schemas/PaymentSessionKind'
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - in: query
                  name: account_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - description: Inclusive lower bound on creation time
                  in: query
                  name: from
                  schema:
                    format: date-time
                    type: string
                - description: Exclusive upper bound on creation time
                  in: query
                  name: to
                  schema:
                    format: date-time
                    type: string
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PaymentSessionPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
            security:
                - bearerAuth: []
            summary: Reconciliation view over all payment sessions
            tags:
                - admin
    /admin/reports:
        get:
            description: |
                Defaults to the open and under-review ones, oldest first — that is the small hot slice the queue is worked from, and the index behind it covers exactly that predicate.
            parameters:
                - description: Defaults to open and reviewing.
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ReportStatus'
                - in: query
                  name: ref_type
                  schema:
                    $ref: '#/components/schemas/ReportRefType'
                - in: query
                  name: reason
                  schema:
                    $ref: '#/components/schemas/ReportReason'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminReportPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
            security:
                - bearerAuth: []
            summary: The report queue
            tags:
                - admin
    /admin/reports/{id}:
        get:
            description: |
                Includes the reported content itself and how many other open reports name the same target, since a moderator decides on the pattern and not one complaint.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReportID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AdminReport'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a report with its target
            tags:
                - admin
    /admin/reports/{id}/claim:
        post:
            description: Moves it out of the open queue so two moderators do not work the same case.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReportID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Report'
                                required:
                                    - data
                                type: object
                    description: Claimed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already claimed or resolved
            security:
                - bearerAuth: []
            summary: Take a report for review
            tags:
                - admin
    /admin/reports/{id}/resolution:
        post:
            description: |
                Records the verdict and what was done about it. `dismissed` takes the action `none`; upholding it names the action instead — the listing was removed, the message was removed, the account was suspended, a warning was issued.

                Recording the action here does not carry it out. Taking a listing down and suspending a seller are separate calls to the modules that own them, so the decision and its effects each stay where they can be audited.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReportID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ResolveReportRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Report'
                                required:
                                    - data
                                type: object
                    description: Resolved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Moderator role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved
            security:
                - bearerAuth: []
            summary: Resolve a report
            tags:
                - admin
    /admin/tags/{slug}:
        delete:
            description: Removing it also detaches it from every listing that carried it.
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Delete a tag
            tags:
                - admin
        parameters:
            - in: path
              name: slug
              required: true
              schema:
                $ref: '#/components/schemas/TagSlug'
        put:
            description: |
                Upsert, not create: the slug is the whole identity of a tag and the only other column is its description, so a separate create and update would be the same statement twice and would need a 409 to tell them apart.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PutTagRequest'
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Tag'
                                required:
                                    - data
                                type: object
                    description: Created or updated
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
            security:
                - bearerAuth: []
            summary: Create or update a tag
            tags:
                - admin
    /admin/tax-info/{accountID}/verification:
        post:
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/TaxVerificationRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/TaxInfo'
                                required:
                                    - data
                                type: object
                    description: Recorded
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This tax code is already verified under another account
            security:
                - bearerAuth: []
            summary: Record the verdict on a tax registration
            tags:
                - admin
    /admin/wallets/{accountID}:
        get:
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read another account's balances
            tags:
                - admin
    /admin/wallets/{accountID}/adjustments:
        post:
            description: |
                The one way a balance changes without a business event behind it, so it is the most auditable: a reason is mandatory and an `idempotency_key` is required, because a retried correction that posts twice is money invented. Reusing a key returns the movement already recorded instead of making a second one.
            parameters:
                - in: path
                  name: accountID
                  required: true
                  schema:
                    $ref: '#/components/schemas/AccountID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/WalletAdjustmentRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Wallet'
                                required:
                                    - data
                                type: object
                    description: |
                        Adjusted, answering the balances as they now stand. The same response for a key that was already applied: a retried correction is the state the admin asked for, not a second credit.
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Malformed, or both deltas are zero
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The adjustment would drive a balance negative
            security:
                - bearerAuth: []
            summary: Correct a balance by hand
            tags:
                - admin
    /admin/withdrawals:
        get:
            parameters:
                - description: Defaults to pending
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WithdrawalPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
            security:
                - bearerAuth: []
            summary: The withdrawal review queue
            tags:
                - admin
    /admin/withdrawals/{id}/approval:
        post:
            description: The wallet was already debited when the request was made, so approving does not move the balance again — it settles the outbound leg.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/WithdrawalApprovalRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Withdrawal'
                                required:
                                    - data
                                type: object
                    description: Approved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved
            security:
                - bearerAuth: []
            summary: Approve a withdrawal and release the transfer
            tags:
                - admin
    /admin/withdrawals/{id}/rejection:
        post:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/WithdrawalRejectionRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Withdrawal'
                                required:
                                    - data
                                type: object
                    description: Rejected and the debit reversed
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Admin role required
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved
            security:
                - bearerAuth: []
            summary: Reject a withdrawal and return the money to the balance
            tags:
                - admin
    /bank-accounts:
        get:
            description: Account numbers come back masked. The full number is only ever used to move money, never to display it back.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BankAccountList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's payout destinations
            tags:
                - finance
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateBankAccountRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/BankAccount'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: |
                        The caller already has this bank account registered. The same account number under a *different* platform account is not refused here — it is a mule signal, and answering it would confirm to a stranger that the number is already on the platform.
            security:
                - bearerAuth: []
            summary: Register a payout destination
            tags:
                - finance
    /bank-accounts/{id}:
        delete:
            description: Soft delete. A completed withdrawal names this row as the place real money went, so financial history cannot lose its payee.
            responses:
                "204":
                    description: Removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A withdrawal against it is still unresolved
            security:
                - bearerAuth: []
            summary: Remove a payout destination
            tags:
                - finance
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/BankAccountID'
        patch:
            description: At most one default per account, so this clears the previous one. Bank details themselves are not editable — register another destination and remove this one, so a completed withdrawal never names a row whose details have since changed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateBankAccountRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/BankAccount'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Make this the default payout destination
            tags:
                - finance
    /cart-items:
        get:
            description: |
                The whole cart, unpaginated. A cart is bounded by what one person means to buy, so there is nothing to page through.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        items:
                                            $ref: '#/components/schemas/CartItem'
                                        type: array
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's cart
            tags:
                - order
        post:
            description: |
                One row per (account, SKU). Adding a SKU already in the cart tops that row up rather than stacking a second one — adding twice means wanting more.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AddCartItemRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/CartItem'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Add a SKU to the cart
            tags:
                - order
    /cart-items/{id}:
        delete:
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Remove a cart row
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/CartItemID'
        patch:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateCartItemRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/CartItem'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Change the quantity of a cart row
            tags:
                - order
    /categories:
        get:
            description: |
                Flat list with a parent reference on each row; a client assembles the tree. A category tree is small so this is unpaginated.

                `near` answers a different question: which categories does this thing belong in. It ranks by cosine distance over `category_embedding` and returns a shortlist with a `score` instead of the tree, which is what the category field of the listing form needs — a seller who has picked a few tags should not have to walk a tree to find where the item goes.
            parameters:
                - description: Rank by closeness to these seeds instead of returning the tree.
                  explode: false
                  in: query
                  name: near
                  schema:
                    items:
                        $ref: '#/components/schemas/SemanticSeed'
                    maxItems: 8
                    minItems: 1
                    type: array
                  style: form
                - description: Only honoured with `near`; the whole tree is never truncated.
                  in: query
                  name: limit
                  schema:
                    default: 10
                    maximum: 50
                    minimum: 1
                    type: integer
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CategoryList'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A `near` seed has no embedding yet
            summary: The category tree, or the categories nearest a set of seeds
            tags:
                - catalog
    /contacts:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ContactList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's saved addresses
            tags:
                - account
        post:
            description: |
                The administrative codes are what a carrier is called with, so they are the routing source of truth and the names beside them are display snapshots. A coordinate is optional: geocoding may fail and the address still has to be saveable.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateContactRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Contact'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Save an address
            tags:
                - account
    /contacts/{id}:
        delete:
            responses:
                "204":
                    description: Removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Remove a saved address
            tags:
                - account
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ContactID'
        patch:
            description: |
                Setting a default clears the previous one. Delivery and pickup defaults are independent flags — the same address is usually both.

                An order already placed keeps its own snapshot of the address, so editing this row never rewrites where a shipment was going.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateContactRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Contact'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Update a saved address
            tags:
                - account
    /contacts/{id}/phone/verification-requests:
        post:
            description: |
                The phone a carrier will actually call, which is why this one is verified and the account's sign-in phone is not. A courier who cannot reach the number is the most common way a delivery fails, so a seller shipping to an unverified number is worth flagging in the UI.

                Re-sending replaces the outstanding code rather than issuing a second valid one.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ContactID'
            responses:
                "202":
                    description: Code sent
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already verified
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Too many requests for this number
            security:
                - bearerAuth: []
            summary: Send a verification code to a saved address's phone
            tags:
                - account
    /contacts/{id}/phone/verifications:
        post:
            description: |
                Editing the number clears the flag again — a verified flag on a number nobody checked is worse than no flag.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ContactID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/VerifyContactPhoneRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Contact'
                                required:
                                    - data
                                type: object
                    description: Verified
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Code is wrong, expired, or already used
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Confirm a saved address's phone with the code
            tags:
                - account
    /conversations:
        get:
            description: |
                Threads by latest activity. A participant sits on either side of the stored pair, so this is answered as two ordered index scans merged together rather than one scan that sorts.
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ConversationPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's inbox
            tags:
                - chat
        post:
            description: |
                Idempotent by design: there is exactly one thread per pair, so this returns the existing one when it exists and creates it otherwise. A client does not have to track whether it has messaged someone before.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartConversationRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Conversation'
                                required:
                                    - data
                                type: object
                    description: The thread already existed
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Conversation'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An account cannot open a thread with itself
            security:
                - bearerAuth: []
            summary: Open the thread with another account
            tags:
                - chat
    /conversations/{id}:
        get:
            description: Carries the counterparty rather than both sides — the caller already knows who they are.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ConversationID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Conversation'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a thread
            tags:
                - chat
    /conversations/{id}/messages:
        get:
            description: |
                Newest first, cursor only. Offset paging would defeat the chunk exclusion the time partitioning exists for.

                A redacted message is still returned, with its body emptied and `deleted_at` set: a gap in a thread that a dispute may hinge on is worse than a visible removal.
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MessagePage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a thread's messages
            tags:
                - chat
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ConversationID'
        post:
            description: |
                Text, attachments or both. `refs` carries pointers to a listing or a variant or an order, which is how a product is referred to inside a thread that is not itself listing-scoped.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SendMessageRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Message'
                                required:
                                    - data
                                type: object
                    description: Sent
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A message needs a body or an attachment
            security:
                - bearerAuth: []
            summary: Send a message
            tags:
                - chat
    /conversations/{id}/read:
        post:
            description: |
                Moves the caller's read mark on the thread — one UPDATE of one row, whatever it covers. Omit `before` to mark the whole thread read. A mark never moves backwards: passing an earlier instant than the current one is a no-op, because un-reading is not a thing a client should be able to do to its own badge.

                Answers the updated thread rather than just the badge, so a client that marked it read can repaint the row from one response instead of a second GET.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ConversationID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MarkConversationReadRequest'
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Conversation'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Mark a thread read up to a point in time
            tags:
                - chat
    /conversations/unread-count:
        get:
            description: |
                One range scan per thread the caller has, each bounded below by that thread's read mark — so an old unread message costs no more than a recent one and chunk exclusion applies throughout, but the work is proportional to how many threads the caller has, not constant. Separate from the inbox because the badge is read far more often than the list.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ChatUnreadCount'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The unread badge across every thread
            tags:
                - chat
    /conversations/uploads:
        post:
            description: |
                The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /conversations/uploads/{id}/confirmation` makes the row real once the object is there — so a message can never render a photo whose bytes never arrived.

                `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateUploadRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/UploadSlot'
                                required:
                                    - data
                                type: object
                    description: Reserved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Larger than this platform accepts
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: That file type is not accepted
            security:
                - bearerAuth: []
            summary: Reserve a slot to upload a message attachment into
            tags:
                - chat
    /conversations/uploads/{id}/confirmation:
        post:
            description: |
                Until this lands the resource resolves to nothing, so a half-finished upload cannot be attached to a message. The recorded size is the store's, not the one declared when the slot was reserved.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ResourceID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Resource'
                                required:
                                    - data
                                type: object
                    description: Confirmed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No such reserved slot of the caller's
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Nothing was uploaded to that slot
            security:
                - bearerAuth: []
            summary: Confirm the bytes are at the store
            tags:
                - chat
    /devices:
        put:
            description: |
                An upsert on the push token, not a create. The token identifies an install and the platform reissues the same one when a different user signs in on that phone, so the row moves to the new account. Without that, the previous owner keeps receiving that phone's notifications.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterDeviceRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Device'
                                required:
                                    - data
                                type: object
                    description: Registered or moved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Register or move a push device
            tags:
                - account
    /devices/{id}:
        delete:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/DeviceID'
            responses:
                "204":
                    description: Unregistered
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Unregister a push device
            tags:
                - account
    /drafts:
        get:
            parameters:
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DraftOrderPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's purchase sessions
            tags:
                - order
        post:
            description: |
                Freezes the listing terms so a price the seller edits afterwards cannot be charged. The server snapshots the listing together with every variant — price and package details live on the variant, and those are exactly what must not move under the buyer — and sets `valid_until`.

                Only a `fixed` listing has one: a `negotiable` price is agreed in an offer, and the accepted offer is what freezes its terms instead. Asking for one is 422.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateDraftRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/DraftOrder'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Listing not found, deleted, or not active
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The listing is priced negotiable; open an offer instead
            security:
                - bearerAuth: []
            summary: Open a purchase session for one listing
            tags:
                - order
    /drafts/{id}:
        delete:
            description: Sets `cancelled_at`. A session that already produced paid items cannot be cancelled.
            responses:
                "204":
                    description: Cancelled
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    $ref: '#/components/responses/Conflict'
            security:
                - bearerAuth: []
            summary: Cancel a purchase session
            tags:
                - order
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/DraftOrder'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a purchase session and its frozen terms
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/DraftOrderID'
    /drafts/{id}/checkout:
        post:
            description: |
                Creates one `item` per line plus a single buyer-checkout payment session, and returns both. Line prices come from the session's frozen snapshot, never from the live catalog. `contact_id` is snapshotted into every line's delivery address: one session covers one SPU and therefore one seller, so a single address is correct.

                The session is spent before the money is asked for: the claim is what makes two concurrent checkouts of one draft impossible, and the loser is answered 409 rather than handed a second payment session for the same frozen price.

                A `negotiable` listing has no draft at all, so there is nothing to check out here — its terms are frozen by the offer both sides agreed on, and `POST /offers/{id}/checkout` is the same checkout from there. The buyer pays delivery either way.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/DraftOrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CheckoutRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/CheckoutResult'
                                required:
                                    - data
                                type: object
                    description: Items created and a payment session opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Session expired, cancelled, or already checked out
                "422":
                    $ref: '#/components/responses/UnprocessableEntity'
            security:
                - bearerAuth: []
            summary: Check out a purchase session (pay-first)
            tags:
                - order
    /email/verification-requests:
        post:
            responses:
                "202":
                    description: Verification message sent
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already verified
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No email on this account
            security:
                - bearerAuth: []
            summary: Send a verification link to the caller's email
            tags:
                - account
    /email/verifications:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/EmailVerificationRequest'
                required: true
            responses:
                "204":
                    description: Verified
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Token is unknown or already used or expired
            summary: Confirm an email address
            tags:
                - account
    /favorites/{listingID}:
        delete:
            description: |
                Idempotent, and deliberately so all the way: unsaving something that was never saved answers 204, and so does a `listingID` that names no listing at all. The post-condition — this listing is not on the caller's wishlist — holds either way, and a 404 here would only tell a stranger which ids exist.
            responses:
                "204":
                    description: Removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Unsave a listing
            tags:
                - catalog
        parameters:
            - in: path
              name: listingID
              required: true
              schema:
                $ref: '#/components/schemas/ListingID'
        put:
            description: |
                Idempotent — the account and listing pair is the whole row, so saving twice is the same as saving once. Read the wishlist back through `GET /listings?favorited=true`.
            responses:
                "204":
                    description: Saved
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Save a listing
            tags:
                - catalog
    /follows/{accountID}:
        delete:
            responses:
                "204":
                    description: Unfollowed
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Unfollow a seller
            tags:
                - account
        parameters:
            - in: path
              name: accountID
              required: true
              schema:
                $ref: '#/components/schemas/AccountID'
        put:
            description: Idempotent. Following yourself is refused.
            responses:
                "204":
                    description: Following
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An account cannot follow itself
            security:
                - bearerAuth: []
            summary: Follow a seller
            tags:
                - account
    /identity-documents:
        post:
            description: |
                Hands the uploaded scans to the KYC vendor. Neither a document number nor a scan is stored here — only the vendor, its case reference and the verdict — so a leak of this data cannot impersonate anyone.

                The verdict may already be final in the response: a vendor that reads the images answers in the same call, so `status` can come back `verified` or `rejected` with no session URL. A vendor that runs its own web flow answers `pending` and returns `vendor_session_url` for the caller to finish in. A client has to read the status rather than assume one of the two.

                A rejected case leaves the account free to submit another one: the usual reason is glare on the card, and there is nothing to appeal.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartIdentityVerificationRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/IdentityVerificationTicket'
                                required:
                                    - data
                                type: object
                    description: Case opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This account already holds a live verified document
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A named scan is missing, unconfirmed, or cannot be fetched
            security:
                - bearerAuth: []
            summary: Start identity verification
            tags:
                - account
    /items:
        get:
            description: |
                `role=seller&pending=true` is the seller's retry list: lines the money has paid for that no order covers yet. Not an inbox — nobody confirms them. The order is written by the payment webhook, so a line sitting here for long is a settlement that has not succeeded yet rather than one waiting on a person.
            parameters:
                - in: query
                  name: role
                  schema:
                    default: buyer
                    enum:
                        - buyer
                        - seller
                    type: string
                - description: Only lines no order covers and nobody cancelled.
                  in: query
                  name: pending
                  schema:
                    default: false
                    type: boolean
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OrderItemPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List checkout lines, as buyer or as seller
            tags:
                - order
    /items/{id}/cancellation:
        post:
            description: |
                Either party may drop a line while it is unpaid — the buyer changed their mind, or the seller cannot fulfil it — and the reserved units go back. The cancelling account is recorded.

                A paid line is 409, and that is checked against the payment session rather than against `order_id`: the order is written by the webhook, so a line that has been charged for has a null `order_id` for as long as that write keeps failing. From there the way back is a refund, which the seller gets to see.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderItemID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/OrderItem'
                                required:
                                    - data
                                type: object
                    description: Cancelled
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The session is paid, or the line is already cancelled
            security:
                - bearerAuth: []
            summary: Cancel a line the money has not reached
            tags:
                - order
    /listings:
        get:
            description: |
                With no parameters, the newest live active listings. The parameters narrow that one query rather than selecting between separate endpoints.

                `q` makes it a search and moves the default sort to `relevance`. `lexical` matches the name by trigram and tolerates missing diacritics, `semantic` runs an approximate nearest-neighbour search over the dense embedding, and `hybrid` combines the dense vector with the sparse lexical one, which is what the embedding model is built for. Each hit then carries `score`, always so that higher is better. A listing whose embedding has not been computed yet can only be found lexically.

                `sort=recommended` is the personalised feed: nearest-neighbour search over listing embeddings from the caller's interest slots. It needs a token, and falls back to newest for an account with no interests computed yet.

                `mine=true` restricts the result to the caller's own listings, and is the only case in which `status` is honoured — a seller has to see what is not public, and nobody else may.

                `favorited=true` is the wishlist page. It is a filter here rather than its own endpoint because a wishlist wants exactly what a feed wants — cards, with prices and stock and every filter available — and a separate endpoint returning saved ids left the client resolving them one by one.

                `ids` resolves a known set, which is how a cart or an order list renders the listings behind its rows. It ignores every other filter and answers for hidden and soft-deleted listings too, because an order that references one still has to display it; `deleted_at` says which. Listings that were never public (`draft`, `pending`) stay out unless they are the caller's own.

                `price-asc` and `price-desc` are answered through the SKU price index rather than a value cached on the listing, so `price` is the cheapest matching variant's. `best-selling` is the one that cannot work that way — it orders by a sum over the variants, and a sum has no per-SKU index to scan in order — so it reads `cached_sold` on the listing, maintained with the sales it counts.
            parameters:
                - description: Resolve exactly these listings, ignoring the other filters.
                  explode: false
                  in: query
                  name: ids
                  schema:
                    items:
                        $ref: '#/components/schemas/ListingID'
                    maxItems: 100
                    type: array
                  style: form
                - description: Free-text query. Turns the request into a search.
                  in: query
                  name: q
                  schema:
                    maxLength: 200
                    minLength: 1
                    type: string
                - description: Search mode. Ignored without a query.
                  in: query
                  name: mode
                  schema:
                    default: hybrid
                    enum:
                        - lexical
                        - semantic
                        - hybrid
                    type: string
                - description: The caller's own listings, in every state.
                  in: query
                  name: mine
                  schema:
                    default: false
                    type: boolean
                - description: The caller's wishlist. This is the wishlist page.
                  in: query
                  name: favorited
                  schema:
                    default: false
                    type: boolean
                - description: Only honoured together with mine=true.
                  in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ListingStatus'
                - in: query
                  name: category_id
                  schema:
                    $ref: '#/components/schemas/CategoryID'
                - in: query
                  name: tag
                  schema:
                    $ref: '#/components/schemas/TagSlug'
                - in: query
                  name: seller_id
                  schema:
                    $ref: '#/components/schemas/AccountID'
                - in: query
                  name: condition
                  schema:
                    $ref: '#/components/schemas/ListingCondition'
                - in: query
                  name: min_price
                  schema:
                    format: int64
                    minimum: 0
                    type: integer
                - in: query
                  name: max_price
                  schema:
                    format: int64
                    minimum: 0
                    type: integer
                - description: Defaults to `relevance` when a query is given and `newest` otherwise.
                  in: query
                  name: sort
                  schema:
                    enum:
                        - newest
                        - rating
                        - price-asc
                        - price-desc
                        - best-selling
                        - relevance
                        - recommended
                    type: string
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListingPage'
                    description: OK
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: |
                        A combination that has no answer: `sort=relevance` without a query, `status` without `mine`, or `sort=recommended` together with `favorited` or `mine` — a personalised ranking of a set the caller already chose is not a ranking of anything. Combinations are refused rather than silently resolved by precedence, so a client never gets a different list than it asked for.
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: '`mine=true`, `favorited=true` or `sort=recommended` without a token'
            security:
                - {}
                - bearerAuth: []
            summary: Browse, search and resolve listings
            tags:
                - catalog
        post:
            description: |
                Creates the listing and its variants together. At least one variant is required because price and shipping weight live there.

                Starts as a draft and is not public until published.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateListingRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Category or tag or image not found
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Slug already taken
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Identity verification is required before selling
            security:
                - bearerAuth: []
            summary: Create a listing
            tags:
                - catalog
    /listings/{id}:
        delete:
            description: |
                Soft delete, and distinct from hiding it. Order history holds the listing and variant ids without a foreign key, so a past order has to stay resolvable after the seller removes the listing.
            parameters:
                - description: The opaque id. A write addresses the listing, never its public slug.
                  in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ListingID'
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An open order still covers this listing
            security:
                - bearerAuth: []
            summary: Delete a listing
            tags:
                - catalog
        get:
            description: |
                Carries the variants with their prices and stock and the seller summary.

                A listing that was never public — `draft` or `pending` — is readable only by its owner and by staff. One that is `hidden` or soft-deleted is readable by anyone, because a cart or an order that references it still has to render; `status` and `deleted_at` say that it cannot be bought.
            parameters:
                - description: |
                    The listing's opaque id or its slug. A slug is lowercase, hyphen-separated and never contains an underscore, so it cannot be mistaken for an id — but only the id is accepted today; resolving a slug needs a lookup this route does not have yet.
                  in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ListingID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - {}
                - bearerAuth: []
            summary: Read a listing
            tags:
                - catalog
        patch:
            description: |
                An edit to a draft applies immediately. An edit to a live listing that trips the prohibited-goods filter is held as a pending edit and applied only when a moderator approves it, so the version buyers see never changes into unreviewed content.

                The slug is fixed at creation. Renaming a listing leaves it alone: there is no redirect table, so a new slug would only turn every link already shared into a 404, and `GET /listings/{id}` takes the id as well.
            parameters:
                - description: The opaque id. A write addresses the listing, never its public slug.
                  in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ListingID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateListingRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Applied
                "202":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Held as a pending edit awaiting moderation
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The listing changed while you were editing it
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The result would break a rule — no variant left, more than ten tags
            security:
                - bearerAuth: []
            summary: Edit a listing
            tags:
                - catalog
    /listings/{id}/publication:
        delete:
            description: |
                Takes it out of every feed while leaving it alive — the seller is away, or the item is temporarily out of stock. Not the same as deleting it.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Hidden
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Not live, so there is nothing to take down
            security:
                - bearerAuth: []
            summary: Hide a live listing
            tags:
                - catalog
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ListingID'
        post:
            description: |
                Publication always goes through moderation: this queues the listing and it stays invisible until a moderator clears it. There is no path that makes a listing live without a human, which is also why re-publishing something a moderator took down cannot undo the takedown.

                An automatic prohibited-goods scan still runs — it orders the queue and flags what to look at first, it does not clear anything.
            responses:
                "202":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Queued for moderation and not yet public
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already live or already under moderation
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A listing needs at least one variant with a price
            security:
                - bearerAuth: []
            summary: Submit a listing for publication
            tags:
                - catalog
    /listings/{id}/variants:
        post:
            description: |
                Answers with the listing. The new variant is the one in `variants` that was not there before — there is no separate id in the response, because a variant is not addressable on its own.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ListingID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateVariantRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Add a variant to a listing
            tags:
                - catalog
    /listings/{listingID}/reviews:
        get:
            description: |
                Public, with the first few replies and the vote tally on each one.

                A cursor belongs to the sort it was issued under: each ordering pages on its own key, so a cursor from `sort=helpful` is meaningless to `sort=newest` and a client that switches sorts starts the traversal again.

                `sort=helpful` orders by a counter that other people are changing while a client pages: a review that gains votes can move across a page boundary and be seen twice or not at all. Cursor paging is exact only over a key that does not move, and `newest` is that key. The drift is accepted rather than designed away — a review appearing twice in a list costs the reader nothing — but a caller that needs a stable traversal should page by `newest`.
            parameters:
                - description: Restrict to one star rating.
                  in: query
                  name: rating
                  schema:
                    maximum: 5
                    minimum: 1
                    type: integer
                - description: '`helpful` reads the stored tally; see the note above about paging it.'
                  in: query
                  name: sort
                  schema:
                    default: newest
                    enum:
                        - newest
                        - helpful
                    type: string
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReviewPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Reviews of a listing
            tags:
                - trust
        parameters:
            - in: path
              name: listingID
              required: true
              schema:
                $ref: '#/components/schemas/ListingID'
        post:
            description: |
                A completed order is required — no purchase, no review — and one review per listing per order, so buying twice earns a second review but one purchase does not earn two. An order that was cancelled or fully refunded earns none: there are no goods left to rate. Rating the counterparty of such a sale is what feedback is for.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitReviewRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Review'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The order does not belong to the caller
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already reviewed for this order
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The order did not include this listing, or did not complete — a cancelled sale leaves no goods to review
            security:
                - bearerAuth: []
            summary: Review a listing you bought
            tags:
                - trust
    /listings/uploads:
        post:
            description: |
                The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /listings/uploads/{id}/confirmation` makes the row real once the object is there — so a listing can never render a photo whose bytes never arrived.

                `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateUploadRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/UploadSlot'
                                required:
                                    - data
                                type: object
                    description: Reserved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Larger than this platform accepts
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: That file type is not accepted
            security:
                - bearerAuth: []
            summary: Reserve a slot to upload a listing photo into
            tags:
                - catalog
    /listings/uploads/{id}/confirmation:
        post:
            description: |
                Until this lands the resource resolves to nothing, so a half-finished upload cannot be attached to a listing. The recorded size is the store's, not the one declared when the slot was reserved.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ResourceID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Resource'
                                required:
                                    - data
                                type: object
                    description: Confirmed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No such reserved slot of the caller's
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Nothing was uploaded to that slot
            security:
                - bearerAuth: []
            summary: Confirm the bytes are at the store
            tags:
                - catalog
    /login:
        post:
            description: The identifier is an email or a phone or a username. Which one it is does not change the answer and is not reported back.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AuthResult'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Wrong credentials
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Account suspended
            summary: Sign in with a password
            tags:
                - account
    /login/oauth:
        post:
            description: |
                Links to an existing account when the provider's subject id is already known, and otherwise creates one. A provider that returns no email — Apple with "hide my email" — gets a generated username, because the account still has to carry an identifier.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/OAuthLoginRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AuthResult'
                                required:
                                    - data
                                type: object
                    description: Signed in to an existing account
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AuthResult'
                                required:
                                    - data
                                type: object
                    description: A new account was created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Provider rejected the credential
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Account suspended
            summary: Sign in through a federated provider
            tags:
                - account
    /logout:
        post:
            description: Also unregisters the caller's push device when one is supplied so the phone stops receiving notifications for an account nobody is signed in to.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LogoutRequest'
                required: false
            responses:
                "204":
                    description: Session revoked
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Revoke the current session
            tags:
                - account
    /me:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Me'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Read the caller's own account and profile
            tags:
                - account
        patch:
            description: |
                Changing the email clears `email_verified` and sends a fresh verification. The last remaining identifier cannot be removed.

                The phone here is an identifier — something to sign in and be reached by — and carries no verified flag. Verification belongs to a phone a carrier will actually call, which is the one on a `contact`: that is where `phone_verified` lives, and editing this one does not touch it.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateAccountRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Me'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Email or phone or username already taken
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: That would leave the account with no identifier
            security:
                - bearerAuth: []
            summary: Change the caller's identifiers
            tags:
                - account
    /me/devices:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeviceList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's push devices
            tags:
                - account
    /me/following:
        get:
            parameters:
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AccountSummaryPage'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Sellers the caller follows
            tags:
                - account
    /me/identity-documents:
        get:
            description: Includes `expires_at` — a passport runs out and a payout gate has to look at more than the status.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/IdentityDocumentList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's verification history
            tags:
                - account
    /me/oauth-identities:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OAuthIdentityList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's linked providers
            tags:
                - account
    /me/oauth-identities/{provider}:
        delete:
            description: Refused when it would leave the account with no way to sign in — no password and no other linked provider.
            parameters:
                - in: path
                  name: provider
                  required: true
                  schema:
                    $ref: '#/components/schemas/OAuthProvider'
            responses:
                "204":
                    description: Unlinked
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This is the only way left to sign in
            security:
                - bearerAuth: []
            summary: Unlink a provider
            tags:
                - account
    /me/profile:
        patch:
            description: This is the shop front. `locale` and `timezone` also decide how notifications are written and when they are sent.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateProfileRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Profile'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Update the caller's public profile
            tags:
                - account
    /me/uploads:
        post:
            description: |
                The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /me/uploads/{id}/confirmation` makes the row real once the object is there. One store serves both `kind`s — an avatar is a public picture and an identity scan is not — because `kind` only narrows the key prefix; what keeps a scan from ever being world-readable is that it is only ever read back through a freshly presigned link, never a public URL.

                `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AccountCreateUploadRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/UploadSlot'
                                required:
                                    - data
                                type: object
                    description: Reserved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Larger than this platform accepts
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: That file type is not accepted
            security:
                - bearerAuth: []
            summary: Reserve a slot to upload an avatar or an identity scan into
            tags:
                - account
    /me/uploads/{id}/confirmation:
        post:
            description: |
                Until this lands the resource resolves to nothing, so a half-finished upload cannot be set as an avatar or submitted as an identity scan. The recorded size is the store's, not the one declared when the slot was reserved.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ResourceID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Resource'
                                required:
                                    - data
                                type: object
                    description: Confirmed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No such reserved slot of the caller's
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Nothing was uploaded to that slot
            security:
                - bearerAuth: []
            summary: Confirm the bytes are at the store
            tags:
                - account
    /messages/{id}:
        delete:
            description: |
                The sender unsending, or a moderator acting on an upheld report. The row survives with its body **and its attachments** cleared: a thread is evidence in a refund dispute, so removal is recorded rather than erased, but a redaction that leaves the picture up has not removed anything.
            responses:
                "204":
                    description: Redacted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Only the sender or a moderator may redact a message
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Redact a message
            tags:
                - chat
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/MessageID'
            - description: |
                The message's own `created_at`, passed straight back. Required, and not redundant: `message` is partitioned on it and its primary key is `(id, created_at)`, so without it the row has to be hunted through every chunk of the table. A client always has the message before editing or redacting it, so it always has this. Named after the field it comes from — anything else would have the client copying `created_at` into a differently-named parameter.
              in: query
              name: created_at
              required: true
              schema:
                format: date-time
                type: string
        patch:
            description: |
                Sender only, and it records that the message was edited — the counterparty has to be able to see that what they read has changed. A system message cannot be edited because it has no author.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateMessageRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Message'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Only the sender may edit a message
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A system message cannot be edited
            security:
                - bearerAuth: []
            summary: Edit a message
            tags:
                - chat
    /notification-preferences:
        get:
            description: |
                Returns every category and channel pair with the value in force. Stored rows are sparse — one exists only where the account differs from the default — and the defaults live in the domain, so resolving them here keeps the client from having to know them.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/NotificationPreferenceList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Read the effective notification settings
            tags:
                - account
        put:
            description: |
                Setting a pair back to its default deletes the row rather than storing the default again.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateNotificationPreferencesRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/NotificationPreferenceList'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Change notification settings
            tags:
                - account
    /notifications:
        get:
            description: |
                Newest first, cursor only. The table is chunked by time, so a cursor is what lets the reader stop at a chunk boundary instead of touching every chunk.
            parameters:
                - in: query
                  name: category
                  schema:
                    $ref: '#/components/schemas/NotificationCategory'
                - description: Restrict to unread ones.
                  in: query
                  name: unread
                  schema:
                    type: boolean
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/NotificationPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The caller's notification feed
            tags:
                - account
    /notifications/read:
        post:
            description: |
                Takes a time bound rather than a list of ids. Marking individual rows by id would have to search every chunk of a time-partitioned table; a bound reads one range. Omit `before` to mark everything read.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MarkNotificationsReadRequest'
                required: false
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/UnreadCount'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Mark notifications read up to a point in time
            tags:
                - account
    /notifications/unread-count:
        get:
            description: A separate endpoint because it is read far more often than the feed and answers from one partial index.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/UnreadCount'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: The unread badge
            tags:
                - account
    /offers:
        get:
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - buyer
                        - seller
                    type: string
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/OfferStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OfferPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List negotiations, as buyer or as seller
            tags:
                - order
        post:
            description: |
                The buyer opens it, from the listing page: a seller has nobody to propose to on their own listing, so they answer rather than start. One active negotiation per (buyer, variant); the terms are then revised in place rather than by stacking rows, and each revision posts a card into the pair's chat thread.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateOfferRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Offer'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A seller cannot open a negotiation on their own listing
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An active negotiation already exists for this (buyer, SKU)
            security:
                - bearerAuth: []
            summary: Open a price negotiation on a variant
            tags:
                - order
    /offers/{id}:
        delete:
            responses:
                "204":
                    description: Cancelled
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Negotiation is no longer active
            security:
                - bearerAuth: []
            summary: Cancel a negotiation
            tags:
                - order
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Offer'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a negotiation
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/OfferID'
        patch:
            description: |
                Revises the current terms in place, moves authorship to the caller and posts the new card into the thread. Only the party that does not own the standing proposal may counter, so the two sides alternate.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateOfferRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Offer'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller already owns the standing proposal, or is not a party
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Negotiation is no longer active
            security:
                - bearerAuth: []
            summary: Counter-offer
            tags:
                - order
    /offers/{id}/acceptance:
        post:
            description: |
                Whoever does *not* own the standing proposal — the two sides alternate, so the price in front of you is always the other party's, and either of them may be the one who says yes.

                Agreeing is not the sale, and nothing is charged. It freezes the price and starts a short window for the buyer to press "create order now" (`POST /offers/{id}/checkout`), where they choose delivery and pay — exactly as they would from a fixed-price listing. That separation is what makes a seller accepting a buyer's price safe: no order exists and no money moves until the buyer checks out.

                The window is short on purpose. An agreed price is a frozen price, and a buyer who lets it lapse negotiates again rather than holding yesterday's number open.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OfferID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Offer'
                                required:
                                    - data
                                type: object
                    description: Agreed; the price is frozen for a short window
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: These are your own terms — the other party answers them
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Negotiation is expired, cancelled, already agreed, or being paid for
            security:
                - bearerAuth: []
            summary: Agree to the terms on the table
            tags:
                - order
    /offers/{id}/checkout:
        post:
            description: |
                The buyer turning an agreed price into an order: they pick a carrier and an address here and pay, in exactly the checkout a fixed-price listing uses. Only the buyer, because only the buyer pays — and only once, so a double-clicked button opens one payment session.

                The negotiated total covers the goods. Delivery is quoted from the carrier on top of it and is also the buyer's, the same as on a fixed-price sale; `POST /shipping-quotes` is how the options and their fees are listed before choosing.

                The order follows when the session completes, with `offer_id` set to this negotiation and no draft.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OfferID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CheckoutOfferRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/CheckoutResult'
                                required:
                                    - data
                                type: object
                    description: Item created and a payment session opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Only the buyer checks out agreed terms
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Terms are not agreed, have lapsed, or are already checked out
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A carrier nobody enabled, or a variant that is no longer sellable
                "502":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No carrier could price the delivery
            security:
                - bearerAuth: []
            summary: Create the order now, on agreed terms (buyer)
            tags:
                - order
    /orders:
        get:
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - buyer
                        - seller
                    type: string
                - in: query
                  name: state
                  schema:
                    $ref: '#/components/schemas/OrderState'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/OrderPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List orders, as buyer or as seller
            tags:
                - order
    /orders/{id}:
        get:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Order'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read an order
            tags:
                - order
    /orders/{id}/cancellation:
        post:
            description: |
                Either party may cancel while the shipment has not left `pending`. The escrow goes back to the buyer and the sale is reversed in the catalog. No body: there is nowhere to record a reason, and a field a client fills in that reaches nobody is worse than none.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Order'
                                required:
                                    - data
                                type: object
                    description: Cancelled
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Shipment already moved, or the order is closed
            security:
                - bearerAuth: []
            summary: Cancel an order
            tags:
                - order
    /orders/{id}/receipt:
        post:
            description: |
                The buyer confirms delivery and attaches unboxing evidence — at least one photo or video is mandatory, because this evidence is what a later refund or dispute is judged on. Sets `received_at` and starts the 72-hour payout countdown in `payout_deadline_at`.

                Confirming twice is a conflict rather than a no-op: a retried upload must not be able to restart the clock and move the payout date.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ConfirmReceiptRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Order'
                                required:
                                    - data
                                type: object
                    description: Receipt recorded
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the buyer of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already confirmed, or the order is cancelled
            security:
                - bearerAuth: []
            summary: Confirm receipt of the goods (buyer)
            tags:
                - order
    /orders/{id}/refunds:
        post:
            description: |
                Covers the whole order; there is no way to refund part of one, and one live refund per order. Evidence is optional here and can be added until the case closes.

                Nothing ships yet — the buyer keeps the goods until the refund is granted. The seller has until `deadline_at` to accept or reject, and letting that pass hands the next move to the buyer rather than settling anything.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateRefundRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Refund'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the buyer of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A refund is already open on this order, or its escrow has already been paid out or returned
            security:
                - bearerAuth: []
            summary: Open a refund request (buyer)
            tags:
                - order
    /orders/{id}/transport:
        get:
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Transport'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read the shipment
            tags:
                - order
    /orders/{id}/transport/checkpoints:
        post:
            description: |
                The seller's route — they are the one who hands the parcel over — and a moderator may correct it; nobody else writes this status.

                Forward-only. Carrier reports arrive out of order, and whether the parcel has left is what decides whether the order can still be cancelled and the escrow taken back, so it is not a fact a later report may undo: a checkpoint at or behind where the shipment already is, or one on a leg that has ended, is 409.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/OrderID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/TransportCheckpointRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Transport'
                                required:
                                    - data
                                type: object
                    description: Recorded
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the seller of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The shipment is already at or past that point
                "422":
                    $ref: '#/components/responses/UnprocessableEntity'
            security:
                - bearerAuth: []
            summary: Report a carrier checkpoint on the outbound leg (seller)
            tags:
                - order
    /orders/{orderID}/feedback:
        get:
            description: |
                Returns the caller's own submission always. The counterparty's arrives as `theirs` only once it is published; while it is still blind `theirs` is null and `theirs_submitted` says whether there is anything waiting, which is as much as can be told without breaking blindness.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/OrderFeedback'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read the feedback on one order
            tags:
                - trust
        parameters:
            - in: path
              name: orderID
              required: true
              schema:
                $ref: '#/components/schemas/OrderID'
        post:
            description: |
                One submission per direction per order, so this cannot be revised — a rating that could be edited after seeing the other side is not blind.

                The direction is derived from which side of the order the caller is on rather than sent, so nobody can file feedback as the other party.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitFeedbackRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Feedback'
                                required:
                                    - data
                                type: object
                    description: Submitted
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not a party to this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already submitted for this direction
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The order is not finished yet
            security:
                - bearerAuth: []
            summary: Rate the counterparty of a completed order
            tags:
                - trust
    /orders/uploads:
        post:
            description: |
                The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /orders/uploads/{id}/confirmation` makes the row real once the object is there — so a receipt confirmation or a refund can never be judged on a photo whose bytes never arrived.

                `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateUploadRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/UploadSlot'
                                required:
                                    - data
                                type: object
                    description: Reserved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Larger than this platform accepts
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: That file type is not accepted
            security:
                - bearerAuth: []
            summary: Reserve a slot to upload evidence into
            tags:
                - order
    /orders/uploads/{id}/confirmation:
        post:
            description: |
                Until this lands the resource resolves to nothing, so a half-finished upload cannot be named as evidence on a receipt confirmation or a refund. The recorded size is the store's, not the one declared when the slot was reserved.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ResourceID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Resource'
                                required:
                                    - data
                                type: object
                    description: Confirmed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No such reserved slot of the caller's
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Nothing was uploaded to that slot
            security:
                - bearerAuth: []
            summary: Confirm the bytes are at the store
            tags:
                - order
    /password:
        put:
            description: Requires the current password even though the caller is already authenticated — a stolen access token must not be enough to take the account.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ChangePasswordRequest'
                required: true
            responses:
                "204":
                    description: Changed and every other session revoked
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Current password is wrong
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This account signs in through a provider and has no password
            security:
                - bearerAuth: []
            summary: Change the caller's password
            tags:
                - account
    /password/reset-requests:
        post:
            description: |
                Always answers 202, whether or not the identifier exists. Reporting that an address is unknown turns this endpoint into a way to enumerate accounts.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PasswordResetRequest'
                required: true
            responses:
                "202":
                    description: If the identifier exists a reset message was sent
                "400":
                    $ref: '#/components/responses/BadRequest'
                "429":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Too many requests for this identifier
            summary: Ask for a password reset
            tags:
                - account
    /password/resets:
        post:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/PasswordResetConfirmRequest'
                required: true
            responses:
                "204":
                    description: Password set and every session revoked
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Token is unknown or already used or expired
            summary: Set a new password with a reset token
            tags:
                - account
    /payment-sessions:
        get:
            description: '`role=payer` is money owed or sent, `role=payee` is money received — seller payouts and refunds land on that side.'
            parameters:
                - description: Omit for both sides.
                  in: query
                  name: role
                  schema:
                    enum:
                        - payer
                        - payee
                    type: string
                - in: query
                  name: kind
                  schema:
                    $ref: '#/components/schemas/PaymentSessionKind'
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PaymentSessionPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's payment sessions
            tags:
                - finance
    /payment-sessions/{id}:
        get:
            description: |
                `outstanding` is the total less what has already settled on a rail, so a client can tell a partly-tendered session from an unpaid one without fetching the legs.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/PaymentSession'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a payment session
            tags:
                - finance
    /payment-sessions/{id}/cancellation:
        post:
            description: Only while nothing has settled. A session with a settled leg is closed by refunding, not by cancelling.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/PaymentSession'
                                required:
                                    - data
                                type: object
                    description: Cancelled
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A leg has already settled
            security:
                - bearerAuth: []
            summary: Abandon an unpaid payment session
            tags:
                - finance
    /payment-sessions/{id}/payments:
        post:
            description: |
                Creates a ledger leg on the chosen rail and returns it with the gateway redirect in `checkout_url`. Omit `amount` to tender the whole outstanding balance; pass it to split the session across several rails, calling this again per rail until the total is covered.

                The leg starts `pending` and only the provider's webhook settles it — this response is not a receipt. Poll the session or the leg for the outcome.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StartPaymentRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Transaction'
                                required:
                                    - data
                                type: object
                    description: Leg created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the payer of this session
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Session or payment option not found
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Session is already settled, cancelled, or expired
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Amount exceeds the session's outstanding balance
            security:
                - bearerAuth: []
            summary: Pay a session through one rail
            tags:
                - finance
    /payment-sessions/{id}/transactions:
        get:
            description: Append-only. A reversal is a new leg with a negative amount pointing at the leg it reverses, never an edit of the original.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/PaymentSessionID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TransactionList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: List the legs of a payment session
            tags:
                - finance
    /refunds:
        get:
            description: '`role=seller` resolves through the order''s seller; a refund row itself only records the buyer.'
            parameters:
                - in: query
                  name: role
                  required: true
                  schema:
                    enum:
                        - buyer
                        - seller
                    type: string
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/RefundStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RefundPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List refunds, as buyer or as seller
            tags:
                - order
    /refunds/{id}:
        delete:
            description: |
                Only while the seller has not decided. Once anything has moved — a decision, a moderator, a parcel — the case is closed by ruling, not by walking away from it.

                The case ends as `cancelled`, which gives the escrow back up: the payout becomes the seller's again.
            responses:
                "204":
                    description: Withdrawn
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The seller has already decided, or the case has moved on
            security:
                - bearerAuth: []
            summary: Withdraw a refund request (buyer)
            tags:
                - order
        get:
            description: |
                The case: `deadline_at` for whoever is on the clock, and the facts each stage recorded. The rounds a moderator has seen are the queue's to list — a party sees the verdict in the status the case ended up in.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Refund'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a refund and its case
            tags:
                - order
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/RefundID'
    /refunds/{id}/acceptance:
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/RefundID'
        post:
            description: |
                Grants it without a moderator. The goods have to come back first, so this books the return shipment on the same carrier as the outbound leg and moves the refund to `returning`; the money goes back once that leg is delivered and the seller has not appealed it. No body — there is nothing left to choose.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Refund'
                                required:
                                    - data
                                type: object
                    description: Accepted; the return shipment is booked
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the seller of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Refund is not awaiting seller review
            security:
                - bearerAuth: []
            summary: Accept a refund (seller)
            tags:
                - order
    /refunds/{id}/attachments:
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/RefundID'
        post:
            description: Either party to the order, while the case is open. A closed one is the record a verdict was reached on, so nothing is added to it afterwards.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AddAttachmentsRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Refund'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The case is closed, or the caller has no open round to attach to
            security:
                - bearerAuth: []
            summary: Add evidence to a refund case
            tags:
                - order
    /refunds/{id}/dispute:
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/RefundID'
        post:
            description: |
                Both escalations are this one call, because both are the same act — asking a moderator to decide — and which round it opens follows from who the caller is and where the refund stands.

                The **buyer** may escalate once the seller has rejected the request or has let the review window pass. That is round 1: ruling for the buyer grants the refund and books the return, ruling for the seller ends it.

                The **seller** may escalate what came back, while the refund is `returned` and the inspection window is open — a counterfeit, the wrong item, an empty box. That is round 2 on the same case: upholding the appeal ends the refund and pays the seller out, denying it releases the money to the buyer.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/OpenDisputeRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/RefundDispute'
                                required:
                                    - data
                                type: object
                    description: A round was opened
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not a party to this order, or not the party whose move it is
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Not escalatable from this state, a round is already open, or the window has closed
            security:
                - bearerAuth: []
            summary: Escalate the case to a moderator
            tags:
                - order
    /refunds/{id}/rejection:
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/RefundID'
        post:
            description: |
                Refusing costs a reason, and it does not close the case: the next move is the buyer's, who may escalate to a moderator until the refund lapses. A seller who simply lets the review window pass ends up in the same place without one.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RejectRefundRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Refund'
                                required:
                                    - data
                                type: object
                    description: Rejected; the buyer may now escalate
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Caller is not the seller of this order
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Refund is not awaiting seller review
            security:
                - bearerAuth: []
            summary: Reject a refund (seller)
            tags:
                - order
    /refunds/{id}/return-transport/checkpoints:
        post:
            description: |
                The return leg exists only from the moment a refund is granted, and `delivered` is what moves the case to `returned` and opens the seller's appeal window. It is the only exit from `returning`.

                Either party to the order may report it. The buyer posted the parcel and the seller received it, and requiring the seller alone would let one who simply never confirms strand the escrow with nobody on a clock — which is what having no writer at all did. A buyer who claims a delivery that did not happen is answered by round two of the dispute, which is exactly what that window is for.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/RefundID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/TransportCheckpointRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Refund'
                                required:
                                    - data
                                type: object
                    description: Recorded; the refund moves to `returned` once the leg is delivered
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No return leg yet, the leg is already past that point, or the order is closed
                "422":
                    $ref: '#/components/responses/UnprocessableEntity'
            security:
                - bearerAuth: []
            summary: Report a checkpoint on the leg carrying the goods back
            tags:
                - order
    /register:
        post:
            description: |
                At least one of email, phone or username is required — an account nobody can be addressed by cannot sign in. The display profile is created in the same transaction, so an account without a name is never a reachable state.

                Always creates a plain user. Moderators come from `POST /admin/moderators`.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AuthResult'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Email or phone or username already taken
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No identifier supplied
            summary: Create an account
            tags:
                - account
    /reports:
        get:
            description: A reporter sees the status of their own reports but not who else reported the same target.
            parameters:
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/ReportStatus'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReportPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: Reports the caller filed
            tags:
                - trust
        post:
            description: |
                A listing, an account, a message, a review or a reply. `ref_id` is the opaque id of that thing and its prefix has to agree with `ref_type`.

                One open report per target per reporter, so re-reporting while the first is still being looked at is a conflict rather than a second row. Enough distinct reports against one listing move it into the moderation queue on their own.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitReportRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Report'
                                required:
                                    - data
                                type: object
                    description: Filed
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The body is invalid, or `ref_id` does not decode as the declared `ref_type`
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The reported thing does not exist
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: You already have an open report against this target
            security:
                - bearerAuth: []
            summary: Report something
            tags:
                - trust
    /review-replies/{id}:
        delete:
            description: The author, or a moderator acting on an upheld report.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReviewReplyID'
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Delete a reply
            tags:
                - trust
    /reviews/{id}:
        delete:
            description: |
                The author, or a moderator acting on an upheld report. Removal drops it from the seller's rating too, so the recomputed reputation follows.
            responses:
                "204":
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Delete a review
            tags:
                - trust
        get:
            description: Carries the whole reply thread, unlike the listing page, which caps it.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Review'
                                required:
                                    - data
                                type: object
                    description: OK
                "404":
                    $ref: '#/components/responses/NotFound'
            summary: Read a review
            tags:
                - trust
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ReviewID'
        patch:
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateReviewRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Review'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Edit your own review
            tags:
                - trust
    /reviews/{id}/replies:
        post:
            description: |
                Mainly the seller answering, though anyone may add to the thread. There is no per-thread limit — a conversation that stops after one reply each is not a conversation.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ReviewID'
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SubmitReviewReplyRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ReviewReply'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Reply to a review
            tags:
                - trust
    /reviews/{id}/vote:
        delete:
            description: |
                Removes the row rather than storing a neutral one. A stored zero is a row that says nothing.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ReviewVoteTally'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Withdraw your vote
            tags:
                - trust
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/ReviewID'
        put:
            description: One vote per account per review, replaced in place. Voting on your own review is refused.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/VoteReviewRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ReviewVoteTally'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    $ref: '#/components/responses/NotFound'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: An account cannot vote on its own review
            security:
                - bearerAuth: []
            summary: Vote a review helpful or not
            tags:
                - trust
    /reviews/uploads:
        post:
            description: |
                The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /reviews/uploads/{id}/confirmation` makes the row real once the object is there — so a review can never render a photo whose bytes never arrived.

                `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateUploadRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/UploadSlot'
                                required:
                                    - data
                                type: object
                    description: Reserved
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "413":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Larger than this platform accepts
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: That file type is not accepted
            security:
                - bearerAuth: []
            summary: Reserve a slot to upload a review photo into
            tags:
                - trust
    /reviews/uploads/{id}/confirmation:
        post:
            description: |
                Until this lands the resource resolves to nothing, so a half-finished upload cannot be attached to a review. The recorded size is the store's, not the one declared when the slot was reserved.
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                    $ref: '#/components/schemas/ResourceID'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Resource'
                                required:
                                    - data
                                type: object
                    description: Confirmed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No such reserved slot of the caller's
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Nothing was uploaded to that slot
            security:
                - bearerAuth: []
            summary: Confirm the bytes are at the store
            tags:
                - trust
    /shipping-quotes:
        post:
            description: |
                One entry per enabled carrier that could price the parcel, so the buyer chooses delivery with the fee in front of them. A carrier that declined is simply absent.

                Name exactly one of `draft_id` or `offer_id`: they are the two things that freeze a price, and the parcel is whichever of them is about to be checked out. The same list serves both, because the buyer pays carriage on a fixed-price sale and a negotiated one alike.

                An estimate a client renders, not a price it can hold: the fee is quoted again at checkout, so a quote kept past the point it was true does not become the charge.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ShippingQuotesRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ShippingQuotes'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Only the buyer prices delivery on agreed terms
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: The draft or the agreed terms are no longer checkout-able
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A variant that is not on this purchase
            security:
                - bearerAuth: []
            summary: What delivery would cost, per carrier
            tags:
                - order
    /tags:
        get:
            description: |
                A tag id is its slug and a natural key, so it is readable on the wire rather than encoded.

                Paginated, unlike the category tree: categories are a curated tree that stays small, while tags grow with the catalog. `q` is a prefix match on the slug, which is what the tag picker types into.

                `near` is the other half of that picker: it ranks the dictionary by cosine distance over `tag_embedding` and returns a `score`, so picking `handmade` offers back the tags that sit next to it in the vector space. The seeds themselves are never in the result — they are already on the listing — and a tag the embedding cron has not reached yet cannot be suggested at all, because `tag` carries no text index to fall back to.

                `q` and `near` are mutually exclusive: one filters the dictionary by what was typed, the other reorders it by meaning, and combining them would rank a set the prefix already decided.
            parameters:
                - description: Prefix match on the slug.
                  in: query
                  name: q
                  schema:
                    maxLength: 100
                    type: string
                - description: Rank by closeness to these seeds. Excludes the seeds.
                  explode: false
                  in: query
                  name: near
                  schema:
                    items:
                        $ref: '#/components/schemas/SemanticSeed'
                    maxItems: 8
                    minItems: 1
                    type: array
                  style: form
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TagPage'
                    description: OK
                "400":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: '`q` and `near` together, or a malformed seed'
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: A `near` seed has no embedding yet
            summary: The tag dictionary
            tags:
                - catalog
    /tax-info:
        get:
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/TaxInfo'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Nothing registered yet
            security:
                - bearerAuth: []
            summary: Read the caller's tax registration
            tags:
                - finance
        put:
            description: |
                One registration per account, so this is an upsert rather than a create. Replacing it resets verification to `pending`: the previous verdict was about the previous tax code. A code already verified under another account is rejected.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpsertTaxInfoRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/TaxInfo'
                                required:
                                    - data
                                type: object
                    description: Registered
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This tax code is already verified under another account
            security:
                - bearerAuth: []
            summary: Register or replace the caller's tax registration
            tags:
                - finance
    /token/refresh:
        post:
            description: The refresh token is a Redis key. One that has been revoked or has expired is indistinguishable from one that never existed.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RefreshRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/AuthResult'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Unknown or revoked refresh token
            summary: Exchange a refresh token for a new access token
            tags:
                - account
    /variants/{id}:
        delete:
            description: |
                Soft delete for the same reason as a listing. Refused for the last remaining variant of a live listing.

                Answers with the listing rather than an empty body: deleting the featured variant moves the flag to a surviving one, and the seller has to see which.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: Deleted
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: This is the only variant of a live listing
            security:
                - bearerAuth: []
            summary: Delete a variant
            tags:
                - catalog
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/VariantID'
        patch:
            description: |
                A price change does not reach a buyer who already opened a purchase session: that session froze the terms it was opened with.

                `quantity` sets the total on hand. Neither `reserved` nor `sold` is settable — checkout and cancellation move the first, completing a purchase moves both — and a total below `reserved + sold` is refused, because an oversold row must not be representable.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateVariantRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/ListingDetail'
                                required:
                                    - data
                                type: object
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Another live variant of this listing already has these attributes
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Quantity below what is already reserved or sold
            security:
                - bearerAuth: []
            summary: Edit a variant
            tags:
                - catalog
    /wallets:
        get:
            description: |
                One wallet per currency, so this is a list rather than a single object. It is short and unpaginated: a balance is opened by receiving money in that currency, not by browsing.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletList'
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's wallet balances
            tags:
                - finance
    /wallets/{currency}:
        get:
            parameters:
                - in: path
                  name: currency
                  required: true
                  schema:
                    $ref: '#/components/schemas/CurrencyCode'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Wallet'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No balance has been opened in this currency
            security:
                - bearerAuth: []
            summary: Read one currency balance
            tags:
                - finance
    /wallets/{currency}/transactions:
        get:
            description: |
                Every balance change with its before/after snapshot, newest first. Ordered by `seq`, not by time: two movements can share a timestamp, and a ledger that cannot be totally ordered cannot be replayed or shown to be complete. A gap in `seq` means a missing row.

                Legs of one logical movement share a `group_id` — a checkout is a debit plus an escrow hold plus a fee — which is what makes "does this add up" answerable.
            parameters:
                - in: path
                  name: currency
                  required: true
                  schema:
                    $ref: '#/components/schemas/CurrencyCode'
                - in: query
                  name: kind
                  schema:
                    $ref: '#/components/schemas/WalletTransactionKind'
                - $ref: '#/components/parameters/Cursor'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WalletTransactionPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: No balance has been opened in this currency
            security:
                - bearerAuth: []
            summary: Read one wallet's ledger
            tags:
                - finance
    /withdrawals:
        get:
            parameters:
                - in: query
                  name: status
                  schema:
                    $ref: '#/components/schemas/PaymentSessionStatus'
                - $ref: '#/components/parameters/Page'
                - $ref: '#/components/parameters/Limit'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/WithdrawalPage'
                    description: OK
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security:
                - bearerAuth: []
            summary: List the caller's withdrawals
            tags:
                - finance
        post:
            description: |
                Debits the available balance and opens a withdrawal for review; an admin resolves it before money leaves the platform. Only the available balance can be drawn — money still held in escrow is not the caller's yet.
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateWithdrawalRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Withdrawal'
                                required:
                                    - data
                                type: object
                    description: Created
                "400":
                    $ref: '#/components/responses/BadRequest'
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Bank account or currency balance not found
                "422":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Insufficient available balance, or the payee identity is unverified
            security:
                - bearerAuth: []
            summary: Request a cash-out to a bank account
            tags:
                - finance
    /withdrawals/{id}:
        delete:
            responses:
                "204":
                    description: Cancelled and the debit reversed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
                "409":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Already resolved by an admin
            security:
                - bearerAuth: []
            summary: Withdraw the request and return the money to the balance
            tags:
                - finance
        get:
            description: A withdrawal is a payment session of kind `withdrawal`, so its id carries the session prefix.
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    data:
                                        $ref: '#/components/schemas/Withdrawal'
                                required:
                                    - data
                                type: object
                    description: OK
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "404":
                    $ref: '#/components/responses/NotFound'
            security:
                - bearerAuth: []
            summary: Read a withdrawal
            tags:
                - finance
        parameters:
            - in: path
              name: id
              required: true
              schema:
                $ref: '#/components/schemas/PaymentSessionID'
servers:
    - description: Gateway (relative to host)
      url: /api/v1
tags:
    - name: account
    - name: catalog
    - name: order
    - name: finance
    - name: chat
    - name: trust
    - name: common
    - description: Moderator and admin console surface. Separate from the user-facing routes because the same URL must never return two different populations depending on the caller's role.
      name: admin