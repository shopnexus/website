// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { CreateWebSocketTicketData, CreateWebSocketTicketErrors, CreateWebSocketTicketResponses, DeleteAdminAccountsByIdSuspensionData, DeleteAdminAccountsByIdSuspensionErrors, DeleteAdminAccountsByIdSuspensionResponses, DeleteAdminCategoriesByIdData, DeleteAdminCategoriesByIdErrors, DeleteAdminCategoriesByIdResponses, DeleteAdminModeratorsByIdData, DeleteAdminModeratorsByIdErrors, DeleteAdminModeratorsByIdResponses, DeleteAdminTagsBySlugData, DeleteAdminTagsBySlugErrors, DeleteAdminTagsBySlugResponses, DeleteBankAccountsByIdData, DeleteBankAccountsByIdErrors, DeleteBankAccountsByIdResponses, DeleteCartItemsByIdData, DeleteCartItemsByIdErrors, DeleteCartItemsByIdResponses, DeleteContactsByIdData, DeleteContactsByIdErrors, DeleteContactsByIdResponses, DeleteDevicesByIdData, DeleteDevicesByIdErrors, DeleteDevicesByIdResponses, DeleteDraftsByIdData, DeleteDraftsByIdErrors, DeleteDraftsByIdResponses, DeleteFavoritesByListingIdData, DeleteFavoritesByListingIdErrors, DeleteFavoritesByListingIdResponses, DeleteFollowsByAccountIdData, DeleteFollowsByAccountIdErrors, DeleteFollowsByAccountIdResponses, DeleteListingsByIdData, DeleteListingsByIdErrors, DeleteListingsByIdPublicationData, DeleteListingsByIdPublicationErrors, DeleteListingsByIdPublicationResponses, DeleteListingsByIdResponses, DeleteMeOauthIdentitiesByProviderData, DeleteMeOauthIdentitiesByProviderErrors, DeleteMeOauthIdentitiesByProviderResponses, DeleteMessagesByIdData, DeleteMessagesByIdErrors, DeleteMessagesByIdResponses, DeleteOffersByIdData, DeleteOffersByIdErrors, DeleteOffersByIdResponses, DeleteRefundsByIdData, DeleteRefundsByIdErrors, DeleteRefundsByIdResponses, DeleteReviewRepliesByIdData, DeleteReviewRepliesByIdErrors, DeleteReviewRepliesByIdResponses, DeleteReviewsByIdData, DeleteReviewsByIdErrors, DeleteReviewsByIdResponses, DeleteReviewsByIdVoteData, DeleteReviewsByIdVoteErrors, DeleteReviewsByIdVoteResponses, DeleteVariantsByIdData, DeleteVariantsByIdErrors, DeleteVariantsByIdResponses, DeleteWithdrawalsByIdData, DeleteWithdrawalsByIdErrors, DeleteWithdrawalsByIdResponses, GetAccountsByAccountIdFeedbackData, GetAccountsByAccountIdFeedbackErrors, GetAccountsByAccountIdFeedbackResponses, GetAccountsByAccountIdReputationData, GetAccountsByAccountIdReputationErrors, GetAccountsByAccountIdReputationResponses, GetAccountsByIdData, GetAccountsByIdErrors, GetAccountsByIdFollowersData, GetAccountsByIdFollowersErrors, GetAccountsByIdFollowersResponses, GetAccountsByIdResponses, GetAdminAccountsData, GetAdminAccountsErrors, GetAdminAccountsResponses, GetAdminIdentityDocumentsData, GetAdminIdentityDocumentsErrors, GetAdminIdentityDocumentsResponses, GetAdministrativeAreasData, GetAdministrativeAreasErrors, GetAdministrativeAreasResponses, GetAdminListingsData, GetAdminListingsErrors, GetAdminListingsResponses, GetAdminOptionsData, GetAdminOptionsErrors, GetAdminOptionsResponses, GetAdminPaymentSessionsData, GetAdminPaymentSessionsErrors, GetAdminPaymentSessionsResponses, GetAdminTicketsByIdData, GetAdminTicketsByIdErrors, GetAdminTicketsByIdResponses, GetAdminTicketsData, GetAdminTicketsErrors, GetAdminTicketsResponses, GetAdminWalletsByAccountIdData, GetAdminWalletsByAccountIdErrors, GetAdminWalletsByAccountIdResponses, GetAdminWithdrawalsData, GetAdminWithdrawalsErrors, GetAdminWithdrawalsResponses, GetBankAccountsData, GetBankAccountsErrors, GetBankAccountsResponses, GetCartItemsData, GetCartItemsErrors, GetCartItemsResponses, GetCategoriesData, GetCategoriesErrors, GetCategoriesResponses, GetContactsData, GetContactsErrors, GetContactsResponses, GetConversationsByIdData, GetConversationsByIdErrors, GetConversationsByIdMessagesData, GetConversationsByIdMessagesErrors, GetConversationsByIdMessagesResponses, GetConversationsByIdResponses, GetConversationsData, GetConversationsErrors, GetConversationsResponses, GetConversationsUnreadCountData, GetConversationsUnreadCountErrors, GetConversationsUnreadCountResponses, GetDraftsByIdData, GetDraftsByIdErrors, GetDraftsByIdResponses, GetDraftsData, GetDraftsErrors, GetDraftsResponses, GetItemsData, GetItemsErrors, GetItemsResponses, GetListingsByIdData, GetListingsByIdErrors, GetListingsByIdResponses, GetListingsByListingIdReviewsData, GetListingsByListingIdReviewsErrors, GetListingsByListingIdReviewsResponses, GetListingsData, GetListingsErrors, GetListingsResponses, GetMeData, GetMeDevicesData, GetMeDevicesErrors, GetMeDevicesResponses, GetMeErrors, GetMeFollowingData, GetMeFollowingErrors, GetMeFollowingResponses, GetMeIdentityDocumentsData, GetMeIdentityDocumentsErrors, GetMeIdentityDocumentsResponses, GetMeOauthIdentitiesData, GetMeOauthIdentitiesErrors, GetMeOauthIdentitiesResponses, GetMeResponses, GetNotificationPreferencesData, GetNotificationPreferencesErrors, GetNotificationPreferencesResponses, GetNotificationsData, GetNotificationsErrors, GetNotificationsResponses, GetNotificationsUnreadCountData, GetNotificationsUnreadCountErrors, GetNotificationsUnreadCountResponses, GetOffersByIdData, GetOffersByIdErrors, GetOffersByIdResponses, GetOffersData, GetOffersErrors, GetOffersResponses, GetOptionsData, GetOptionsErrors, GetOptionsResponses, GetOrdersByIdData, GetOrdersByIdErrors, GetOrdersByIdResponses, GetOrdersByIdTransportData, GetOrdersByIdTransportErrors, GetOrdersByIdTransportResponses, GetOrdersByOrderIdFeedbackData, GetOrdersByOrderIdFeedbackErrors, GetOrdersByOrderIdFeedbackResponses, GetOrdersData, GetOrdersErrors, GetOrdersResponses, GetOrdersSummaryData, GetOrdersSummaryErrors, GetOrdersSummaryResponses, GetPaymentSessionsByIdData, GetPaymentSessionsByIdErrors, GetPaymentSessionsByIdResponses, GetPaymentSessionsByIdTransactionsData, GetPaymentSessionsByIdTransactionsErrors, GetPaymentSessionsByIdTransactionsResponses, GetPaymentSessionsData, GetPaymentSessionsErrors, GetPaymentSessionsResponses, GetRefundsByIdData, GetRefundsByIdErrors, GetRefundsByIdResponses, GetRefundsData, GetRefundsErrors, GetRefundsResponses, GetReviewsByIdData, GetReviewsByIdErrors, GetReviewsByIdResponses, GetTagsData, GetTagsErrors, GetTagsResponses, GetTaxInfoData, GetTaxInfoErrors, GetTaxInfoResponses, GetTicketsByIdData, GetTicketsByIdErrors, GetTicketsByIdResponses, GetTicketsData, GetTicketsErrors, GetTicketsResponses, GetWalletsByCurrencyData, GetWalletsByCurrencyErrors, GetWalletsByCurrencyResponses, GetWalletsByCurrencyTransactionsData, GetWalletsByCurrencyTransactionsErrors, GetWalletsByCurrencyTransactionsResponses, GetWalletsData, GetWalletsErrors, GetWalletsResponses, GetWithdrawalsByIdData, GetWithdrawalsByIdErrors, GetWithdrawalsByIdResponses, GetWithdrawalsData, GetWithdrawalsErrors, GetWithdrawalsResponses, PatchAdminCategoriesByIdData, PatchAdminCategoriesByIdErrors, PatchAdminCategoriesByIdResponses, PatchAdminOptionsByIdData, PatchAdminOptionsByIdErrors, PatchAdminOptionsByIdResponses, PatchBankAccountsByIdData, PatchBankAccountsByIdErrors, PatchBankAccountsByIdResponses, PatchCartItemsByIdData, PatchCartItemsByIdErrors, PatchCartItemsByIdResponses, PatchContactsByIdData, PatchContactsByIdErrors, PatchContactsByIdResponses, PatchListingsByIdData, PatchListingsByIdErrors, PatchListingsByIdResponses, PatchMeData, PatchMeErrors, PatchMeProfileData, PatchMeProfileErrors, PatchMeProfileResponses, PatchMeResponses, PatchMessagesByIdData, PatchMessagesByIdErrors, PatchMessagesByIdResponses, PatchOffersByIdData, PatchOffersByIdErrors, PatchOffersByIdResponses, PatchReviewsByIdData, PatchReviewsByIdErrors, PatchReviewsByIdResponses, PatchVariantsByIdData, PatchVariantsByIdErrors, PatchVariantsByIdResponses, PostAdminAccountsByIdSuspensionData, PostAdminAccountsByIdSuspensionErrors, PostAdminAccountsByIdSuspensionResponses, PostAdminCategoriesData, PostAdminCategoriesErrors, PostAdminCategoriesResponses, PostAdminIdentityDocumentsByIdVerdictData, PostAdminIdentityDocumentsByIdVerdictErrors, PostAdminIdentityDocumentsByIdVerdictResponses, PostAdminListingsByIdApprovalData, PostAdminListingsByIdApprovalErrors, PostAdminListingsByIdApprovalResponses, PostAdminListingsByIdTakedownData, PostAdminListingsByIdTakedownErrors, PostAdminListingsByIdTakedownResponses, PostAdminModeratorsData, PostAdminModeratorsErrors, PostAdminModeratorsResponses, PostAdminRefundsByIdVerdictData, PostAdminRefundsByIdVerdictErrors, PostAdminRefundsByIdVerdictResponses, PostAdminTaxInfoByAccountIdVerificationData, PostAdminTaxInfoByAccountIdVerificationErrors, PostAdminTaxInfoByAccountIdVerificationResponses, PostAdminTicketsByIdClaimData, PostAdminTicketsByIdClaimErrors, PostAdminTicketsByIdClaimResponses, PostAdminTicketsByIdResolutionData, PostAdminTicketsByIdResolutionErrors, PostAdminTicketsByIdResolutionResponses, PostAdminWalletsByAccountIdAdjustmentsData, PostAdminWalletsByAccountIdAdjustmentsErrors, PostAdminWalletsByAccountIdAdjustmentsResponses, PostAdminWithdrawalsByIdApprovalData, PostAdminWithdrawalsByIdApprovalErrors, PostAdminWithdrawalsByIdApprovalResponses, PostAdminWithdrawalsByIdRejectionData, PostAdminWithdrawalsByIdRejectionErrors, PostAdminWithdrawalsByIdRejectionResponses, PostBankAccountsData, PostBankAccountsErrors, PostBankAccountsResponses, PostCartItemsData, PostCartItemsErrors, PostCartItemsResponses, PostContactsByIdPhoneVerificationRequestsData, PostContactsByIdPhoneVerificationRequestsErrors, PostContactsByIdPhoneVerificationRequestsResponses, PostContactsByIdPhoneVerificationsData, PostContactsByIdPhoneVerificationsErrors, PostContactsByIdPhoneVerificationsResponses, PostContactsData, PostContactsErrors, PostContactsResponses, PostConversationsByIdMessagesData, PostConversationsByIdMessagesErrors, PostConversationsByIdMessagesResponses, PostConversationsByIdReadData, PostConversationsByIdReadErrors, PostConversationsByIdReadResponses, PostConversationsData, PostConversationsErrors, PostConversationsResponses, PostConversationsUploadsByIdConfirmationData, PostConversationsUploadsByIdConfirmationErrors, PostConversationsUploadsByIdConfirmationResponses, PostConversationsUploadsData, PostConversationsUploadsErrors, PostConversationsUploadsResponses, PostDraftsByIdCheckoutData, PostDraftsByIdCheckoutErrors, PostDraftsByIdCheckoutResponses, PostDraftsData, PostDraftsErrors, PostDraftsResponses, PostEmailVerificationRequestsData, PostEmailVerificationRequestsErrors, PostEmailVerificationRequestsResponses, PostEmailVerificationsData, PostEmailVerificationsErrors, PostEmailVerificationsResponses, PostIdentityDocumentsData, PostIdentityDocumentsErrors, PostIdentityDocumentsResponses, PostItemsByIdCancellationData, PostItemsByIdCancellationErrors, PostItemsByIdCancellationResponses, PostListingsByIdPublicationData, PostListingsByIdPublicationErrors, PostListingsByIdPublicationResponses, PostListingsByIdVariantsData, PostListingsByIdVariantsErrors, PostListingsByIdVariantsResponses, PostListingsByListingIdReviewsData, PostListingsByListingIdReviewsErrors, PostListingsByListingIdReviewsResponses, PostListingsData, PostListingsErrors, PostListingsResponses, PostListingsSuggestionsData, PostListingsSuggestionsErrors, PostListingsSuggestionsResponses, PostListingsUploadsByIdConfirmationData, PostListingsUploadsByIdConfirmationErrors, PostListingsUploadsByIdConfirmationResponses, PostListingsUploadsData, PostListingsUploadsErrors, PostListingsUploadsResponses, PostLoginData, PostLoginErrors, PostLoginOauthData, PostLoginOauthErrors, PostLoginOauthResponses, PostLoginResponses, PostLogoutData, PostLogoutErrors, PostLogoutResponses, PostMeUploadsByIdConfirmationData, PostMeUploadsByIdConfirmationErrors, PostMeUploadsByIdConfirmationResponses, PostMeUploadsData, PostMeUploadsErrors, PostMeUploadsResponses, PostNotificationsReadData, PostNotificationsReadErrors, PostNotificationsReadResponses, PostOffersByIdAcceptanceData, PostOffersByIdAcceptanceErrors, PostOffersByIdAcceptanceResponses, PostOffersByIdCheckoutData, PostOffersByIdCheckoutErrors, PostOffersByIdCheckoutResponses, PostOffersData, PostOffersErrors, PostOffersResponses, PostOrdersByIdCancellationData, PostOrdersByIdCancellationErrors, PostOrdersByIdCancellationResponses, PostOrdersByIdConfirmationData, PostOrdersByIdConfirmationErrors, PostOrdersByIdConfirmationResponses, PostOrdersByIdDeclineData, PostOrdersByIdDeclineErrors, PostOrdersByIdDeclineResponses, PostOrdersByIdReceiptData, PostOrdersByIdReceiptErrors, PostOrdersByIdReceiptResponses, PostOrdersByIdRefundsData, PostOrdersByIdRefundsErrors, PostOrdersByIdRefundsResponses, PostOrdersByIdTransportCheckpointsData, PostOrdersByIdTransportCheckpointsErrors, PostOrdersByIdTransportCheckpointsResponses, PostOrdersByOrderIdFeedbackData, PostOrdersByOrderIdFeedbackErrors, PostOrdersByOrderIdFeedbackResponses, PostOrdersUploadsByIdConfirmationData, PostOrdersUploadsByIdConfirmationErrors, PostOrdersUploadsByIdConfirmationResponses, PostOrdersUploadsData, PostOrdersUploadsErrors, PostOrdersUploadsResponses, PostPasswordResetRequestsData, PostPasswordResetRequestsErrors, PostPasswordResetRequestsResponses, PostPasswordResetsData, PostPasswordResetsErrors, PostPasswordResetsResponses, PostPaymentSessionsByIdCancellationData, PostPaymentSessionsByIdCancellationErrors, PostPaymentSessionsByIdCancellationResponses, PostPaymentSessionsByIdPaymentsData, PostPaymentSessionsByIdPaymentsErrors, PostPaymentSessionsByIdPaymentsResponses, PostRefundsByIdAcceptanceData, PostRefundsByIdAcceptanceErrors, PostRefundsByIdAcceptanceResponses, PostRefundsByIdAttachmentsData, PostRefundsByIdAttachmentsErrors, PostRefundsByIdAttachmentsResponses, PostRefundsByIdReturnTransportCheckpointsData, PostRefundsByIdReturnTransportCheckpointsErrors, PostRefundsByIdReturnTransportCheckpointsResponses, PostRegisterData, PostRegisterErrors, PostRegisterResponses, PostReviewsByIdRepliesData, PostReviewsByIdRepliesErrors, PostReviewsByIdRepliesResponses, PostReviewsUploadsByIdConfirmationData, PostReviewsUploadsByIdConfirmationErrors, PostReviewsUploadsByIdConfirmationResponses, PostReviewsUploadsData, PostReviewsUploadsErrors, PostReviewsUploadsResponses, PostShippingQuotesData, PostShippingQuotesErrors, PostShippingQuotesResponses, PostTicketsData, PostTicketsErrors, PostTicketsResponses, PostTokenRefreshData, PostTokenRefreshErrors, PostTokenRefreshResponses, PostWithdrawalsData, PostWithdrawalsErrors, PostWithdrawalsResponses, PutAdminTagsBySlugData, PutAdminTagsBySlugErrors, PutAdminTagsBySlugResponses, PutDevicesData, PutDevicesErrors, PutDevicesResponses, PutFavoritesByListingIdData, PutFavoritesByListingIdErrors, PutFavoritesByListingIdResponses, PutFollowsByAccountIdData, PutFollowsByAccountIdErrors, PutFollowsByAccountIdResponses, PutNotificationPreferencesData, PutNotificationPreferencesErrors, PutNotificationPreferencesResponses, PutPasswordData, PutPasswordErrors, PutPasswordResponses, PutReviewsByIdVoteData, PutReviewsByIdVoteErrors, PutReviewsByIdVoteResponses, PutTaxInfoData, PutTaxInfoErrors, PutTaxInfoResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Published feedback received by an account
 *
 * Published rows only. A blind submission is not visible to anyone but its author until the window closes.
 */
export const getAccountsByAccountIdFeedback = <ThrowOnError extends boolean = false>(options: Options<GetAccountsByAccountIdFeedbackData, ThrowOnError>): RequestResult<GetAccountsByAccountIdFeedbackResponses, GetAccountsByAccountIdFeedbackErrors, ThrowOnError> => (options.client ?? client).get<GetAccountsByAccountIdFeedbackResponses, GetAccountsByAccountIdFeedbackErrors, ThrowOnError>({ url: '/accounts/{accountID}/feedback', ...options });

/**
 * Read an account's reputation for one role
 *
 * Transaction ratings and product-review ratings are reported separately rather than merged. Only a seller can have review ratings.
 *
 */
export const getAccountsByAccountIdReputation = <ThrowOnError extends boolean = false>(options: Options<GetAccountsByAccountIdReputationData, ThrowOnError>): RequestResult<GetAccountsByAccountIdReputationResponses, GetAccountsByAccountIdReputationErrors, ThrowOnError> => (options.client ?? client).get<GetAccountsByAccountIdReputationResponses, GetAccountsByAccountIdReputationErrors, ThrowOnError>({ url: '/accounts/{accountID}/reputation', ...options });

/**
 * Read another account's public profile
 *
 * The seller page. Deliberately narrow: a display name, an avatar, a blurb and a join date. Email, phone, birth date and saved addresses are never part of it.
 *
 */
export const getAccountsById = <ThrowOnError extends boolean = false>(options: Options<GetAccountsByIdData, ThrowOnError>): RequestResult<GetAccountsByIdResponses, GetAccountsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetAccountsByIdResponses, GetAccountsByIdErrors, ThrowOnError>({ url: '/accounts/{id}', ...options });

/**
 * Who follows this seller
 */
export const getAccountsByIdFollowers = <ThrowOnError extends boolean = false>(options: Options<GetAccountsByIdFollowersData, ThrowOnError>): RequestResult<GetAccountsByIdFollowersResponses, GetAccountsByIdFollowersErrors, ThrowOnError> => (options.client ?? client).get<GetAccountsByIdFollowersResponses, GetAccountsByIdFollowersErrors, ThrowOnError>({ url: '/accounts/{id}/followers', ...options });

/**
 * Search accounts
 *
 * `q` is an **exact** match on email, phone or username — each is unique, so that is a key lookup and is how a moderator arrives here from a report — or a **fragment** match on the display name, which is answered by a trigram index. Whichever it is, it is one query; the caller does not choose.
 *
 */
export const getAdminAccounts = <ThrowOnError extends boolean = false>(options?: Options<GetAdminAccountsData, ThrowOnError>): RequestResult<GetAdminAccountsResponses, GetAdminAccountsErrors, ThrowOnError> => (options?.client ?? client).get<GetAdminAccountsResponses, GetAdminAccountsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/accounts',
    ...options
});

/**
 * Lift a suspension
 *
 * Clears the reason and the deadline with it. Past suspensions stay in the audit log rather than on the row.
 */
export const deleteAdminAccountsByIdSuspension = <ThrowOnError extends boolean = false>(options: Options<DeleteAdminAccountsByIdSuspensionData, ThrowOnError>): RequestResult<DeleteAdminAccountsByIdSuspensionResponses, DeleteAdminAccountsByIdSuspensionErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAdminAccountsByIdSuspensionResponses, DeleteAdminAccountsByIdSuspensionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/accounts/{id}/suspension',
    ...options
});

/**
 * Suspend an account
 *
 * The outcome of upholding a report against a seller. Omit `until` for a permanent suspension.
 *
 * Suspending also drops the account's sessions. A suspended row does not stop an access token that is already in circulation, so the two have to happen together.
 *
 */
export const postAdminAccountsByIdSuspension = <ThrowOnError extends boolean = false>(options: Options<PostAdminAccountsByIdSuspensionData, ThrowOnError>): RequestResult<PostAdminAccountsByIdSuspensionResponses, PostAdminAccountsByIdSuspensionErrors, ThrowOnError> => (options.client ?? client).post<PostAdminAccountsByIdSuspensionResponses, PostAdminAccountsByIdSuspensionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/accounts/{id}/suspension',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a category
 */
export const postAdminCategories = <ThrowOnError extends boolean = false>(options: Options<PostAdminCategoriesData, ThrowOnError>): RequestResult<PostAdminCategoriesResponses, PostAdminCategoriesErrors, ThrowOnError> => (options.client ?? client).post<PostAdminCategoriesResponses, PostAdminCategoriesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/categories',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a category
 *
 * Children are promoted to roots rather than deleted with it, so a branch is never lost by accident. A category still holding listings cannot be deleted at all — move them first.
 *
 */
export const deleteAdminCategoriesById = <ThrowOnError extends boolean = false>(options: Options<DeleteAdminCategoriesByIdData, ThrowOnError>): RequestResult<DeleteAdminCategoriesByIdResponses, DeleteAdminCategoriesByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAdminCategoriesByIdResponses, DeleteAdminCategoriesByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/categories/{id}',
    ...options
});

/**
 * Update a category
 */
export const patchAdminCategoriesById = <ThrowOnError extends boolean = false>(options: Options<PatchAdminCategoriesByIdData, ThrowOnError>): RequestResult<PatchAdminCategoriesByIdResponses, PatchAdminCategoriesByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchAdminCategoriesByIdResponses, PatchAdminCategoriesByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/categories/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The identity review queue
 */
export const getAdminIdentityDocuments = <ThrowOnError extends boolean = false>(options?: Options<GetAdminIdentityDocumentsData, ThrowOnError>): RequestResult<GetAdminIdentityDocumentsResponses, GetAdminIdentityDocumentsErrors, ThrowOnError> => (options?.client ?? client).get<GetAdminIdentityDocumentsResponses, GetAdminIdentityDocumentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/identity-documents',
    ...options
});

/**
 * Record an identity verdict
 *
 * `verified` requires `expires_at` when the document type carries one, because the payout gate reads the expiry and not only the status. `rejected` requires a reason.
 *
 */
export const postAdminIdentityDocumentsByIdVerdict = <ThrowOnError extends boolean = false>(options: Options<PostAdminIdentityDocumentsByIdVerdictData, ThrowOnError>): RequestResult<PostAdminIdentityDocumentsByIdVerdictResponses, PostAdminIdentityDocumentsByIdVerdictErrors, ThrowOnError> => (options.client ?? client).post<PostAdminIdentityDocumentsByIdVerdictResponses, PostAdminIdentityDocumentsByIdVerdictErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/identity-documents/{id}/verdict',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The listing moderation queue
 *
 * Every listing awaiting a decision, oldest first, which is the order they should be worked: one waiting for its first publication, and one that is live while holding an edit its seller submitted. Defaults to both.
 *
 */
export const getAdminListings = <ThrowOnError extends boolean = false>(options?: Options<GetAdminListingsData, ThrowOnError>): RequestResult<GetAdminListingsResponses, GetAdminListingsErrors, ThrowOnError> => (options?.client ?? client).get<GetAdminListingsResponses, GetAdminListingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/listings',
    ...options
});

/**
 * Clear a listing for publication
 *
 * Applies any held pending edit and makes it live.
 */
export const postAdminListingsByIdApproval = <ThrowOnError extends boolean = false>(options: Options<PostAdminListingsByIdApprovalData, ThrowOnError>): RequestResult<PostAdminListingsByIdApprovalResponses, PostAdminListingsByIdApprovalErrors, ThrowOnError> => (options.client ?? client).post<PostAdminListingsByIdApprovalResponses, PostAdminListingsByIdApprovalErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/listings/{id}/approval',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Take a listing down
 *
 * The moderator's verdict against a listing that breaks policy. Removes it from the marketplace and records the reason. Suspending the seller as well is a separate decision and a separate call.
 *
 */
export const postAdminListingsByIdTakedown = <ThrowOnError extends boolean = false>(options: Options<PostAdminListingsByIdTakedownData, ThrowOnError>): RequestResult<PostAdminListingsByIdTakedownResponses, PostAdminListingsByIdTakedownErrors, ThrowOnError> => (options.client ?? client).post<PostAdminListingsByIdTakedownResponses, PostAdminListingsByIdTakedownErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/listings/{id}/takedown',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Provision a moderator account
 *
 * Only an admin may do this and there is no self-service path — a moderator decides disputes and takes listings down, so the role is granted, never claimed.
 *
 */
export const postAdminModerators = <ThrowOnError extends boolean = false>(options: Options<PostAdminModeratorsData, ThrowOnError>): RequestResult<PostAdminModeratorsResponses, PostAdminModeratorsErrors, ThrowOnError> => (options.client ?? client).post<PostAdminModeratorsResponses, PostAdminModeratorsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/moderators',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke the moderator role
 *
 * Demotes to a plain user and drops the account's sessions. The account itself survives, since it may have traded.
 */
export const deleteAdminModeratorsById = <ThrowOnError extends boolean = false>(options: Options<DeleteAdminModeratorsByIdData, ThrowOnError>): RequestResult<DeleteAdminModeratorsByIdResponses, DeleteAdminModeratorsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAdminModeratorsByIdResponses, DeleteAdminModeratorsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/moderators/{id}',
    ...options
});

/**
 * List every row of a category, operator view
 *
 * Every row including the ones switched off — "why is this carrier missing from checkout" being the question this answers — each with the `provider` serving it, plus the set of providers this deployment has to move a row to. Admin only.
 *
 */
export const getAdminOptions = <ThrowOnError extends boolean = false>(options: Options<GetAdminOptionsData, ThrowOnError>): RequestResult<GetAdminOptionsResponses, GetAdminOptionsErrors, ThrowOnError> => (options.client ?? client).get<GetAdminOptionsResponses, GetAdminOptionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/options',
    ...options
});

/**
 * Switch a row off, rename it, or move it to another provider
 *
 * Absent fields are left alone. The id, the category and the slug do not change: a settled payment and a shipped parcel hold the slug as plain text, so it is permanent, and moving a row between categories would change what those records mean.
 *
 * `provider` is checked against what this deployment actually has — one nobody registered is 422, because the first person to find out otherwise would be a buyer at a checkout. Switching a row off takes it out of `/options` without deleting it, which is what keeps every past order that names it resolvable.
 *
 */
export const patchAdminOptionsById = <ThrowOnError extends boolean = false>(options: Options<PatchAdminOptionsByIdData, ThrowOnError>): RequestResult<PatchAdminOptionsByIdResponses, PatchAdminOptionsByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchAdminOptionsByIdResponses, PatchAdminOptionsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/options/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reconciliation view over all payment sessions
 *
 * Filter by kind, status and a settlement window. Intended for tying platform totals to what the rails report.
 */
export const getAdminPaymentSessions = <ThrowOnError extends boolean = false>(options?: Options<GetAdminPaymentSessionsData, ThrowOnError>): RequestResult<GetAdminPaymentSessionsResponses, GetAdminPaymentSessionsErrors, ThrowOnError> => (options?.client ?? client).get<GetAdminPaymentSessionsResponses, GetAdminPaymentSessionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/payment-sessions',
    ...options
});

/**
 * Decide a refund staff were asked about (moderator)
 *
 * The refund has to be `disputed`, which is what a party escalating through trust set it to. There are no rounds: whether the goods have already come back is what a verdict reads to know which of the two situations it is in.
 *
 * `buyer_wins: true` with the goods still at the buyer's grants the refund and books the return shipment, so the money is not released yet. `buyer_wins: true` once the return has been delivered sends the escrow back to the buyer and closes the order with it. `buyer_wins: false` ends the refund either way, and the payout to the seller stands.
 *
 * The money moves before the refund goes terminal, and the refund and the order it closes are written together: a settled refund over an order the payout can still see is money paid twice.
 *
 */
export const postAdminRefundsByIdVerdict = <ThrowOnError extends boolean = false>(options: Options<PostAdminRefundsByIdVerdictData, ThrowOnError>): RequestResult<PostAdminRefundsByIdVerdictResponses, PostAdminRefundsByIdVerdictErrors, ThrowOnError> => (options.client ?? client).post<PostAdminRefundsByIdVerdictResponses, PostAdminRefundsByIdVerdictErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/refunds/{id}/verdict',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a tag
 *
 * Removing it also detaches it from every listing that carried it.
 */
export const deleteAdminTagsBySlug = <ThrowOnError extends boolean = false>(options: Options<DeleteAdminTagsBySlugData, ThrowOnError>): RequestResult<DeleteAdminTagsBySlugResponses, DeleteAdminTagsBySlugErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAdminTagsBySlugResponses, DeleteAdminTagsBySlugErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/tags/{slug}',
    ...options
});

/**
 * Create or update a tag
 *
 * Upsert, not create: the slug is the whole identity of a tag and the only other column is its description, so a separate create and update would be the same statement twice and would need a 409 to tell them apart.
 *
 */
export const putAdminTagsBySlug = <ThrowOnError extends boolean = false>(options: Options<PutAdminTagsBySlugData, ThrowOnError>): RequestResult<PutAdminTagsBySlugResponses, PutAdminTagsBySlugErrors, ThrowOnError> => (options.client ?? client).put<PutAdminTagsBySlugResponses, PutAdminTagsBySlugErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/tags/{slug}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Record the verdict on a tax registration
 */
export const postAdminTaxInfoByAccountIdVerification = <ThrowOnError extends boolean = false>(options: Options<PostAdminTaxInfoByAccountIdVerificationData, ThrowOnError>): RequestResult<PostAdminTaxInfoByAccountIdVerificationResponses, PostAdminTaxInfoByAccountIdVerificationErrors, ThrowOnError> => (options.client ?? client).post<PostAdminTaxInfoByAccountIdVerificationResponses, PostAdminTaxInfoByAccountIdVerificationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/tax-info/{accountID}/verification',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The ticket queue
 *
 * Defaults to the open and under-review ones, oldest first — that is the small hot slice the queue is worked from, and the index behind it covers exactly that predicate.
 *
 */
export const getAdminTickets = <ThrowOnError extends boolean = false>(options?: Options<GetAdminTicketsData, ThrowOnError>): RequestResult<GetAdminTicketsResponses, GetAdminTicketsErrors, ThrowOnError> => (options?.client ?? client).get<GetAdminTicketsResponses, GetAdminTicketsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/tickets',
    ...options
});

/**
 * Read a ticket with what it is about
 *
 * Includes the thing itself and how many other open tickets name the same target, since a moderator decides on the pattern and not one complaint.
 *
 */
export const getAdminTicketsById = <ThrowOnError extends boolean = false>(options: Options<GetAdminTicketsByIdData, ThrowOnError>): RequestResult<GetAdminTicketsByIdResponses, GetAdminTicketsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetAdminTicketsByIdResponses, GetAdminTicketsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/tickets/{id}',
    ...options
});

/**
 * Take a ticket for review
 *
 * Moves it out of the open queue so two moderators do not work the same case. The requester is never told who claimed it: support answers as the desk, so a decision is the platform's and not a named person's to be argued with afterwards.
 *
 */
export const postAdminTicketsByIdClaim = <ThrowOnError extends boolean = false>(options: Options<PostAdminTicketsByIdClaimData, ThrowOnError>): RequestResult<PostAdminTicketsByIdClaimResponses, PostAdminTicketsByIdClaimErrors, ThrowOnError> => (options.client ?? client).post<PostAdminTicketsByIdClaimResponses, PostAdminTicketsByIdClaimErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/tickets/{id}/claim',
    ...options
});

/**
 * Resolve a ticket
 *
 * Records the verdict and what was done about it. `none` is the turn-down — read, answered, nothing done — which is why it is an action rather than a second status.
 *
 * Recording the action here does not carry it out. Taking a listing down and suspending a seller are separate calls to the modules that own them, so the decision and its effects each stay where they can be audited. A `refund-dispute` is the case where that separation is absolute: its verdict moves money, so it is made by `POST /admin/refunds/{id}/verdict` and this route answers 409 for one — closing it by hand would mark a case settled with the escrow untouched.
 *
 * Whatever staff want the requester to read goes in the ticket's thread as an ordinary message. `note` is the record kept beside the verdict.
 *
 */
export const postAdminTicketsByIdResolution = <ThrowOnError extends boolean = false>(options: Options<PostAdminTicketsByIdResolutionData, ThrowOnError>): RequestResult<PostAdminTicketsByIdResolutionResponses, PostAdminTicketsByIdResolutionErrors, ThrowOnError> => (options.client ?? client).post<PostAdminTicketsByIdResolutionResponses, PostAdminTicketsByIdResolutionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/tickets/{id}/resolution',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read another account's balances
 */
export const getAdminWalletsByAccountId = <ThrowOnError extends boolean = false>(options: Options<GetAdminWalletsByAccountIdData, ThrowOnError>): RequestResult<GetAdminWalletsByAccountIdResponses, GetAdminWalletsByAccountIdErrors, ThrowOnError> => (options.client ?? client).get<GetAdminWalletsByAccountIdResponses, GetAdminWalletsByAccountIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/wallets/{accountID}',
    ...options
});

/**
 * Correct a balance by hand
 *
 * The one way a balance changes without a business event behind it, so it is the most auditable: a reason is mandatory and an `idempotency_key` is required, because a retried correction that posts twice is money invented. Reusing a key returns the movement already recorded instead of making a second one.
 *
 */
export const postAdminWalletsByAccountIdAdjustments = <ThrowOnError extends boolean = false>(options: Options<PostAdminWalletsByAccountIdAdjustmentsData, ThrowOnError>): RequestResult<PostAdminWalletsByAccountIdAdjustmentsResponses, PostAdminWalletsByAccountIdAdjustmentsErrors, ThrowOnError> => (options.client ?? client).post<PostAdminWalletsByAccountIdAdjustmentsResponses, PostAdminWalletsByAccountIdAdjustmentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/wallets/{accountID}/adjustments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The withdrawal review queue
 */
export const getAdminWithdrawals = <ThrowOnError extends boolean = false>(options?: Options<GetAdminWithdrawalsData, ThrowOnError>): RequestResult<GetAdminWithdrawalsResponses, GetAdminWithdrawalsErrors, ThrowOnError> => (options?.client ?? client).get<GetAdminWithdrawalsResponses, GetAdminWithdrawalsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/withdrawals',
    ...options
});

/**
 * Approve a withdrawal and release the transfer
 *
 * The wallet was already debited when the request was made, so approving does not move the balance again — it settles the outbound leg.
 */
export const postAdminWithdrawalsByIdApproval = <ThrowOnError extends boolean = false>(options: Options<PostAdminWithdrawalsByIdApprovalData, ThrowOnError>): RequestResult<PostAdminWithdrawalsByIdApprovalResponses, PostAdminWithdrawalsByIdApprovalErrors, ThrowOnError> => (options.client ?? client).post<PostAdminWithdrawalsByIdApprovalResponses, PostAdminWithdrawalsByIdApprovalErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/withdrawals/{id}/approval',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reject a withdrawal and return the money to the balance
 */
export const postAdminWithdrawalsByIdRejection = <ThrowOnError extends boolean = false>(options: Options<PostAdminWithdrawalsByIdRejectionData, ThrowOnError>): RequestResult<PostAdminWithdrawalsByIdRejectionResponses, PostAdminWithdrawalsByIdRejectionErrors, ThrowOnError> => (options.client ?? client).post<PostAdminWithdrawalsByIdRejectionResponses, PostAdminWithdrawalsByIdRejectionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/admin/withdrawals/{id}/rejection',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The administrative divisions an address is written in
 *
 * Two tiers, one level per request: no `parent` answers the provinces, a province code answers its wards. Vietnam goes province to ward, so there is no district level to ask for — a contact's `district_code` stays null.
 *
 * Unauthenticated, because an address form and the listing browse's area filter both need it before anybody has signed in. The list is served from the backend rather than from a public third-party API so that a seller's own address does not depend on somebody else's uptime, and so both clients spell a code the same way: it is the zero-padded string the columns store, and a client sends back exactly what it was given.
 *
 */
export const getAdministrativeAreas = <ThrowOnError extends boolean = false>(options?: Options<GetAdministrativeAreasData, ThrowOnError>): RequestResult<GetAdministrativeAreasResponses, GetAdministrativeAreasErrors, ThrowOnError> => (options?.client ?? client).get<GetAdministrativeAreasResponses, GetAdministrativeAreasErrors, ThrowOnError>({ url: '/administrative-areas', ...options });

/**
 * List the caller's payout destinations
 *
 * Account numbers come back masked. The full number is only ever used to move money, never to display it back.
 */
export const getBankAccounts = <ThrowOnError extends boolean = false>(options?: Options<GetBankAccountsData, ThrowOnError>): RequestResult<GetBankAccountsResponses, GetBankAccountsErrors, ThrowOnError> => (options?.client ?? client).get<GetBankAccountsResponses, GetBankAccountsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/bank-accounts',
    ...options
});

/**
 * Register a payout destination
 */
export const postBankAccounts = <ThrowOnError extends boolean = false>(options: Options<PostBankAccountsData, ThrowOnError>): RequestResult<PostBankAccountsResponses, PostBankAccountsErrors, ThrowOnError> => (options.client ?? client).post<PostBankAccountsResponses, PostBankAccountsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/bank-accounts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a payout destination
 *
 * Soft delete. A completed withdrawal names this row as the place real money went, so financial history cannot lose its payee.
 */
export const deleteBankAccountsById = <ThrowOnError extends boolean = false>(options: Options<DeleteBankAccountsByIdData, ThrowOnError>): RequestResult<DeleteBankAccountsByIdResponses, DeleteBankAccountsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteBankAccountsByIdResponses, DeleteBankAccountsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/bank-accounts/{id}',
    ...options
});

/**
 * Make this the default payout destination
 *
 * At most one default per account, so this clears the previous one. Bank details themselves are not editable — register another destination and remove this one, so a completed withdrawal never names a row whose details have since changed.
 */
export const patchBankAccountsById = <ThrowOnError extends boolean = false>(options: Options<PatchBankAccountsByIdData, ThrowOnError>): RequestResult<PatchBankAccountsByIdResponses, PatchBankAccountsByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchBankAccountsByIdResponses, PatchBankAccountsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/bank-accounts/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the caller's cart
 *
 * The whole cart, unpaginated. A cart is bounded by what one person means to buy, so there is nothing to page through.
 *
 */
export const getCartItems = <ThrowOnError extends boolean = false>(options?: Options<GetCartItemsData, ThrowOnError>): RequestResult<GetCartItemsResponses, GetCartItemsErrors, ThrowOnError> => (options?.client ?? client).get<GetCartItemsResponses, GetCartItemsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cart-items',
    ...options
});

/**
 * Add a SKU to the cart
 *
 * One row per (account, SKU). Adding a SKU already in the cart tops that row up rather than stacking a second one — adding twice means wanting more.
 *
 */
export const postCartItems = <ThrowOnError extends boolean = false>(options: Options<PostCartItemsData, ThrowOnError>): RequestResult<PostCartItemsResponses, PostCartItemsErrors, ThrowOnError> => (options.client ?? client).post<PostCartItemsResponses, PostCartItemsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cart-items',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a cart row
 */
export const deleteCartItemsById = <ThrowOnError extends boolean = false>(options: Options<DeleteCartItemsByIdData, ThrowOnError>): RequestResult<DeleteCartItemsByIdResponses, DeleteCartItemsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteCartItemsByIdResponses, DeleteCartItemsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cart-items/{id}',
    ...options
});

/**
 * Change the quantity of a cart row
 */
export const patchCartItemsById = <ThrowOnError extends boolean = false>(options: Options<PatchCartItemsByIdData, ThrowOnError>): RequestResult<PatchCartItemsByIdResponses, PatchCartItemsByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchCartItemsByIdResponses, PatchCartItemsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/cart-items/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The category tree, or the categories nearest a set of seeds
 *
 * Flat list with a parent reference on each row; a client assembles the tree. A category tree is small so this is unpaginated.
 *
 * `near` answers a different question: which categories does this thing belong in. It ranks by cosine distance over `category_embedding` and returns a shortlist with a `score` instead of the tree, which is what the category field of the listing form needs — a seller who has picked a few tags should not have to walk a tree to find where the item goes.
 *
 */
export const getCategories = <ThrowOnError extends boolean = false>(options?: Options<GetCategoriesData, ThrowOnError>): RequestResult<GetCategoriesResponses, GetCategoriesErrors, ThrowOnError> => (options?.client ?? client).get<GetCategoriesResponses, GetCategoriesErrors, ThrowOnError>({
    querySerializer: { parameters: { near: { array: { explode: false } } } },
    url: '/categories',
    ...options
});

/**
 * List the caller's saved addresses
 */
export const getContacts = <ThrowOnError extends boolean = false>(options?: Options<GetContactsData, ThrowOnError>): RequestResult<GetContactsResponses, GetContactsErrors, ThrowOnError> => (options?.client ?? client).get<GetContactsResponses, GetContactsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/contacts',
    ...options
});

/**
 * Save an address
 *
 * The administrative codes are what a carrier is called with, so they are the routing source of truth and the names beside them are display snapshots. A coordinate is optional: geocoding may fail and the address still has to be saveable.
 *
 */
export const postContacts = <ThrowOnError extends boolean = false>(options: Options<PostContactsData, ThrowOnError>): RequestResult<PostContactsResponses, PostContactsErrors, ThrowOnError> => (options.client ?? client).post<PostContactsResponses, PostContactsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/contacts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a saved address
 */
export const deleteContactsById = <ThrowOnError extends boolean = false>(options: Options<DeleteContactsByIdData, ThrowOnError>): RequestResult<DeleteContactsByIdResponses, DeleteContactsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteContactsByIdResponses, DeleteContactsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/contacts/{id}',
    ...options
});

/**
 * Update a saved address
 *
 * Setting a default clears the previous one. Delivery and pickup defaults are independent flags — the same address is usually both.
 *
 * An order already placed keeps its own snapshot of the address, so editing this row never rewrites where a shipment was going.
 *
 */
export const patchContactsById = <ThrowOnError extends boolean = false>(options: Options<PatchContactsByIdData, ThrowOnError>): RequestResult<PatchContactsByIdResponses, PatchContactsByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchContactsByIdResponses, PatchContactsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/contacts/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a verification code to a saved address's phone
 *
 * The phone a carrier will actually call, which is why this one is verified and the account's sign-in phone is not. A courier who cannot reach the number is the most common way a delivery fails, so a seller shipping to an unverified number is worth flagging in the UI.
 *
 * Re-sending replaces the outstanding code rather than issuing a second valid one.
 *
 */
export const postContactsByIdPhoneVerificationRequests = <ThrowOnError extends boolean = false>(options: Options<PostContactsByIdPhoneVerificationRequestsData, ThrowOnError>): RequestResult<PostContactsByIdPhoneVerificationRequestsResponses, PostContactsByIdPhoneVerificationRequestsErrors, ThrowOnError> => (options.client ?? client).post<PostContactsByIdPhoneVerificationRequestsResponses, PostContactsByIdPhoneVerificationRequestsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/contacts/{id}/phone/verification-requests',
    ...options
});

/**
 * Confirm a saved address's phone with the code
 *
 * Editing the number clears the flag again — a verified flag on a number nobody checked is worse than no flag.
 *
 */
export const postContactsByIdPhoneVerifications = <ThrowOnError extends boolean = false>(options: Options<PostContactsByIdPhoneVerificationsData, ThrowOnError>): RequestResult<PostContactsByIdPhoneVerificationsResponses, PostContactsByIdPhoneVerificationsErrors, ThrowOnError> => (options.client ?? client).post<PostContactsByIdPhoneVerificationsResponses, PostContactsByIdPhoneVerificationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/contacts/{id}/phone/verifications',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The caller's inbox
 *
 * Threads by latest activity. A participant sits on either side of the stored pair, so this is answered as two ordered index scans merged together rather than one scan that sorts.
 *
 */
export const getConversations = <ThrowOnError extends boolean = false>(options?: Options<GetConversationsData, ThrowOnError>): RequestResult<GetConversationsResponses, GetConversationsErrors, ThrowOnError> => (options?.client ?? client).get<GetConversationsResponses, GetConversationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations',
    ...options
});

/**
 * Open the thread with another account
 *
 * Idempotent by design: there is exactly one thread per pair, so this returns the existing one when it exists and creates it otherwise. A client does not have to track whether it has messaged someone before.
 *
 */
export const postConversations = <ThrowOnError extends boolean = false>(options: Options<PostConversationsData, ThrowOnError>): RequestResult<PostConversationsResponses, PostConversationsErrors, ThrowOnError> => (options.client ?? client).post<PostConversationsResponses, PostConversationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read a thread
 *
 * Carries the counterparty rather than both sides — the caller already knows who they are.
 */
export const getConversationsById = <ThrowOnError extends boolean = false>(options: Options<GetConversationsByIdData, ThrowOnError>): RequestResult<GetConversationsByIdResponses, GetConversationsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetConversationsByIdResponses, GetConversationsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations/{id}',
    ...options
});

/**
 * Read a thread's messages
 *
 * Newest first, cursor only. Offset paging would defeat the chunk exclusion the time partitioning exists for.
 *
 * A redacted message is still returned, with its body emptied and `deleted_at` set: a gap in a thread that a dispute may hinge on is worse than a visible removal.
 *
 */
export const getConversationsByIdMessages = <ThrowOnError extends boolean = false>(options: Options<GetConversationsByIdMessagesData, ThrowOnError>): RequestResult<GetConversationsByIdMessagesResponses, GetConversationsByIdMessagesErrors, ThrowOnError> => (options.client ?? client).get<GetConversationsByIdMessagesResponses, GetConversationsByIdMessagesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations/{id}/messages',
    ...options
});

/**
 * Send a message
 *
 * Text, attachments or both. `refs` carries pointers to a listing or a variant or an order, which is how a product is referred to inside a thread that is not itself listing-scoped.
 *
 */
export const postConversationsByIdMessages = <ThrowOnError extends boolean = false>(options: Options<PostConversationsByIdMessagesData, ThrowOnError>): RequestResult<PostConversationsByIdMessagesResponses, PostConversationsByIdMessagesErrors, ThrowOnError> => (options.client ?? client).post<PostConversationsByIdMessagesResponses, PostConversationsByIdMessagesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations/{id}/messages',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Mark a thread read up to a point in time
 *
 * Moves the caller's read mark on the thread — one UPDATE of one row, whatever it covers. Omit `before` to mark the whole thread read. A mark never moves backwards: passing an earlier instant than the current one is a no-op, because un-reading is not a thing a client should be able to do to its own badge.
 *
 * Answers the updated thread rather than just the badge, so a client that marked it read can repaint the row from one response instead of a second GET.
 *
 */
export const postConversationsByIdRead = <ThrowOnError extends boolean = false>(options: Options<PostConversationsByIdReadData, ThrowOnError>): RequestResult<PostConversationsByIdReadResponses, PostConversationsByIdReadErrors, ThrowOnError> => (options.client ?? client).post<PostConversationsByIdReadResponses, PostConversationsByIdReadErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations/{id}/read',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The unread badge across every thread
 *
 * One range scan per thread the caller has, each bounded below by that thread's read mark — so an old unread message costs no more than a recent one and chunk exclusion applies throughout, but the work is proportional to how many threads the caller has, not constant. Separate from the inbox because the badge is read far more often than the list.
 *
 */
export const getConversationsUnreadCount = <ThrowOnError extends boolean = false>(options?: Options<GetConversationsUnreadCountData, ThrowOnError>): RequestResult<GetConversationsUnreadCountResponses, GetConversationsUnreadCountErrors, ThrowOnError> => (options?.client ?? client).get<GetConversationsUnreadCountResponses, GetConversationsUnreadCountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations/unread-count',
    ...options
});

/**
 * Reserve a slot to upload a message attachment into
 *
 * The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /conversations/uploads/{id}/confirmation` makes the row real once the object is there — so a message can never render a photo whose bytes never arrived.
 *
 * `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
 *
 */
export const postConversationsUploads = <ThrowOnError extends boolean = false>(options: Options<PostConversationsUploadsData, ThrowOnError>): RequestResult<PostConversationsUploadsResponses, PostConversationsUploadsErrors, ThrowOnError> => (options.client ?? client).post<PostConversationsUploadsResponses, PostConversationsUploadsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations/uploads',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm the bytes are at the store
 *
 * Until this lands the resource resolves to nothing, so a half-finished upload cannot be attached to a message. The recorded size is the store's, not the one declared when the slot was reserved.
 * The answer carries a signed `url` for the bytes just uploaded. That is the only time it is handed over unasked: a second confirmation is refused, and the resource is otherwise only seen through whatever it gets attached to.
 *
 */
export const postConversationsUploadsByIdConfirmation = <ThrowOnError extends boolean = false>(options: Options<PostConversationsUploadsByIdConfirmationData, ThrowOnError>): RequestResult<PostConversationsUploadsByIdConfirmationResponses, PostConversationsUploadsByIdConfirmationErrors, ThrowOnError> => (options.client ?? client).post<PostConversationsUploadsByIdConfirmationResponses, PostConversationsUploadsByIdConfirmationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conversations/uploads/{id}/confirmation',
    ...options
});

/**
 * Register or move a push device
 *
 * An upsert on the push token, not a create. The token identifies an install and the platform reissues the same one when a different user signs in on that phone, so the row moves to the new account. Without that, the previous owner keeps receiving that phone's notifications.
 *
 */
export const putDevices = <ThrowOnError extends boolean = false>(options: Options<PutDevicesData, ThrowOnError>): RequestResult<PutDevicesResponses, PutDevicesErrors, ThrowOnError> => (options.client ?? client).put<PutDevicesResponses, PutDevicesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/devices',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unregister a push device
 */
export const deleteDevicesById = <ThrowOnError extends boolean = false>(options: Options<DeleteDevicesByIdData, ThrowOnError>): RequestResult<DeleteDevicesByIdResponses, DeleteDevicesByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteDevicesByIdResponses, DeleteDevicesByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/devices/{id}',
    ...options
});

/**
 * List the caller's purchase sessions
 */
export const getDrafts = <ThrowOnError extends boolean = false>(options?: Options<GetDraftsData, ThrowOnError>): RequestResult<GetDraftsResponses, GetDraftsErrors, ThrowOnError> => (options?.client ?? client).get<GetDraftsResponses, GetDraftsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/drafts',
    ...options
});

/**
 * Open a purchase session for one listing
 *
 * Freezes the listing terms so a price the seller edits afterwards cannot be charged. The server snapshots the listing together with every variant — price and package details live on the variant, and those are exactly what must not move under the buyer — and sets `valid_until`.
 *
 * Every listing has one, whichever way it is priced. A `negotiable` listing carries the seller's asking price like any other, and a buyer who does not want to haggle takes it: the listing page offers both, and this is the "buy now" half. Negotiating instead freezes the agreed terms in the offer, and `POST /offers/{id}/checkout` is the same checkout from there.
 *
 */
export const postDrafts = <ThrowOnError extends boolean = false>(options: Options<PostDraftsData, ThrowOnError>): RequestResult<PostDraftsResponses, PostDraftsErrors, ThrowOnError> => (options.client ?? client).post<PostDraftsResponses, PostDraftsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/drafts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel a purchase session
 *
 * Sets `cancelled_at`. A session that already produced paid items cannot be cancelled.
 */
export const deleteDraftsById = <ThrowOnError extends boolean = false>(options: Options<DeleteDraftsByIdData, ThrowOnError>): RequestResult<DeleteDraftsByIdResponses, DeleteDraftsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteDraftsByIdResponses, DeleteDraftsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/drafts/{id}',
    ...options
});

/**
 * Read a purchase session and its frozen terms
 */
export const getDraftsById = <ThrowOnError extends boolean = false>(options: Options<GetDraftsByIdData, ThrowOnError>): RequestResult<GetDraftsByIdResponses, GetDraftsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetDraftsByIdResponses, GetDraftsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/drafts/{id}',
    ...options
});

/**
 * Check out a purchase session (pay-first)
 *
 * Creates one `item` per line plus a single buyer-checkout payment session, and returns both. Line prices come from the session's frozen snapshot, never from the live catalog. `contact_id` is snapshotted into every line's delivery address: one session covers one SPU and therefore one seller, so a single address is correct.
 *
 * The session is spent before the money is asked for: the claim is what makes two concurrent checkouts of one draft impossible, and the loser is answered 409 rather than handed a second payment session for the same frozen price.
 *
 * This is the checkout for terms a draft froze — the seller's own price, whether the listing is `fixed` or `negotiable`. Terms that were *negotiated* are checked out at `POST /offers/{id}/checkout` instead, which is the same flow against the agreed price. The buyer pays delivery either way.
 *
 */
export const postDraftsByIdCheckout = <ThrowOnError extends boolean = false>(options: Options<PostDraftsByIdCheckoutData, ThrowOnError>): RequestResult<PostDraftsByIdCheckoutResponses, PostDraftsByIdCheckoutErrors, ThrowOnError> => (options.client ?? client).post<PostDraftsByIdCheckoutResponses, PostDraftsByIdCheckoutErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/drafts/{id}/checkout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a verification link to the caller's email
 */
export const postEmailVerificationRequests = <ThrowOnError extends boolean = false>(options?: Options<PostEmailVerificationRequestsData, ThrowOnError>): RequestResult<PostEmailVerificationRequestsResponses, PostEmailVerificationRequestsErrors, ThrowOnError> => (options?.client ?? client).post<PostEmailVerificationRequestsResponses, PostEmailVerificationRequestsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/email/verification-requests',
    ...options
});

/**
 * Confirm an email address
 */
export const postEmailVerifications = <ThrowOnError extends boolean = false>(options: Options<PostEmailVerificationsData, ThrowOnError>): RequestResult<PostEmailVerificationsResponses, PostEmailVerificationsErrors, ThrowOnError> => (options.client ?? client).post<PostEmailVerificationsResponses, PostEmailVerificationsErrors, ThrowOnError>({
    url: '/email/verifications',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unsave a listing
 *
 * Idempotent, and deliberately so all the way: unsaving something that was never saved answers 204, and so does a `listingID` that names no listing at all. The post-condition — this listing is not on the caller's wishlist — holds either way, and a 404 here would only tell a stranger which ids exist.
 *
 */
export const deleteFavoritesByListingId = <ThrowOnError extends boolean = false>(options: Options<DeleteFavoritesByListingIdData, ThrowOnError>): RequestResult<DeleteFavoritesByListingIdResponses, DeleteFavoritesByListingIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteFavoritesByListingIdResponses, DeleteFavoritesByListingIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/favorites/{listingID}',
    ...options
});

/**
 * Save a listing
 *
 * Idempotent — the account and listing pair is the whole row, so saving twice is the same as saving once. Read the wishlist back through `GET /listings?favorited=true`.
 *
 */
export const putFavoritesByListingId = <ThrowOnError extends boolean = false>(options: Options<PutFavoritesByListingIdData, ThrowOnError>): RequestResult<PutFavoritesByListingIdResponses, PutFavoritesByListingIdErrors, ThrowOnError> => (options.client ?? client).put<PutFavoritesByListingIdResponses, PutFavoritesByListingIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/favorites/{listingID}',
    ...options
});

/**
 * Unfollow a seller
 */
export const deleteFollowsByAccountId = <ThrowOnError extends boolean = false>(options: Options<DeleteFollowsByAccountIdData, ThrowOnError>): RequestResult<DeleteFollowsByAccountIdResponses, DeleteFollowsByAccountIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteFollowsByAccountIdResponses, DeleteFollowsByAccountIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/follows/{accountID}',
    ...options
});

/**
 * Follow a seller
 *
 * Idempotent. Following yourself is refused.
 */
export const putFollowsByAccountId = <ThrowOnError extends boolean = false>(options: Options<PutFollowsByAccountIdData, ThrowOnError>): RequestResult<PutFollowsByAccountIdResponses, PutFollowsByAccountIdErrors, ThrowOnError> => (options.client ?? client).put<PutFollowsByAccountIdResponses, PutFollowsByAccountIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/follows/{accountID}',
    ...options
});

/**
 * Start identity verification
 *
 * Hands the uploaded scans to the KYC vendor. Neither a document number nor a scan is stored here — only the vendor, its case reference and the verdict — so a leak of this data cannot impersonate anyone.
 *
 * The verdict may already be final in the response: a vendor that reads the images answers in the same call, so `status` can come back `verified` or `rejected` with no session URL. A vendor that runs its own web flow answers `pending` and returns `vendor_session_url` for the caller to finish in. A client has to read the status rather than assume one of the two.
 *
 * A rejected case leaves the account free to submit another one: the usual reason is glare on the card, and there is nothing to appeal.
 *
 */
export const postIdentityDocuments = <ThrowOnError extends boolean = false>(options: Options<PostIdentityDocumentsData, ThrowOnError>): RequestResult<PostIdentityDocumentsResponses, PostIdentityDocumentsErrors, ThrowOnError> => (options.client ?? client).post<PostIdentityDocumentsResponses, PostIdentityDocumentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/identity-documents',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List checkout lines, as buyer or as seller
 *
 * `role=seller&pending=true` is the seller's retry list: lines the money has paid for that no order covers yet. Not an inbox — nobody confirms them. The order is written by the payment webhook, so a line sitting here for long is a settlement that has not succeeded yet rather than one waiting on a person.
 *
 */
export const getItems = <ThrowOnError extends boolean = false>(options?: Options<GetItemsData, ThrowOnError>): RequestResult<GetItemsResponses, GetItemsErrors, ThrowOnError> => (options?.client ?? client).get<GetItemsResponses, GetItemsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/items',
    ...options
});

/**
 * Cancel a line the money has not reached
 *
 * Either party may drop a line while it is unpaid — the buyer changed their mind, or the seller cannot fulfil it — and the reserved units go back. The cancelling account is recorded.
 *
 * A paid line is 409, and that is checked against the payment session rather than against `order_id`: the order is written by the webhook, so a line that has been charged for has a null `order_id` for as long as that write keeps failing. From there the way back is a refund, which the seller gets to see.
 *
 */
export const postItemsByIdCancellation = <ThrowOnError extends boolean = false>(options: Options<PostItemsByIdCancellationData, ThrowOnError>): RequestResult<PostItemsByIdCancellationResponses, PostItemsByIdCancellationErrors, ThrowOnError> => (options.client ?? client).post<PostItemsByIdCancellationResponses, PostItemsByIdCancellationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/items/{id}/cancellation',
    ...options
});

/**
 * Browse, search and resolve listings
 *
 * With no parameters, the newest live active listings. The parameters narrow that one query rather than selecting between separate endpoints.
 *
 * `q` makes it a search and moves the default sort to `relevance`. `lexical` matches the name by trigram and tolerates missing diacritics, `semantic` runs an approximate nearest-neighbour search over the dense embedding, and `hybrid` combines the dense vector with the sparse lexical one, which is what the embedding model is built for. Each hit then carries `score`, always so that higher is better. A listing whose embedding has not been computed yet can only be found lexically.
 *
 * `sort=recommended` is the personalised feed. A buyer is credited with up to four interests at once, derived from what they saved and refreshed as they save more; each one searches the catalogue on its own, a further share of the page goes to whatever was posted most recently, and the results are merged in proportion to how much of that buyer's behaviour each source accounts for. So an occasional taste still reaches the page instead of being crowded out by the dominant one, and something outside every one of them still gets in — a feed that only ever answers its own past has no way to learn it was wrong. The page is *drawn* from a pool several pages deep rather than taken off the top, so two runs differ; `seed` is what holds one run still while it is paged through. Their own listings and anything already on their wishlist are left out. It needs a token, and falls back to newest for an account with no interests computed yet.
 *
 * `score` is the similarity to the interest that surfaced a card, and is `null` for one drawn for being new — nothing measured it against anything.
 *
 * `mine=true` restricts the result to the caller's own listings, and is the only case in which `status` is honoured — a seller has to see what is not public, and nobody else may.
 *
 * `favorited=true` is the wishlist page. It is a filter here rather than its own endpoint because a wishlist wants exactly what a feed wants — cards, with prices and stock and every filter available — and a separate endpoint returning saved ids left the client resolving them one by one.
 *
 * `ids` resolves a known set, which is how a cart or an order list renders the listings behind its rows. It ignores every other filter and answers for hidden and soft-deleted listings too, because an order that references one still has to display it; `deleted_at` says which. Listings that were never public (`draft`, `pending`) stay out unless they are the caller's own.
 *
 * Location is a C2C buyer's first filter — collecting in person, or just trusting a seller two districts away over one across the country. It is the seller's pickup address as the listing snapshotted it when they published, so it is the same address a carrier collects from, and it never moves under a listing that has already sold. `province_code`/`district_code`/ `ward_code` filter on it; `lat`+`lon` or `near_contact_id` measure from where the buyer is, `radius_km` bounds the result and `sort=distance` orders by it.
 *
 * `price-asc` and `price-desc` are answered through the SKU price index rather than a value cached on the listing, so `price` is the cheapest matching variant's. `best-selling` is the one that cannot work that way — it orders by a sum over the variants, and a sum has no per-SKU index to scan in order — so it reads `cached_sold` on the listing, maintained with the sales it counts.
 *
 */
export const getListings = <ThrowOnError extends boolean = false>(options?: Options<GetListingsData, ThrowOnError>): RequestResult<GetListingsResponses, GetListingsErrors, ThrowOnError> => (options?.client ?? client).get<GetListingsResponses, GetListingsErrors, ThrowOnError>({
    querySerializer: { parameters: { ids: { array: { explode: false } } } },
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings',
    ...options
});

/**
 * Create a listing
 *
 * Creates the listing and its variants together. At least one variant is required because price and shipping weight live there.
 *
 * Starts as a draft and is not public until published.
 *
 */
export const postListings = <ThrowOnError extends boolean = false>(options: Options<PostListingsData, ThrowOnError>): RequestResult<PostListingsResponses, PostListingsErrors, ThrowOnError> => (options.client ?? client).post<PostListingsResponses, PostListingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a listing
 *
 * Soft delete, and distinct from hiding it. Order history holds the listing and variant ids without a foreign key, so a past order has to stay resolvable after the seller removes the listing.
 *
 */
export const deleteListingsById = <ThrowOnError extends boolean = false>(options: Options<DeleteListingsByIdData, ThrowOnError>): RequestResult<DeleteListingsByIdResponses, DeleteListingsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteListingsByIdResponses, DeleteListingsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/{id}',
    ...options
});

/**
 * Read a listing
 *
 * Carries the variants with their prices and stock and the seller summary.
 *
 * A listing that was never public — `draft` or `pending` — is readable only by its owner and by staff. One that is `hidden` or soft-deleted is readable by anyone, because a cart or an order that references it still has to render; `status` and `deleted_at` say that it cannot be bought.
 *
 */
export const getListingsById = <ThrowOnError extends boolean = false>(options: Options<GetListingsByIdData, ThrowOnError>): RequestResult<GetListingsByIdResponses, GetListingsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetListingsByIdResponses, GetListingsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/{id}',
    ...options
});

/**
 * Edit a listing
 *
 * An edit to a draft applies immediately. An edit to a live listing that trips the prohibited-goods filter is held as a pending edit and applied only when a moderator approves it, so the version buyers see never changes into unreviewed content.
 *
 * The slug's readable half is fixed at creation. Renaming a listing leaves it alone, so a link shared before the rename keeps saying what it said then — and it would resolve either way, since the id it carries is what addresses the listing.
 *
 */
export const patchListingsById = <ThrowOnError extends boolean = false>(options: Options<PatchListingsByIdData, ThrowOnError>): RequestResult<PatchListingsByIdResponses, PatchListingsByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchListingsByIdResponses, PatchListingsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Hide a live listing
 *
 * Takes it out of every feed while leaving it alive — the seller is away, or the item is temporarily out of stock. Not the same as deleting it.
 *
 */
export const deleteListingsByIdPublication = <ThrowOnError extends boolean = false>(options: Options<DeleteListingsByIdPublicationData, ThrowOnError>): RequestResult<DeleteListingsByIdPublicationResponses, DeleteListingsByIdPublicationErrors, ThrowOnError> => (options.client ?? client).delete<DeleteListingsByIdPublicationResponses, DeleteListingsByIdPublicationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/{id}/publication',
    ...options
});

/**
 * Submit a listing for publication
 *
 * Publication always goes through moderation: this queues the listing and it stays invisible until a moderator clears it. There is no path that makes a listing live without a human, which is also why re-publishing something a moderator took down cannot undo the takedown.
 *
 * An automatic prohibited-goods scan still runs — it orders the queue and flags what to look at first, it does not clear anything.
 *
 * This is also where the listing gets its location: `pickup_contact_id` names which of the seller's own saved addresses a carrier collects from, and omitting it means their default pickup address. A seller with neither is refused with 422 rather than left with a live listing every checkout fails on — the address is both where a carrier goes and how buyers find the listing, so it is taken now and frozen onto the row.
 *
 */
export const postListingsByIdPublication = <ThrowOnError extends boolean = false>(options: Options<PostListingsByIdPublicationData, ThrowOnError>): RequestResult<PostListingsByIdPublicationResponses, PostListingsByIdPublicationErrors, ThrowOnError> => (options.client ?? client).post<PostListingsByIdPublicationResponses, PostListingsByIdPublicationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/{id}/publication',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Add a variant to a listing
 *
 * Answers with the listing. The new variant is the one in `variants` that was not there before — there is no separate id in the response, because a variant is not addressable on its own.
 *
 */
export const postListingsByIdVariants = <ThrowOnError extends boolean = false>(options: Options<PostListingsByIdVariantsData, ThrowOnError>): RequestResult<PostListingsByIdVariantsResponses, PostListingsByIdVariantsErrors, ThrowOnError> => (options.client ?? client).post<PostListingsByIdVariantsResponses, PostListingsByIdVariantsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/{id}/variants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reviews of a listing
 *
 * Public, with the first few replies and the vote tally on each one.
 *
 * A cursor belongs to the sort it was issued under: each ordering pages on its own key, so a cursor from `sort=helpful` is meaningless to `sort=newest` and a client that switches sorts starts the traversal again.
 *
 * `sort=helpful` orders by a counter that other people are changing while a client pages: a review that gains votes can move across a page boundary and be seen twice or not at all. Cursor paging is exact only over a key that does not move, and `newest` is that key. The drift is accepted rather than designed away — a review appearing twice in a list costs the reader nothing — but a caller that needs a stable traversal should page by `newest`.
 *
 */
export const getListingsByListingIdReviews = <ThrowOnError extends boolean = false>(options: Options<GetListingsByListingIdReviewsData, ThrowOnError>): RequestResult<GetListingsByListingIdReviewsResponses, GetListingsByListingIdReviewsErrors, ThrowOnError> => (options.client ?? client).get<GetListingsByListingIdReviewsResponses, GetListingsByListingIdReviewsErrors, ThrowOnError>({ url: '/listings/{listingID}/reviews', ...options });

/**
 * Review a listing you bought
 *
 * A completed order is required — no purchase, no review — and one review per listing per order, so buying twice earns a second review but one purchase does not earn two. An order that was cancelled or fully refunded earns none: there are no goods left to rate. Rating the counterparty of such a sale is what feedback is for.
 *
 */
export const postListingsByListingIdReviews = <ThrowOnError extends boolean = false>(options: Options<PostListingsByListingIdReviewsData, ThrowOnError>): RequestResult<PostListingsByListingIdReviewsResponses, PostListingsByListingIdReviewsErrors, ThrowOnError> => (options.client ?? client).post<PostListingsByListingIdReviewsResponses, PostListingsByListingIdReviewsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/{listingID}/reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Fill in a listing from photos and what the seller said
 *
 * "Photo in, listing out": the seller snaps the item, says a sentence about it, and gets a filled-in form back. One synchronous call — the client shows the form as a skeleton and fills it from the answer.
 *
 * Nothing is stored. This route writes no listing and no draft: the answer is a *suggestion*, the seller corrects what they disagree with, and `POST /listings` is still how a listing comes into existence. That is deliberate — a route that posted for them would make a model the author of claims about somebody else's goods, its condition and its price, and there is no version of that a marketplace can stand behind. An abandoned attempt leaves nothing behind either.
 *
 * Send at least one of `attachments`, `note` or `voice_note`. Photos are ordinary confirmed uploads (`POST /listings/uploads`), so the client can be uploading them while the seller is still talking; the first three are what the model reads. The voice note travels inline rather than as an upload because it is input and not content — nothing keeps it. Every field of the answer is optional except `name`: one the model could not stand behind comes back empty, and a blank box the seller fills beats a wrong value they have to notice. `price` is only ever a number the seller said out loud, never an estimate.
 *
 */
export const postListingsSuggestions = <ThrowOnError extends boolean = false>(options: Options<PostListingsSuggestionsData, ThrowOnError>): RequestResult<PostListingsSuggestionsResponses, PostListingsSuggestionsErrors, ThrowOnError> => (options.client ?? client).post<PostListingsSuggestionsResponses, PostListingsSuggestionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/suggestions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reserve a slot to upload a listing photo into
 *
 * The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /listings/uploads/{id}/confirmation` makes the row real once the object is there — so a listing can never render a photo whose bytes never arrived.
 *
 * `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
 *
 */
export const postListingsUploads = <ThrowOnError extends boolean = false>(options: Options<PostListingsUploadsData, ThrowOnError>): RequestResult<PostListingsUploadsResponses, PostListingsUploadsErrors, ThrowOnError> => (options.client ?? client).post<PostListingsUploadsResponses, PostListingsUploadsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/uploads',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm the bytes are at the store
 *
 * Until this lands the resource resolves to nothing, so a half-finished upload cannot be attached to a listing. The recorded size is the store's, not the one declared when the slot was reserved.
 * The answer carries a signed `url` for the bytes just uploaded. That is the only time it is handed over unasked: a second confirmation is refused, and the resource is otherwise only seen through whatever it gets attached to.
 *
 */
export const postListingsUploadsByIdConfirmation = <ThrowOnError extends boolean = false>(options: Options<PostListingsUploadsByIdConfirmationData, ThrowOnError>): RequestResult<PostListingsUploadsByIdConfirmationResponses, PostListingsUploadsByIdConfirmationErrors, ThrowOnError> => (options.client ?? client).post<PostListingsUploadsByIdConfirmationResponses, PostListingsUploadsByIdConfirmationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/listings/uploads/{id}/confirmation',
    ...options
});

/**
 * Sign in with a password
 *
 * The identifier is an email or a phone or a username. Which one it is does not change the answer and is not reported back.
 */
export const postLogin = <ThrowOnError extends boolean = false>(options: Options<PostLoginData, ThrowOnError>): RequestResult<PostLoginResponses, PostLoginErrors, ThrowOnError> => (options.client ?? client).post<PostLoginResponses, PostLoginErrors, ThrowOnError>({
    url: '/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Sign in through a federated provider
 *
 * Links to an existing account when the provider's subject id is already known, and otherwise creates one. A provider that returns no email — Apple with "hide my email" — gets a generated username, because the account still has to carry an identifier.
 *
 */
export const postLoginOauth = <ThrowOnError extends boolean = false>(options: Options<PostLoginOauthData, ThrowOnError>): RequestResult<PostLoginOauthResponses, PostLoginOauthErrors, ThrowOnError> => (options.client ?? client).post<PostLoginOauthResponses, PostLoginOauthErrors, ThrowOnError>({
    url: '/login/oauth',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke the current session
 *
 * Also unregisters the caller's push device when one is supplied so the phone stops receiving notifications for an account nobody is signed in to.
 */
export const postLogout = <ThrowOnError extends boolean = false>(options?: Options<PostLogoutData, ThrowOnError>): RequestResult<PostLogoutResponses, PostLogoutErrors, ThrowOnError> => (options?.client ?? client).post<PostLogoutResponses, PostLogoutErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/logout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Read the caller's own account and profile
 */
export const getMe = <ThrowOnError extends boolean = false>(options?: Options<GetMeData, ThrowOnError>): RequestResult<GetMeResponses, GetMeErrors, ThrowOnError> => (options?.client ?? client).get<GetMeResponses, GetMeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me',
    ...options
});

/**
 * Change the caller's identifiers
 *
 * Changing the email clears `email_verified` and sends a fresh verification. The last remaining identifier cannot be removed.
 *
 * The phone here is an identifier — something to sign in and be reached by — and carries no verified flag. Verification belongs to a phone a carrier will actually call, which is the one on a `contact`: that is where `phone_verified` lives, and editing this one does not touch it.
 *
 */
export const patchMe = <ThrowOnError extends boolean = false>(options: Options<PatchMeData, ThrowOnError>): RequestResult<PatchMeResponses, PatchMeErrors, ThrowOnError> => (options.client ?? client).patch<PatchMeResponses, PatchMeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the caller's push devices
 */
export const getMeDevices = <ThrowOnError extends boolean = false>(options?: Options<GetMeDevicesData, ThrowOnError>): RequestResult<GetMeDevicesResponses, GetMeDevicesErrors, ThrowOnError> => (options?.client ?? client).get<GetMeDevicesResponses, GetMeDevicesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/devices',
    ...options
});

/**
 * Sellers the caller follows
 */
export const getMeFollowing = <ThrowOnError extends boolean = false>(options?: Options<GetMeFollowingData, ThrowOnError>): RequestResult<GetMeFollowingResponses, GetMeFollowingErrors, ThrowOnError> => (options?.client ?? client).get<GetMeFollowingResponses, GetMeFollowingErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/following',
    ...options
});

/**
 * The caller's verification history
 *
 * Includes `expires_at` — a passport runs out and a payout gate has to look at more than the status.
 */
export const getMeIdentityDocuments = <ThrowOnError extends boolean = false>(options?: Options<GetMeIdentityDocumentsData, ThrowOnError>): RequestResult<GetMeIdentityDocumentsResponses, GetMeIdentityDocumentsErrors, ThrowOnError> => (options?.client ?? client).get<GetMeIdentityDocumentsResponses, GetMeIdentityDocumentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/identity-documents',
    ...options
});

/**
 * List the caller's linked providers
 */
export const getMeOauthIdentities = <ThrowOnError extends boolean = false>(options?: Options<GetMeOauthIdentitiesData, ThrowOnError>): RequestResult<GetMeOauthIdentitiesResponses, GetMeOauthIdentitiesErrors, ThrowOnError> => (options?.client ?? client).get<GetMeOauthIdentitiesResponses, GetMeOauthIdentitiesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/oauth-identities',
    ...options
});

/**
 * Unlink a provider
 *
 * Refused when it would leave the account with no way to sign in — no password and no other linked provider.
 */
export const deleteMeOauthIdentitiesByProvider = <ThrowOnError extends boolean = false>(options: Options<DeleteMeOauthIdentitiesByProviderData, ThrowOnError>): RequestResult<DeleteMeOauthIdentitiesByProviderResponses, DeleteMeOauthIdentitiesByProviderErrors, ThrowOnError> => (options.client ?? client).delete<DeleteMeOauthIdentitiesByProviderResponses, DeleteMeOauthIdentitiesByProviderErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/oauth-identities/{provider}',
    ...options
});

/**
 * Update the caller's public profile
 *
 * This is the shop front. `locale` and `timezone` also decide how notifications are written and when they are sent.
 */
export const patchMeProfile = <ThrowOnError extends boolean = false>(options: Options<PatchMeProfileData, ThrowOnError>): RequestResult<PatchMeProfileResponses, PatchMeProfileErrors, ThrowOnError> => (options.client ?? client).patch<PatchMeProfileResponses, PatchMeProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/profile',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reserve a slot to upload an avatar or an identity scan into
 *
 * The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /me/uploads/{id}/confirmation` makes the row real once the object is there. One store serves both `kind`s — an avatar is a public picture and an identity scan is not — because `kind` only narrows the key prefix; what keeps a scan from ever being world-readable is that it is only ever read back through a freshly presigned link, never a public URL.
 *
 * `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
 *
 */
export const postMeUploads = <ThrowOnError extends boolean = false>(options: Options<PostMeUploadsData, ThrowOnError>): RequestResult<PostMeUploadsResponses, PostMeUploadsErrors, ThrowOnError> => (options.client ?? client).post<PostMeUploadsResponses, PostMeUploadsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/uploads',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm the bytes are at the store
 *
 * Until this lands the resource resolves to nothing, so a half-finished upload cannot be set as an avatar or submitted as an identity scan. The recorded size is the store's, not the one declared when the slot was reserved.
 * The answer carries a signed `url` for the bytes just uploaded. That is the only time it is handed over unasked: a second confirmation is refused, and the resource is otherwise only seen through whatever it gets attached to.
 *
 */
export const postMeUploadsByIdConfirmation = <ThrowOnError extends boolean = false>(options: Options<PostMeUploadsByIdConfirmationData, ThrowOnError>): RequestResult<PostMeUploadsByIdConfirmationResponses, PostMeUploadsByIdConfirmationErrors, ThrowOnError> => (options.client ?? client).post<PostMeUploadsByIdConfirmationResponses, PostMeUploadsByIdConfirmationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/me/uploads/{id}/confirmation',
    ...options
});

/**
 * Redact a message
 *
 * The sender unsending, or a moderator acting on an upheld report. The row survives with its body **and its attachments** cleared: a thread is evidence in a refund dispute, so removal is recorded rather than erased, but a redaction that leaves the picture up has not removed anything.
 *
 */
export const deleteMessagesById = <ThrowOnError extends boolean = false>(options: Options<DeleteMessagesByIdData, ThrowOnError>): RequestResult<DeleteMessagesByIdResponses, DeleteMessagesByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteMessagesByIdResponses, DeleteMessagesByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/messages/{id}',
    ...options
});

/**
 * Edit a message
 *
 * Sender only, and it records that the message was edited — the counterparty has to be able to see that what they read has changed. A system message cannot be edited because it has no author.
 *
 */
export const patchMessagesById = <ThrowOnError extends boolean = false>(options: Options<PatchMessagesByIdData, ThrowOnError>): RequestResult<PatchMessagesByIdResponses, PatchMessagesByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchMessagesByIdResponses, PatchMessagesByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/messages/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read the effective notification settings
 *
 * Returns every category and channel pair with the value in force. Stored rows are sparse — one exists only where the account differs from the default — and the defaults live in the domain, so resolving them here keeps the client from having to know them.
 *
 */
export const getNotificationPreferences = <ThrowOnError extends boolean = false>(options?: Options<GetNotificationPreferencesData, ThrowOnError>): RequestResult<GetNotificationPreferencesResponses, GetNotificationPreferencesErrors, ThrowOnError> => (options?.client ?? client).get<GetNotificationPreferencesResponses, GetNotificationPreferencesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/notification-preferences',
    ...options
});

/**
 * Change notification settings
 *
 * Setting a pair back to its default deletes the row rather than storing the default again.
 *
 */
export const putNotificationPreferences = <ThrowOnError extends boolean = false>(options: Options<PutNotificationPreferencesData, ThrowOnError>): RequestResult<PutNotificationPreferencesResponses, PutNotificationPreferencesErrors, ThrowOnError> => (options.client ?? client).put<PutNotificationPreferencesResponses, PutNotificationPreferencesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/notification-preferences',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The caller's notification feed
 *
 * Newest first, cursor only. The table is chunked by time, so a cursor is what lets the reader stop at a chunk boundary instead of touching every chunk.
 *
 */
export const getNotifications = <ThrowOnError extends boolean = false>(options?: Options<GetNotificationsData, ThrowOnError>): RequestResult<GetNotificationsResponses, GetNotificationsErrors, ThrowOnError> => (options?.client ?? client).get<GetNotificationsResponses, GetNotificationsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/notifications',
    ...options
});

/**
 * Mark notifications read up to a point in time
 *
 * Takes a time bound rather than a list of ids. Marking individual rows by id would have to search every chunk of a time-partitioned table; a bound reads one range. Omit `before` to mark everything read.
 *
 */
export const postNotificationsRead = <ThrowOnError extends boolean = false>(options?: Options<PostNotificationsReadData, ThrowOnError>): RequestResult<PostNotificationsReadResponses, PostNotificationsReadErrors, ThrowOnError> => (options?.client ?? client).post<PostNotificationsReadResponses, PostNotificationsReadErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/notifications/read',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * The unread badge
 *
 * A separate endpoint because it is read far more often than the feed and answers from one partial index.
 */
export const getNotificationsUnreadCount = <ThrowOnError extends boolean = false>(options?: Options<GetNotificationsUnreadCountData, ThrowOnError>): RequestResult<GetNotificationsUnreadCountResponses, GetNotificationsUnreadCountErrors, ThrowOnError> => (options?.client ?? client).get<GetNotificationsUnreadCountResponses, GetNotificationsUnreadCountErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/notifications/unread-count',
    ...options
});

/**
 * List the caller's negotiations, both sides at once
 *
 * Both sides in one list, and there is no `role` to pick: an account haggles as a buyer on one listing and as a seller on another, and "which of these is waiting on me" spans the two — `author_id` answers that per row, since whoever owns the standing proposal is whose turn it is *not*. A required `role` was documented here for a while and never read by the service, so both values answered the same list.
 *
 */
export const getOffers = <ThrowOnError extends boolean = false>(options?: Options<GetOffersData, ThrowOnError>): RequestResult<GetOffersResponses, GetOffersErrors, ThrowOnError> => (options?.client ?? client).get<GetOffersResponses, GetOffersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/offers',
    ...options
});

/**
 * Open a price negotiation on a variant
 *
 * The buyer opens it, from the listing page: a seller has nobody to propose to on their own listing, so they answer rather than start. One active negotiation per (buyer, variant); the terms are then revised in place rather than by stacking rows, and each revision posts a card into the pair's chat thread.
 *
 * `total` may not exceed the variant's listed price for that `quantity`. Negotiation only moves the price down: the asking price is already an offer to sell at it, so terms above it are a proposal with no reason to exist — the buyer would simply buy.
 *
 */
export const postOffers = <ThrowOnError extends boolean = false>(options: Options<PostOffersData, ThrowOnError>): RequestResult<PostOffersResponses, PostOffersErrors, ThrowOnError> => (options.client ?? client).post<PostOffersResponses, PostOffersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/offers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel a negotiation
 */
export const deleteOffersById = <ThrowOnError extends boolean = false>(options: Options<DeleteOffersByIdData, ThrowOnError>): RequestResult<DeleteOffersByIdResponses, DeleteOffersByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteOffersByIdResponses, DeleteOffersByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/offers/{id}',
    ...options
});

/**
 * Read a negotiation
 */
export const getOffersById = <ThrowOnError extends boolean = false>(options: Options<GetOffersByIdData, ThrowOnError>): RequestResult<GetOffersByIdResponses, GetOffersByIdErrors, ThrowOnError> => (options.client ?? client).get<GetOffersByIdResponses, GetOffersByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/offers/{id}',
    ...options
});

/**
 * Counter-offer
 *
 * Revises the current terms in place, moves authorship to the caller and posts the new card into the thread. Only the party that does not own the standing proposal may counter, so the two sides alternate.
 *
 * A counter may go *up* — that is a seller holding out — but not above the variant's listed price for that `quantity`, the same ceiling opening one has. The price is read when the counter lands, so a seller who lowered their asking price mid-thread has lowered it here too.
 *
 */
export const patchOffersById = <ThrowOnError extends boolean = false>(options: Options<PatchOffersByIdData, ThrowOnError>): RequestResult<PatchOffersByIdResponses, PatchOffersByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchOffersByIdResponses, PatchOffersByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/offers/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Agree to the terms on the table
 *
 * Whoever does *not* own the standing proposal — the two sides alternate, so the price in front of you is always the other party's, and either of them may be the one who says yes.
 *
 * Agreeing is not the sale, and nothing is charged. It freezes the price and starts a short window for the buyer to press "create order now" (`POST /offers/{id}/checkout`), where they choose delivery and pay — exactly as they would from a fixed-price listing. That separation is what makes a seller accepting a buyer's price safe: no order exists and no money moves until the buyer checks out.
 *
 * The window is short on purpose. An agreed price is a frozen price, and a buyer who lets it lapse negotiates again rather than holding yesterday's number open.
 *
 */
export const postOffersByIdAcceptance = <ThrowOnError extends boolean = false>(options: Options<PostOffersByIdAcceptanceData, ThrowOnError>): RequestResult<PostOffersByIdAcceptanceResponses, PostOffersByIdAcceptanceErrors, ThrowOnError> => (options.client ?? client).post<PostOffersByIdAcceptanceResponses, PostOffersByIdAcceptanceErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/offers/{id}/acceptance',
    ...options
});

/**
 * Create the order now, on agreed terms (buyer)
 *
 * The buyer turning an agreed price into an order: they pick a carrier and an address here and pay, in exactly the checkout a fixed-price listing uses. Only the buyer, because only the buyer pays — and only once, so a double-clicked button opens one payment session.
 *
 * The negotiated total covers the goods. Delivery is quoted from the carrier on top of it and is also the buyer's, the same as on a fixed-price sale; `POST /shipping-quotes` is how the options and their fees are listed before choosing.
 *
 * The order follows when the session completes, with `offer_id` set to this negotiation and no draft.
 *
 */
export const postOffersByIdCheckout = <ThrowOnError extends boolean = false>(options: Options<PostOffersByIdCheckoutData, ThrowOnError>): RequestResult<PostOffersByIdCheckoutResponses, PostOffersByIdCheckoutErrors, ThrowOnError> => (options.client ?? client).post<PostOffersByIdCheckoutResponses, PostOffersByIdCheckoutErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/offers/{id}/checkout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List what may be chosen in one category
 *
 * Where a valid `payment_option` and `transport_option` come from. Operator configuration rather than the caller's data, so it is unpaginated and identical for everybody — but it is not constant: a row an operator disables leaves this list, and a client that hardcoded a slug gets 422 from the route that consumes it instead.
 *
 * Only the id, the name and the description are published. Which implementation serves a row, and which rows are switched off, is the operator's view — see `/admin/options`.
 *
 * A category outside the user-visible set answers 404, exactly as one nobody defined does: telling the two apart would let anyone enumerate the platform's operator surface.
 *
 */
export const getOptions = <ThrowOnError extends boolean = false>(options: Options<GetOptionsData, ThrowOnError>): RequestResult<GetOptionsResponses, GetOptionsErrors, ThrowOnError> => (options.client ?? client).get<GetOptionsResponses, GetOptionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/options',
    ...options
});

/**
 * List the caller's orders, both sides by default
 */
export const getOrders = <ThrowOnError extends boolean = false>(options?: Options<GetOrdersData, ThrowOnError>): RequestResult<GetOrdersResponses, GetOrdersErrors, ThrowOnError> => (options?.client ?? client).get<GetOrdersResponses, GetOrdersErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders',
    ...options
});

/**
 * Read an order
 */
export const getOrdersById = <ThrowOnError extends boolean = false>(options: Options<GetOrdersByIdData, ThrowOnError>): RequestResult<GetOrdersByIdResponses, GetOrdersByIdErrors, ThrowOnError> => (options.client ?? client).get<GetOrdersByIdResponses, GetOrdersByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}',
    ...options
});

/**
 * Cancel an order
 *
 * Either party may cancel while the shipment has not left `pending`. The escrow goes back to the buyer and the sale is reversed in the catalog. No body: there is nowhere to record a reason, and a field a client fills in that reaches nobody is worse than none.
 *
 */
export const postOrdersByIdCancellation = <ThrowOnError extends boolean = false>(options: Options<PostOrdersByIdCancellationData, ThrowOnError>): RequestResult<PostOrdersByIdCancellationResponses, PostOrdersByIdCancellationErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersByIdCancellationResponses, PostOrdersByIdCancellationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}/cancellation',
    ...options
});

/**
 * Accept a paid order (seller)
 *
 * The seller agreeing to the sale, and the only thing that hands the parcel to the carrier. Everything before it — the order row, the escrow, the frozen carrier and fee — is the buyer's side of the transaction and obliges the seller to post nothing. Not re-runnable: a second confirmation would book a second parcel for one sale.
 *
 */
export const postOrdersByIdConfirmation = <ThrowOnError extends boolean = false>(options: Options<PostOrdersByIdConfirmationData, ThrowOnError>): RequestResult<PostOrdersByIdConfirmationResponses, PostOrdersByIdConfirmationErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersByIdConfirmationResponses, PostOrdersByIdConfirmationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}/confirmation',
    ...options
});

/**
 * Refuse a paid order (seller)
 *
 * Out of stock, mispriced, whatever they say. The escrow goes back in full, delivery included, because the parcel never left. Same destination as letting the confirmation window pass, reached without making the buyer wait out a clock for an answer the seller already has — which is why the reason is required and kept.
 *
 */
export const postOrdersByIdDecline = <ThrowOnError extends boolean = false>(options: Options<PostOrdersByIdDeclineData, ThrowOnError>): RequestResult<PostOrdersByIdDeclineResponses, PostOrdersByIdDeclineErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersByIdDeclineResponses, PostOrdersByIdDeclineErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}/decline',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm receipt of the goods (buyer)
 *
 * The buyer confirms delivery and attaches unboxing evidence — at least one photo or video is mandatory, because this evidence is what a later refund is judged on. Sets `received_at` and starts the 72-hour payout countdown in `payout_deadline_at`.
 *
 * Confirming twice is a conflict rather than a no-op: a retried upload must not be able to restart the clock and move the payout date.
 *
 */
export const postOrdersByIdReceipt = <ThrowOnError extends boolean = false>(options: Options<PostOrdersByIdReceiptData, ThrowOnError>): RequestResult<PostOrdersByIdReceiptResponses, PostOrdersByIdReceiptErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersByIdReceiptResponses, PostOrdersByIdReceiptErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}/receipt',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Open a refund request (buyer)
 *
 * Covers the whole order; there is no way to refund part of one, and one live refund per order. Evidence is optional here and can be added until the case closes.
 *
 * Nothing ships yet — the buyer keeps the goods until the refund is granted. The seller has until `deadline_at` to accept or reject, and letting that pass hands the next move to the buyer rather than settling anything.
 *
 */
export const postOrdersByIdRefunds = <ThrowOnError extends boolean = false>(options: Options<PostOrdersByIdRefundsData, ThrowOnError>): RequestResult<PostOrdersByIdRefundsResponses, PostOrdersByIdRefundsErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersByIdRefundsResponses, PostOrdersByIdRefundsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}/refunds',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read the shipment
 */
export const getOrdersByIdTransport = <ThrowOnError extends boolean = false>(options: Options<GetOrdersByIdTransportData, ThrowOnError>): RequestResult<GetOrdersByIdTransportResponses, GetOrdersByIdTransportErrors, ThrowOnError> => (options.client ?? client).get<GetOrdersByIdTransportResponses, GetOrdersByIdTransportErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}/transport',
    ...options
});

/**
 * Correct a checkpoint on the outbound leg (moderator)
 *
 * Staff only. The carrier reports this leg on its own webhook, and this route is for a courier that got it wrong or never reported at all — neither party to the order writes it. Whether the parcel has left is what decides whether the order can still be cancelled and the escrow taken back, so a seller saying so was one request against days of the buyer's: a seller who sees the status wrong raises an `order-issue` ticket instead.
 *
 * Forward-only. Carrier reports arrive out of order, and whether the parcel has left is what decides whether the order can still be cancelled and the escrow taken back, so it is not a fact a later report may undo: a checkpoint at or behind where the shipment already is, or one on a leg that has ended, is 409.
 *
 */
export const postOrdersByIdTransportCheckpoints = <ThrowOnError extends boolean = false>(options: Options<PostOrdersByIdTransportCheckpointsData, ThrowOnError>): RequestResult<PostOrdersByIdTransportCheckpointsResponses, PostOrdersByIdTransportCheckpointsErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersByIdTransportCheckpointsResponses, PostOrdersByIdTransportCheckpointsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{id}/transport/checkpoints',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read the feedback on one order
 *
 * Returns the caller's own submission always. The counterparty's arrives as `theirs` only once it is published; while it is still blind `theirs` is null and `theirs_submitted` says whether there is anything waiting, which is as much as can be told without breaking blindness.
 *
 */
export const getOrdersByOrderIdFeedback = <ThrowOnError extends boolean = false>(options: Options<GetOrdersByOrderIdFeedbackData, ThrowOnError>): RequestResult<GetOrdersByOrderIdFeedbackResponses, GetOrdersByOrderIdFeedbackErrors, ThrowOnError> => (options.client ?? client).get<GetOrdersByOrderIdFeedbackResponses, GetOrdersByOrderIdFeedbackErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{orderID}/feedback',
    ...options
});

/**
 * Rate the counterparty of a completed order
 *
 * One submission per direction per order, so this cannot be revised — a rating that could be edited after seeing the other side is not blind.
 *
 * The direction is derived from which side of the order the caller is on rather than sent, so nobody can file feedback as the other party.
 *
 */
export const postOrdersByOrderIdFeedback = <ThrowOnError extends boolean = false>(options: Options<PostOrdersByOrderIdFeedbackData, ThrowOnError>): RequestResult<PostOrdersByOrderIdFeedbackResponses, PostOrdersByOrderIdFeedbackErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersByOrderIdFeedbackResponses, PostOrdersByOrderIdFeedbackErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/{orderID}/feedback',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The caller's own sales or purchases over a window
 *
 * What a seller's dashboard leads with. The window filters `created_at`, so the counts, the totals and the daily series all describe **one** set of orders — the ones placed in it, as they stand now. Mixing "placed in the window" with "completed in the window" would produce three numbers the reader cannot reconcile.
 *
 * `totals` counts the goods of completed orders only, and excludes a cancelled line and the delivery fee: the fee is the courier's money and never reaches the seller. It is a list per currency because a shop may price in more than one, and adding those together is a figure that means nothing — which is also why the daily series carries counts and no money.
 *
 * Absent bounds mean the last 30 days ending now; `to` is exclusive and the window may span at most a year, so the series is at most 366 buckets.
 *
 */
export const getOrdersSummary = <ThrowOnError extends boolean = false>(options: Options<GetOrdersSummaryData, ThrowOnError>): RequestResult<GetOrdersSummaryResponses, GetOrdersSummaryErrors, ThrowOnError> => (options.client ?? client).get<GetOrdersSummaryResponses, GetOrdersSummaryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/summary',
    ...options
});

/**
 * Reserve a slot to upload evidence into
 *
 * The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /orders/uploads/{id}/confirmation` makes the row real once the object is there — so a receipt confirmation or a refund can never be judged on a photo whose bytes never arrived.
 *
 * `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
 *
 */
export const postOrdersUploads = <ThrowOnError extends boolean = false>(options: Options<PostOrdersUploadsData, ThrowOnError>): RequestResult<PostOrdersUploadsResponses, PostOrdersUploadsErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersUploadsResponses, PostOrdersUploadsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/uploads',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm the bytes are at the store
 *
 * Until this lands the resource resolves to nothing, so a half-finished upload cannot be named as evidence on a receipt confirmation or a refund. The recorded size is the store's, not the one declared when the slot was reserved.
 * The answer carries a signed `url` for the bytes just uploaded. That is the only time it is handed over unasked: a second confirmation is refused, and the resource is otherwise only seen through whatever it gets attached to.
 *
 */
export const postOrdersUploadsByIdConfirmation = <ThrowOnError extends boolean = false>(options: Options<PostOrdersUploadsByIdConfirmationData, ThrowOnError>): RequestResult<PostOrdersUploadsByIdConfirmationResponses, PostOrdersUploadsByIdConfirmationErrors, ThrowOnError> => (options.client ?? client).post<PostOrdersUploadsByIdConfirmationResponses, PostOrdersUploadsByIdConfirmationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/orders/uploads/{id}/confirmation',
    ...options
});

/**
 * Change the caller's password
 *
 * Requires the current password even though the caller is already authenticated — a stolen access token must not be enough to take the account.
 */
export const putPassword = <ThrowOnError extends boolean = false>(options: Options<PutPasswordData, ThrowOnError>): RequestResult<PutPasswordResponses, PutPasswordErrors, ThrowOnError> => (options.client ?? client).put<PutPasswordResponses, PutPasswordErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/password',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ask for a password reset
 *
 * Always answers 202, whether or not the identifier exists. Reporting that an address is unknown turns this endpoint into a way to enumerate accounts.
 *
 */
export const postPasswordResetRequests = <ThrowOnError extends boolean = false>(options: Options<PostPasswordResetRequestsData, ThrowOnError>): RequestResult<PostPasswordResetRequestsResponses, PostPasswordResetRequestsErrors, ThrowOnError> => (options.client ?? client).post<PostPasswordResetRequestsResponses, PostPasswordResetRequestsErrors, ThrowOnError>({
    url: '/password/reset-requests',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set a new password with a reset token
 */
export const postPasswordResets = <ThrowOnError extends boolean = false>(options: Options<PostPasswordResetsData, ThrowOnError>): RequestResult<PostPasswordResetsResponses, PostPasswordResetsErrors, ThrowOnError> => (options.client ?? client).post<PostPasswordResetsResponses, PostPasswordResetsErrors, ThrowOnError>({
    url: '/password/resets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the caller's payment sessions
 *
 * `role=payer` is money owed or sent, `role=payee` is money received — seller payouts and refunds land on that side.
 */
export const getPaymentSessions = <ThrowOnError extends boolean = false>(options?: Options<GetPaymentSessionsData, ThrowOnError>): RequestResult<GetPaymentSessionsResponses, GetPaymentSessionsErrors, ThrowOnError> => (options?.client ?? client).get<GetPaymentSessionsResponses, GetPaymentSessionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/payment-sessions',
    ...options
});

/**
 * Read a payment session
 *
 * `outstanding` is the total less what has already settled on a rail, so a client can tell a partly-tendered session from an unpaid one without fetching the legs.
 *
 */
export const getPaymentSessionsById = <ThrowOnError extends boolean = false>(options: Options<GetPaymentSessionsByIdData, ThrowOnError>): RequestResult<GetPaymentSessionsByIdResponses, GetPaymentSessionsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetPaymentSessionsByIdResponses, GetPaymentSessionsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/payment-sessions/{id}',
    ...options
});

/**
 * Abandon an unpaid payment session
 *
 * Only while nothing has settled. A session with a settled leg is closed by refunding, not by cancelling.
 */
export const postPaymentSessionsByIdCancellation = <ThrowOnError extends boolean = false>(options: Options<PostPaymentSessionsByIdCancellationData, ThrowOnError>): RequestResult<PostPaymentSessionsByIdCancellationResponses, PostPaymentSessionsByIdCancellationErrors, ThrowOnError> => (options.client ?? client).post<PostPaymentSessionsByIdCancellationResponses, PostPaymentSessionsByIdCancellationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/payment-sessions/{id}/cancellation',
    ...options
});

/**
 * Pay a session through one rail
 *
 * Creates a ledger leg on the chosen rail and returns it with the gateway redirect in `checkout_url`. Omit `amount` to tender the whole outstanding balance; pass it to split the session across several rails, calling this again per rail until the total is covered.
 *
 * Tendering a rail that already has an unfinished attempt **resumes** it: the same leg comes back with the same `checkout_url`, and `amount` is ignored. A redirect rail reports nothing when the payer closes its tab, so a second live gateway page would be one the session cannot account for — whichever payment landed second would have no outstanding balance left to settle against.
 *
 * The leg starts `pending` and only the provider's webhook settles it — this response is not a receipt. Poll the session or the leg for the outcome.
 *
 */
export const postPaymentSessionsByIdPayments = <ThrowOnError extends boolean = false>(options: Options<PostPaymentSessionsByIdPaymentsData, ThrowOnError>): RequestResult<PostPaymentSessionsByIdPaymentsResponses, PostPaymentSessionsByIdPaymentsErrors, ThrowOnError> => (options.client ?? client).post<PostPaymentSessionsByIdPaymentsResponses, PostPaymentSessionsByIdPaymentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/payment-sessions/{id}/payments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the legs of a payment session
 *
 * Append-only. A reversal is a new leg with a negative amount pointing at the leg it reverses, never an edit of the original.
 */
export const getPaymentSessionsByIdTransactions = <ThrowOnError extends boolean = false>(options: Options<GetPaymentSessionsByIdTransactionsData, ThrowOnError>): RequestResult<GetPaymentSessionsByIdTransactionsResponses, GetPaymentSessionsByIdTransactionsErrors, ThrowOnError> => (options.client ?? client).get<GetPaymentSessionsByIdTransactionsResponses, GetPaymentSessionsByIdTransactionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/payment-sessions/{id}/transactions',
    ...options
});

/**
 * List the caller's refunds, both sides by default
 *
 * Omit `role` for both sides, which is what "what is waiting on me" needs: a buyer's own claims and the ones raised against their sales are the same queue of things to answer. `role=seller` resolves through the order's seller, because a refund row itself only records the buyer.
 *
 */
export const getRefunds = <ThrowOnError extends boolean = false>(options?: Options<GetRefundsData, ThrowOnError>): RequestResult<GetRefundsResponses, GetRefundsErrors, ThrowOnError> => (options?.client ?? client).get<GetRefundsResponses, GetRefundsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/refunds',
    ...options
});

/**
 * Withdraw a refund request (buyer)
 *
 * Only while the seller has not decided. Once anything has moved — a decision, an escalation, a parcel — the case is closed by a verdict, not by walking away from it.
 *
 * The case ends as `cancelled`, which gives the escrow back up: the payout becomes the seller's again.
 *
 */
export const deleteRefundsById = <ThrowOnError extends boolean = false>(options: Options<DeleteRefundsByIdData, ThrowOnError>): RequestResult<DeleteRefundsByIdResponses, DeleteRefundsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteRefundsByIdResponses, DeleteRefundsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/refunds/{id}',
    ...options
});

/**
 * Read a refund and its case
 *
 * The case: `deadline_at` for whoever is on the clock, and the facts each stage recorded. A party sees a staff verdict in the status the case ended up in.
 *
 */
export const getRefundsById = <ThrowOnError extends boolean = false>(options: Options<GetRefundsByIdData, ThrowOnError>): RequestResult<GetRefundsByIdResponses, GetRefundsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetRefundsByIdResponses, GetRefundsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/refunds/{id}',
    ...options
});

/**
 * Accept a refund (seller)
 *
 * Grants it without staff. The goods have to come back first, so this books the return shipment on the same carrier as the outbound leg and moves the refund to `returning`; the money goes back once the seller has reported that leg delivered and has not escalated what arrived. No body — there is nothing left to choose.
 *
 */
export const postRefundsByIdAcceptance = <ThrowOnError extends boolean = false>(options: Options<PostRefundsByIdAcceptanceData, ThrowOnError>): RequestResult<PostRefundsByIdAcceptanceResponses, PostRefundsByIdAcceptanceErrors, ThrowOnError> => (options.client ?? client).post<PostRefundsByIdAcceptanceResponses, PostRefundsByIdAcceptanceErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/refunds/{id}/acceptance',
    ...options
});

/**
 * Add evidence to a refund case
 *
 * The buyer, while the case is open. A closed one is the record a verdict was reached on, so nothing is added to it afterwards. The seller does not write here — they answer by opening a `refund-dispute` ticket, and that thread carries their evidence.
 *
 */
export const postRefundsByIdAttachments = <ThrowOnError extends boolean = false>(options: Options<PostRefundsByIdAttachmentsData, ThrowOnError>): RequestResult<PostRefundsByIdAttachmentsResponses, PostRefundsByIdAttachmentsErrors, ThrowOnError> => (options.client ?? client).post<PostRefundsByIdAttachmentsResponses, PostRefundsByIdAttachmentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/refunds/{id}/attachments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Report a checkpoint on the leg carrying the goods back
 *
 * The return leg exists only from the moment a refund is granted, and this is the only exit from `returning`. Either party to the order may report it, unlike the outbound leg: this one is never booked with a carrier, so no webhook can report it and requiring the seller alone would let one who simply never confirms strand the escrow with nobody on a clock.
 *
 * Who reports `delivered` is what decides where the case goes. From the **seller** it is an acknowledgement that the goods arrived, so the refund moves to `returned` and their inspection window opens — and letting that window pass settles for the buyer, which is their own silence costing them. From the **buyer** it is a claim about somebody else's warehouse, so the refund moves to `disputed` and staff decide: on the inspection window a buyer who posted nothing at all had only to report `delivered` and wait out an inattentive seller to keep both the money and the goods.
 *
 * Reporting a position short of `delivered` moves the leg and leaves the case in `returning` whoever sent it.
 *
 */
export const postRefundsByIdReturnTransportCheckpoints = <ThrowOnError extends boolean = false>(options: Options<PostRefundsByIdReturnTransportCheckpointsData, ThrowOnError>): RequestResult<PostRefundsByIdReturnTransportCheckpointsResponses, PostRefundsByIdReturnTransportCheckpointsErrors, ThrowOnError> => (options.client ?? client).post<PostRefundsByIdReturnTransportCheckpointsResponses, PostRefundsByIdReturnTransportCheckpointsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/refunds/{id}/return-transport/checkpoints',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create an account
 *
 * At least one of email, phone or username is required — an account nobody can be addressed by cannot sign in. The display profile is created in the same transaction, so an account without a name is never a reachable state.
 *
 * Always creates a plain user. Moderators come from `POST /admin/moderators`.
 *
 */
export const postRegister = <ThrowOnError extends boolean = false>(options: Options<PostRegisterData, ThrowOnError>): RequestResult<PostRegisterResponses, PostRegisterErrors, ThrowOnError> => (options.client ?? client).post<PostRegisterResponses, PostRegisterErrors, ThrowOnError>({
    url: '/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a reply
 *
 * The author, or a moderator acting on an upheld report.
 */
export const deleteReviewRepliesById = <ThrowOnError extends boolean = false>(options: Options<DeleteReviewRepliesByIdData, ThrowOnError>): RequestResult<DeleteReviewRepliesByIdResponses, DeleteReviewRepliesByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteReviewRepliesByIdResponses, DeleteReviewRepliesByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/review-replies/{id}',
    ...options
});

/**
 * Delete a review
 *
 * The author, or a moderator acting on an upheld report. Removal drops it from the seller's rating too, so the recomputed reputation follows.
 *
 */
export const deleteReviewsById = <ThrowOnError extends boolean = false>(options: Options<DeleteReviewsByIdData, ThrowOnError>): RequestResult<DeleteReviewsByIdResponses, DeleteReviewsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteReviewsByIdResponses, DeleteReviewsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/reviews/{id}',
    ...options
});

/**
 * Read a review
 *
 * Carries the whole reply thread, unlike the listing page, which caps it.
 */
export const getReviewsById = <ThrowOnError extends boolean = false>(options: Options<GetReviewsByIdData, ThrowOnError>): RequestResult<GetReviewsByIdResponses, GetReviewsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetReviewsByIdResponses, GetReviewsByIdErrors, ThrowOnError>({ url: '/reviews/{id}', ...options });

/**
 * Edit your own review
 */
export const patchReviewsById = <ThrowOnError extends boolean = false>(options: Options<PatchReviewsByIdData, ThrowOnError>): RequestResult<PatchReviewsByIdResponses, PatchReviewsByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchReviewsByIdResponses, PatchReviewsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/reviews/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reply to a review
 *
 * Mainly the seller answering, though anyone may add to the thread. There is no per-thread limit — a conversation that stops after one reply each is not a conversation.
 *
 */
export const postReviewsByIdReplies = <ThrowOnError extends boolean = false>(options: Options<PostReviewsByIdRepliesData, ThrowOnError>): RequestResult<PostReviewsByIdRepliesResponses, PostReviewsByIdRepliesErrors, ThrowOnError> => (options.client ?? client).post<PostReviewsByIdRepliesResponses, PostReviewsByIdRepliesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/reviews/{id}/replies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Withdraw your vote
 *
 * Removes the row rather than storing a neutral one. A stored zero is a row that says nothing.
 *
 */
export const deleteReviewsByIdVote = <ThrowOnError extends boolean = false>(options: Options<DeleteReviewsByIdVoteData, ThrowOnError>): RequestResult<DeleteReviewsByIdVoteResponses, DeleteReviewsByIdVoteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteReviewsByIdVoteResponses, DeleteReviewsByIdVoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/reviews/{id}/vote',
    ...options
});

/**
 * Vote a review helpful or not
 *
 * One vote per account per review, replaced in place. Voting on your own review is refused.
 */
export const putReviewsByIdVote = <ThrowOnError extends boolean = false>(options: Options<PutReviewsByIdVoteData, ThrowOnError>): RequestResult<PutReviewsByIdVoteResponses, PutReviewsByIdVoteErrors, ThrowOnError> => (options.client ?? client).put<PutReviewsByIdVoteResponses, PutReviewsByIdVoteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/reviews/{id}/vote',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reserve a slot to upload a review photo into
 *
 * The bytes never pass through this API. The answer is a short-lived signed URL to PUT to, and `POST /reviews/uploads/{id}/confirmation` makes the row real once the object is there — so a review can never render a photo whose bytes never arrived.
 *
 * `mime` and `size` are checked before a byte moves: a slot signed for anything is a slot for anything.
 *
 */
export const postReviewsUploads = <ThrowOnError extends boolean = false>(options: Options<PostReviewsUploadsData, ThrowOnError>): RequestResult<PostReviewsUploadsResponses, PostReviewsUploadsErrors, ThrowOnError> => (options.client ?? client).post<PostReviewsUploadsResponses, PostReviewsUploadsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/reviews/uploads',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Confirm the bytes are at the store
 *
 * Until this lands the resource resolves to nothing, so a half-finished upload cannot be attached to a review. The recorded size is the store's, not the one declared when the slot was reserved.
 * The answer carries a signed `url` for the bytes just uploaded. That is the only time it is handed over unasked: a second confirmation is refused, and the resource is otherwise only seen through whatever it gets attached to.
 *
 */
export const postReviewsUploadsByIdConfirmation = <ThrowOnError extends boolean = false>(options: Options<PostReviewsUploadsByIdConfirmationData, ThrowOnError>): RequestResult<PostReviewsUploadsByIdConfirmationResponses, PostReviewsUploadsByIdConfirmationErrors, ThrowOnError> => (options.client ?? client).post<PostReviewsUploadsByIdConfirmationResponses, PostReviewsUploadsByIdConfirmationErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/reviews/uploads/{id}/confirmation',
    ...options
});

/**
 * What delivery would cost, per carrier
 *
 * One entry per enabled carrier that could price the parcel, so the buyer sees delivery before they pay for it. A carrier that declined is simply absent.
 *
 * Name exactly one source. `variant_id` is the listing page's question — what would this cost to send — answered before any draft exists, with `quantity` defaulting to one and the caller's default address used when `contact_id` is omitted. `draft_id` and `offer_id` are the two things that freeze a price, and the parcel is whichever of them is about to be checked out. The same list serves all three, because the buyer pays carriage on a fixed-price sale and a negotiated one alike.
 *
 * An estimate a client renders, not a price it can hold: the fee is quoted again at checkout, so a quote kept past the point it was true does not become the charge. Show it as its own line beside the price rather than folded into it — the total a buyer is charged is the checkout's, itemised.
 *
 */
export const postShippingQuotes = <ThrowOnError extends boolean = false>(options: Options<PostShippingQuotesData, ThrowOnError>): RequestResult<PostShippingQuotesResponses, PostShippingQuotesErrors, ThrowOnError> => (options.client ?? client).post<PostShippingQuotesResponses, PostShippingQuotesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/shipping-quotes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The tag dictionary
 *
 * A tag id is its slug and a natural key, so it is readable on the wire rather than encoded.
 *
 * Paginated, unlike the category tree: categories are a curated tree that stays small, while tags grow with the catalog. `q` is a prefix match on the slug, which is what the tag picker types into.
 *
 * `near` is the other half of that picker: it ranks the dictionary by cosine distance over `tag_embedding` and returns a `score`, so picking `handmade` offers back the tags that sit next to it in the vector space. The seeds themselves are never in the result — they are already on the listing — and a tag the embedding cron has not reached yet cannot be suggested at all, because `tag` carries no text index to fall back to.
 *
 * `q` and `near` are mutually exclusive: one filters the dictionary by what was typed, the other reorders it by meaning, and combining them would rank a set the prefix already decided.
 *
 */
export const getTags = <ThrowOnError extends boolean = false>(options?: Options<GetTagsData, ThrowOnError>): RequestResult<GetTagsResponses, GetTagsErrors, ThrowOnError> => (options?.client ?? client).get<GetTagsResponses, GetTagsErrors, ThrowOnError>({
    querySerializer: { parameters: { near: { array: { explode: false } } } },
    url: '/tags',
    ...options
});

/**
 * Read the caller's tax registration
 */
export const getTaxInfo = <ThrowOnError extends boolean = false>(options?: Options<GetTaxInfoData, ThrowOnError>): RequestResult<GetTaxInfoResponses, GetTaxInfoErrors, ThrowOnError> => (options?.client ?? client).get<GetTaxInfoResponses, GetTaxInfoErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/tax-info',
    ...options
});

/**
 * Register or replace the caller's tax registration
 *
 * One registration per account, so this is an upsert rather than a create. Replacing it resets verification to `pending`: the previous verdict was about the previous tax code. A code already verified under another account is rejected.
 *
 */
export const putTaxInfo = <ThrowOnError extends boolean = false>(options: Options<PutTaxInfoData, ThrowOnError>): RequestResult<PutTaxInfoResponses, PutTaxInfoErrors, ThrowOnError> => (options.client ?? client).put<PutTaxInfoResponses, PutTaxInfoErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/tax-info',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Tickets the caller raised
 *
 * Their own, newest first. A requester never sees who else reported the same target, nor which moderator is working theirs.
 */
export const getTickets = <ThrowOnError extends boolean = false>(options?: Options<GetTicketsData, ThrowOnError>): RequestResult<GetTicketsResponses, GetTicketsErrors, ThrowOnError> => (options?.client ?? client).get<GetTicketsResponses, GetTicketsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/tickets',
    ...options
});

/**
 * Raise a ticket
 *
 * `ref_id` is required exactly for the kinds that are about something — the report kinds, `refund-dispute`, `order-issue` — and its prefix has to agree with the kind. `reason` is required for the five `report-*` kinds and refused on every other one: it is what a report says is wrong. Both rules depend on `kind`, so neither can be a `required` in the schema — breaking either is 422.
 *
 * `body` and `attachments` open the thread: they are posted as the first message, and everything said afterwards is ordinary chat.
 *
 * One open ticket per target per requester, so raising the same complaint twice while the first is still open is a conflict rather than a second row. A `refund-dispute` also moves the refund into staff's hands first, so order's own guards — the right party, the right moment — are what answer 403 and 409 here.
 *
 * A `refund-dispute` names the **order**, not the refund: one live refund per sale is a database index, so which case it is stays order's to resolve, and every dispute about that sale then files against one target. That is what keeps a seller's complaint about what came back in the thread they were already using. It is also raised for a buyer automatically when the seller lets the 48-hour review window pass — silence is not a verdict, and the buyer is not asked to chase a case they already opened.
 *
 */
export const postTickets = <ThrowOnError extends boolean = false>(options: Options<PostTicketsData, ThrowOnError>): RequestResult<PostTicketsResponses, PostTicketsErrors, ThrowOnError> => (options.client ?? client).post<PostTicketsResponses, PostTicketsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/tickets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read a ticket the caller raised
 *
 * Somebody else's is 404 rather than 403: a ticket id is guessable, and confirming one exists already says something.
 *
 * This also repairs a missing `conversation_id`. The row and the thread live in different schemas, so one lands first — a ticket whose thread was never written is answerable on the next read rather than mute.
 *
 */
export const getTicketsById = <ThrowOnError extends boolean = false>(options: Options<GetTicketsByIdData, ThrowOnError>): RequestResult<GetTicketsByIdResponses, GetTicketsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetTicketsByIdResponses, GetTicketsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/tickets/{id}',
    ...options
});

/**
 * Exchange a refresh token for a new access token
 *
 * The refresh token is a Redis key. One that has been revoked or has expired is indistinguishable from one that never existed.
 */
export const postTokenRefresh = <ThrowOnError extends boolean = false>(options: Options<PostTokenRefreshData, ThrowOnError>): RequestResult<PostTokenRefreshResponses, PostTokenRefreshErrors, ThrowOnError> => (options.client ?? client).post<PostTokenRefreshResponses, PostTokenRefreshErrors, ThrowOnError>({
    url: '/token/refresh',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a variant
 *
 * Soft delete for the same reason as a listing. Refused for the last remaining variant of a live listing.
 *
 * Answers with the listing rather than an empty body: deleting the featured variant moves the flag to a surviving one, and the seller has to see which.
 *
 */
export const deleteVariantsById = <ThrowOnError extends boolean = false>(options: Options<DeleteVariantsByIdData, ThrowOnError>): RequestResult<DeleteVariantsByIdResponses, DeleteVariantsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteVariantsByIdResponses, DeleteVariantsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/variants/{id}',
    ...options
});

/**
 * Edit a variant
 *
 * A price change does not reach a buyer who already opened a purchase session: that session froze the terms it was opened with.
 *
 * `quantity` sets the total on hand. Neither `reserved` nor `sold` is settable — checkout and cancellation move the first, completing a purchase moves both — and a total below `reserved + sold` is refused, because an oversold row must not be representable.
 *
 */
export const patchVariantsById = <ThrowOnError extends boolean = false>(options: Options<PatchVariantsByIdData, ThrowOnError>): RequestResult<PatchVariantsByIdResponses, PatchVariantsByIdErrors, ThrowOnError> => (options.client ?? client).patch<PatchVariantsByIdResponses, PatchVariantsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/variants/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the caller's wallet balances
 *
 * One wallet per currency, so this is a list rather than a single object. It is short and unpaginated: a balance is opened by receiving money in that currency, not by browsing.
 *
 */
export const getWallets = <ThrowOnError extends boolean = false>(options?: Options<GetWalletsData, ThrowOnError>): RequestResult<GetWalletsResponses, GetWalletsErrors, ThrowOnError> => (options?.client ?? client).get<GetWalletsResponses, GetWalletsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wallets',
    ...options
});

/**
 * Read one currency balance
 */
export const getWalletsByCurrency = <ThrowOnError extends boolean = false>(options: Options<GetWalletsByCurrencyData, ThrowOnError>): RequestResult<GetWalletsByCurrencyResponses, GetWalletsByCurrencyErrors, ThrowOnError> => (options.client ?? client).get<GetWalletsByCurrencyResponses, GetWalletsByCurrencyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wallets/{currency}',
    ...options
});

/**
 * Read one wallet's ledger
 *
 * Every balance change with its before/after snapshot, newest first. Ordered by `seq`, not by time: two movements can share a timestamp, and a ledger that cannot be totally ordered cannot be replayed or shown to be complete. A gap in `seq` means a missing row.
 *
 * Legs of one logical movement share a `ref_type`/`ref_id` — a checkout is a debit plus an escrow hold plus a fee, all three against the same payment session — which is what makes "does this add up" answerable. An adjustment references nothing, so its legs are grouped only by `seq` adjacency.
 *
 */
export const getWalletsByCurrencyTransactions = <ThrowOnError extends boolean = false>(options: Options<GetWalletsByCurrencyTransactionsData, ThrowOnError>): RequestResult<GetWalletsByCurrencyTransactionsResponses, GetWalletsByCurrencyTransactionsErrors, ThrowOnError> => (options.client ?? client).get<GetWalletsByCurrencyTransactionsResponses, GetWalletsByCurrencyTransactionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/wallets/{currency}/transactions',
    ...options
});

/**
 * List the caller's withdrawals
 */
export const getWithdrawals = <ThrowOnError extends boolean = false>(options?: Options<GetWithdrawalsData, ThrowOnError>): RequestResult<GetWithdrawalsResponses, GetWithdrawalsErrors, ThrowOnError> => (options?.client ?? client).get<GetWithdrawalsResponses, GetWithdrawalsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/withdrawals',
    ...options
});

/**
 * Request a cash-out to a bank account
 *
 * Debits the available balance and opens a withdrawal for review; an admin resolves it before money leaves the platform. Only the available balance can be drawn — money still held in escrow is not the caller's yet.
 *
 */
export const postWithdrawals = <ThrowOnError extends boolean = false>(options: Options<PostWithdrawalsData, ThrowOnError>): RequestResult<PostWithdrawalsResponses, PostWithdrawalsErrors, ThrowOnError> => (options.client ?? client).post<PostWithdrawalsResponses, PostWithdrawalsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/withdrawals',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Withdraw the request and return the money to the balance
 */
export const deleteWithdrawalsById = <ThrowOnError extends boolean = false>(options: Options<DeleteWithdrawalsByIdData, ThrowOnError>): RequestResult<DeleteWithdrawalsByIdResponses, DeleteWithdrawalsByIdErrors, ThrowOnError> => (options.client ?? client).delete<DeleteWithdrawalsByIdResponses, DeleteWithdrawalsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/withdrawals/{id}',
    ...options
});

/**
 * Read a withdrawal
 *
 * A withdrawal is a payment session of kind `withdrawal`, so its id carries the session prefix.
 */
export const getWithdrawalsById = <ThrowOnError extends boolean = false>(options: Options<GetWithdrawalsByIdData, ThrowOnError>): RequestResult<GetWithdrawalsByIdResponses, GetWithdrawalsByIdErrors, ThrowOnError> => (options.client ?? client).get<GetWithdrawalsByIdResponses, GetWithdrawalsByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/withdrawals/{id}',
    ...options
});

/**
 * Mint a WebSocket handshake ticket
 *
 * Returns a single-use ticket for `GET /ws`. Valid for 30 seconds and destroyed
 * on redemption, so every reconnect needs a fresh one.
 *
 * This exists because a browser cannot set an Authorization header on
 * `new WebSocket()`. Putting the access token in the query string instead would
 * write a live credential into proxy logs and browser history.
 *
 * The realtime surface itself is described in asyncapi.yaml.
 *
 */
export const createWebSocketTicket = <ThrowOnError extends boolean = false>(options?: Options<CreateWebSocketTicketData, ThrowOnError>): RequestResult<CreateWebSocketTicketResponses, CreateWebSocketTicketErrors, ThrowOnError> => (options?.client ?? client).post<CreateWebSocketTicketResponses, CreateWebSocketTicketErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/ws/tickets',
    ...options
});
