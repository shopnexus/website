// Generated by scripts/gen-ws-events.mjs from ../server/api/asyncapi.gen.yaml. Do not edit.
//
// The realtime events the backend may push over the WebSocket. Discriminated on `code`,
// so a handler switch is exhaustive and adding an event server-side turns every
// incomplete switch into a type error.

import type {
	ConversationReadMark,
	DeletedMessageRef,
	Message,
	Notification,
	Offer,
	OrderRef,
} from "./types.gen"

export type RealtimeEvent =
	/** A new row in your notification feed. */
	| { code: "account.notification_created"; at: string; data: Notification }
	/** The other participant read the thread up to an instant. */
	| { code: "chat.conversation_read"; at: string; data: ConversationReadMark }
	/** Somebody sent a message in a conversation you are part of. */
	| { code: "chat.message_created"; at: string; data: Message }
	/** The other participant deleted a message. */
	| { code: "chat.message_deleted"; at: string; data: DeletedMessageRef }
	/** The other participant edited a message; body and edited_at changed. */
	| { code: "chat.message_updated"; at: string; data: Message }
	/** A negotiation's standing terms changed — countered, accepted, withdrawn or expired. */
	| { code: "order.offer_updated"; at: string; data: Offer }
	/** A paid checkout became an order. Sent to buyer and seller. */
	| { code: "order.placed"; at: string; data: OrderRef }
	/** Escrow closed and the payout was released. Sent to buyer and seller. */
	| { code: "order.settled"; at: string; data: OrderRef }

export type RealtimeCode = RealtimeEvent["code"]

/** Every code, for runtime validation of a message off the wire. */
export const REALTIME_CODES: readonly RealtimeCode[] = [
	"account.notification_created",
	"chat.conversation_read",
	"chat.message_created",
	"chat.message_deleted",
	"chat.message_updated",
	"order.offer_updated",
	"order.placed",
	"order.settled",
] as const
